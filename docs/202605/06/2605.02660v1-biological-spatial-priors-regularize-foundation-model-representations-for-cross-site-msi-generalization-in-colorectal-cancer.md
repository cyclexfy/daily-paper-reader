---
title: Biological Spatial Priors Regularize Foundation Model Representations for Cross-Site MSI Generalization in Colorectal Cancer
title_zh: 生物空间先验正则化基础模型表示以实现结直肠癌跨中心 MSI 泛化
authors: Dasari Naga Raju
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.02660v1"
tags: ["query:cpath"]
score: 10.0
evidence: 正则化基础模型表示以用于结直肠癌全切片图像的 MSI 预测
tldr: 本研究针对结直肠癌中微卫星不稳定（MSI）预测模型在跨中心应用时泛化性差的问题，提出利用生物学空间先验来正则化基础模型表征。通过在TransMIL聚合器中引入反映肿瘤边缘淋巴细胞反应的边缘距离编码和局部免疫邻域编码，引导模型学习更具生物学意义且站点无关的特征。实验表明，该方法在TCGA-COAD训练并直接在TCGA-READ测试时，显著提升了跨站点泛化性能，证明了生物学先验在减少站点特异性干扰方面的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.02660v1/fig-001.webp\", \"caption\": \"Figure 1: Pipeline for MSI prediction from whole slide images (H&E). Tile features are extracted using a foundation model, augmented with a peripheral distance (PD) spatial prior, and aggregated using TransMIL for slide-level prediction.\", \"page\": 5, \"index\": 1, \"width\": 821, \"height\": 450}, {\"url\": \"assets/figures/arxiv/2605.02660v1/fig-002.webp\", \"caption\": \"Table 2: TransMIL with spatial biological priors. Peripheral Distance (PD) encodes margin proximity. Local Immune Neighborhood (LIN) encodes neighborhood LYM/TUM ratio. READ MSS Spec is MSS specificity on TCGA-READ (49 MSS cases).\", \"page\": 7, \"index\": 2, \"width\": 867, \"height\": 212}, {\"url\": \"assets/figures/arxiv/2605.02660v1/fig-003.webp\", \"caption\": \"Table 1: Results for all foundation model and aggregator configurations. COAD MSI AUC is mean ± std across 5 folds. READ MSS Spec is MSS specificity on TCGA-READ (49 MSS cases). Kather 2019 benchmark AUC: 0.840 [4].\", \"page\": 7, \"index\": 3, \"width\": 827, \"height\": 250}]"
motivation: 现有的基础模型在预测MSI时仍会捕捉到站点特异性的纹理干扰，导致跨机构应用时泛化能力不足。
method: 在TransMIL聚合器中注入基于边缘距离和局部免疫邻域的生物学空间先验，以引导模型关注与MSI相关的保守形态特征。
result: 边缘距离编码在外部测试集TCGA-READ上实现了1.000的特异性，优于所有基准配置。
conclusion: 引入基于生物学背景的空间先验可作为有效的正则化手段，显著提升病理基础模型在跨站点任务中的鲁棒性。
---

## 摘要
从常规苏木精-伊红（H&E）全视野数字化切片（WSIs）预测微卫星不稳定（MSI）状态是分子检测的一种实用替代方案，但在单一机构训练的模型在不同站点获取的切片上泛化效果往往较差。尽管基础模型表示具有通用性，但它们在编码 MSI 底层保守生物形态的同时，仍会编码特定站点的纹理。我们研究了源自已知 MSI 组织学的切片级空间先验是否能引导这些表示向更具站点不变性的特征发展。我们引入了一种基于外周距离编码的生物启发空间先验，反映了肿瘤浸润边缘的克罗恩病样外周淋巴细胞反应，并评估了反映每个切片即时空间邻域内淋巴细胞与肿瘤比例的次要局部免疫邻域编码。这两种先验都在自注意力机制之前注入 TransMIL 聚合器，使 Transformer 能够在所有注意力层中将空间生物背景与 UNI2-h 或 Virchow2 特征整合。我们评估了六种基础模型和 MIL 聚合器组合作为参考，然后评估了每种空间先验的效果。在 TCGA-COAD（137 张切片）上训练并在不进行重新训练的情况下对 TCGA-READ（50 张切片）进行外部评估，外周距离编码在 COAD 上实现了 0.959 +/- 0.012 的 MSI AUC，在 READ 上实现了 1.000 的 MSS 特异性，而最强的参考配置分别为 0.957 和 0.939。局部免疫邻域编码实现了相当的内部 AUC，但跨站点特异性较低，这表明边缘接近度比局部免疫密度编码了更具站点不变性的生物信号。结果表明，基于生物学的空间先验可作为正则化项，减少对特定站点成像模式的依赖。

## Abstract
Predicting microsatellite instability (MSI) status from routine hematoxylin and eosin (H&E) whole slide images (WSIs) offers a practical alternative to molecular testing, but models trained at one institution tend to generalize poorly to slides acquired at a different site. Foundation model representations, despite their generality, still encode site-specific texture alongside the conserved biological morphology underlying MSI. We investigate whether tile-level spatial priors derived from known MSI histology can guide these representations toward more site-invariant features. We introduce a biologically motivated spatial prior based on peripheral distance encoding, reflecting the Crohn's-like peripheral lymphocytic reaction at the tumor invasive margin, and evaluate a secondary local immune neighborhood encoding reflecting the lymphocyte-to-tumor ratio in each tile's immediate spatial neighborhood. Both priors are injected into a TransMIL aggregator before self-attention, allowing the transformer to integrate spatial biological context with UNI2-h or Virchow2 features across all attention layers. We evaluate six foundation model and MIL aggregator combinations as a reference, then assess the effect of each spatial prior. Training on TCGA-COAD (137 slides) and evaluating externally on TCGA-READ (50 slides) without retraining, peripheral distance encoding achieves MSI AUC 0.959 +/- 0.012 on COAD and MSS specificity 1.000 on READ, compared to 0.957 and 0.939 for the strongest reference configuration. Local immune neighborhood encoding achieves comparable internal AUC but lower cross-site specificity, suggesting margin proximity encodes a more site-invariant biological signal than local immune density. Results suggest biologically grounded spatial priors act as regularizers that reduce reliance on site-specific imaging patterns.

---

## 论文详细总结（自动生成）

这是一份关于论文《Biological Spatial Priors Regularize Foundation Model Representations for Cross-Site MSI Generalization in Colorectal Cancer》的结构化深入总结：

### 0. 源代码链接
*   [https://github.com/raajuuu1998/peripheral-distance-msi](https://github.com/raajuuu1998/peripheral-distance-msi)

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在结直肠癌（CRC）中，通过 H&E 切片预测微卫星不稳定（MSI）状态面临**跨中心泛化性差**的挑战。
*   **研究动机**：尽管病理基础模型（Foundation Models）提取的特征非常强大，但仍会捕捉到特定站点的扫描仪特性、染色风格等非生物学干扰。
*   **核心思想**：利用 MSI-H 肿瘤特有的生物学空间特征（如肿瘤边缘的“克罗恩病样”淋巴细胞反应）作为**空间先验（Spatial Priors）**，引导模型关注具有生物学意义且站点无关的区域，从而起到正则化作用，提升跨站点预测的鲁棒性。

### 2. 方法论
论文提出了两种生物学驱动的空间编码方式，并将其注入 TransMIL 聚合器中：
*   **外周距离编码 (Peripheral Distance, PD)**：
    *   **原理**：基于 MSI-H 肿瘤的淋巴细胞浸润通常集中在肿瘤浸润边缘（通常靠近组织切片的边缘）。
    *   **实现**：计算每个瓦片（Tile）到切片四个边界的最小归一化距离，生成一个标量值。该值在边缘处为 1，中心处趋近于 0。
*   **局部免疫邻域编码 (Local Immune Neighborhood, LIN)**：
    *   **原理**：反映局部空间内淋巴细胞与肿瘤细胞的共存关系。
    *   **实现**：利用预训练的组织分类器获取每个瓦片的淋巴细胞（LYM）和肿瘤（TUM）概率，计算给定半径邻域内两者的平均概率比值的对数。
*   **模型集成**：将这些标量先验拼接到基础模型（如 UNI2-h 或 Virchow2）提取的特征向量后，输入 TransMIL。通过 Transformer 的自注意力机制，使模型在所有层中都能结合空间背景进行推理。

### 3. 实验设计
*   **数据集**：
    *   **训练集（内部验证）**：TCGA-COAD（结肠癌，137 张切片），采用 5 折分层交叉验证。
    *   **外部测试集（跨站点评估）**：TCGA-READ（直肠癌，50 张切片），直接测试，不进行任何微调。
*   **Benchmark 与对比方法**：
    *   **基准线**：Kather 等人 (2019) 的 ResNet 结果 (AUC 0.840)。
    *   **对比配置**：组合了 2 种基础模型（UNI2-h, Virchow2）和 3 种 MIL 聚合器（ABMIL, CLAM-SB, TransMIL），共 6 种参考配置。
*   **评估指标**：MSI AUC、超突变（Hypermutation）AUC 以及在 READ 数据集上的 MSS 特异性（Specificity）。

### 4. 资源与算力
*   **硬件**：使用了单张 **NVIDIA GPU**（具体型号未指明，但提到支持 bfloat16 混合精度训练）。
*   **训练细节**：训练 30 个 epoch，使用 Adam 优化器，学习率为 $10^{-4}$，应用了余弦退火调度和线性预热。
*   **数据处理**：在 20x 倍率下提取 256x256 的瓦片，每张切片包含 800 到 6000 个瓦片。

### 5. 实验数量与充分性
*   **实验规模**：
    *   对比了 6 种主流基础模型+聚合器的组合。
    *   针对两种空间先验分别在两种基础模型上进行了消融和对比实验。
    *   进行了注意力图（Attention Maps）的可视化定性分析。
*   **充分性评价**：实验设计较为客观，特别是采用了“完全不重训”的外部测试方式，严谨地验证了泛化能力。但受限于 TCGA 数据集本身，样本量（尤其是 MSI-H 阳性样本）相对较小。

### 6 主要结论与发现
*   **外周距离 (PD) 效果显著**：在 UNI2-h + TransMIL 架构下，加入 PD 先验使 TCGA-READ 的 MSS 特异性从 0.939 提升至 **1.000（零误报）**，同时提升了内部验证的 AUC。
*   **基础模型差异**：UNI2-h 在该任务上普遍优于 Virchow2，可能与其预训练数据的多样性有关。
*   **先验的正则化作用**：注意力图显示，加入 PD 后，模型在 MSI-H 切片上会显著减少对组织内部纹理的关注，转而聚焦于边缘区域，这符合生物学预期。
*   **LIN 的局限性**：局部免疫邻域编码在某些配置下会降低内部 AUC，表现出更强的正则化压力，且对基础模型的依赖性更高。

### 7. 优点
*   **生物学可解释性强**：不同于纯数学的坐标编码，PD 编码直接对应临床已知的“克罗恩病样反应”空间分布。
*   **轻量且通用**：无需额外的瓦片级标注或复杂的分割模型，仅通过坐标计算即可实现，且能适配各种基础模型。
*   **显著提升泛化性**：在跨站点测试中实现了极高的特异性，这对减少临床误诊具有重要意义。

### 8. 不足与局限
*   **样本量限制**：TCGA-COAD 的 MSI-H 样本仅 23 例，TCGA-READ 仅 1 例 MSI-H，导致无法有效评估外部测试集的灵敏度（Sensitivity）。
*   **先验过于粗糙**：外周距离只是肿瘤边缘的几何近似，对于形状复杂的组织块，可能无法准确代表真实的浸润边缘。
*   **站点覆盖有限**：实验仅在 TCGA 内部的不同子集间进行，尚未在完全独立的第三方医院队列（非 TCGA 来源）上验证。

（完）
