---
title: "UniSemAlign: Text-Prototype Alignment with a Foundation Encoder for Semi-Supervised Histopathology Segmentation"
title_zh: UniSemAlign：基于基础编码器的文本-原型对齐半监督组织病理学分割
authors: "Le-Van Thai, Tien Dat Nguyen, Hoai Nhan Pham, Lan Anh Dinh Thi, Duy-Dong Nguyen, Ngoc Lam Quang Bui"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09169v1"
tags: ["query:cpath"]
score: 10.0
evidence: 基于病理预训练基础编码器和文本原型对齐的组织病理学研究
tldr: 针对病理图像分割中像素级标注稀缺和伪标签不可靠的问题，本文提出UniSemAlign框架。该框架基于预训练Transformer编码器，通过在共享嵌入空间中引入原型级和文本级双模态语义对齐，为像素级学习提供结构化引导。通过融合对齐表示与视觉预测，显著提升了半监督场景下的分割性能，在GlaS和CRAG数据集上表现优异。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.09169v1/fig-001.webp\", \"caption\": \"Figure 2. Qualitative results for different semi-supervised methods under the 10% labeling setting on GlaS-2017 and CRAG-2019. Pink boxes highlight notable differences between competing methods and our approach.\", \"page\": 7, \"index\": 1, \"width\": 1036, \"height\": 415}, {\"url\": \"assets/figures/arxiv/2604.09169v1/fig-002.webp\", \"caption\": \"Figure 3. Qualitative comparison of the dual semantic alignment branches on GlaS (10% labeled). From left to right: (a) input image, (b) UniSemAlign (ours), (c) Ground truth, (d) without text branch, (e) without prototype branch. Pink boxes highlight notable differences. The prototype branch primarily improves structural completeness and boundary coherence, while the text branch suppresses ambiguous/background activations.\", \"page\": 7, \"index\": 2, \"width\": 496, \"height\": 269}, {\"url\": \"assets/figures/arxiv/2604.09169v1/fig-003.webp\", \"caption\": \"Table 1. Quantitative results on GlaS-2017 and CRAG-2019 datasets under two labeled ratios. We highlight the best performance for each metric in bold, and the second-best in underlined.\", \"page\": 6, \"index\": 3, \"width\": 956, \"height\": 581}, {\"url\": \"assets/figures/arxiv/2604.09169v1/fig-004.webp\", \"caption\": \"Figure 1. Overview of UniSemAlign. An input image is encoded by UNI ViT-B/16 and decoded by DeepLabV3+ to produce visual logits. In parallel, pixel embeddings are projected into a shared semantic space and aligned with learnable class prototypes and prompt-guided text embeddings generated by a frozen CONCH text encoder with learnable context tokens. Cosine similarity between pixel embeddings and these class representations produces alignment logits, which are fused with the decoder logits to generate pseudo-labels for semi-supervised training.\", \"page\": 3, \"index\": 4, \"width\": 1036, \"height\": 484}]"
motivation: 旨在解决计算病理学中因标注稀缺导致的伪标签监督不可靠以及类别歧义带来的挑战。
method: 提出一种双模态语义对齐框架，通过在共享空间中结合原型级和文本级对齐分支，为视觉分割提供显式的类结构引导。
result: "在GlaS和CRAG数据集上，仅使用10%标注数据时，Dice指标分别提升了2.6%和8.6%，大幅超越现有半监督基线方法。"
conclusion: 通过引入跨模态语义对齐和预训练基础模型，可以有效增强半监督病理图像分割的鲁棒性和准确性。
---

## 摘要
计算病理学中的半监督语义分割由于像素级标注稀缺和伪标签监督不可靠，仍然面临挑战。我们提出了 UniSemAlign，这是一个双模态语义对齐框架，通过将显式的类级结构注入像素级学习中来增强视觉分割。UniSemAlign 基于病理预训练的 Transformer 编码器，在共享嵌入空间中引入了互补的原型级和文本级对齐分支，提供结构化引导，从而减少类别歧义并稳定伪标签细化过程。对齐后的表示与视觉预测融合，为未标记的组织病理学图像生成更可靠的监督信号。该框架通过有监督分割、跨视图一致性和跨模态对齐目标进行端到端训练。在 GlaS 和 CRAG 数据集上的广泛实验表明，UniSemAlign 在有限监督下显著优于近期的半监督基准模型，在仅有 10% 标记数据的情况下，GlaS 上的 Dice 指标提升高达 2.6%，CRAG 上提升 8.6%，且在 20% 监督下也表现出强劲的提升。代码可在以下网址获取：https://github.com/thailevann/UniSemAlign

## Abstract
Semi-supervised semantic segmentation in computational pathology remains challenging due to scarce pixel-level annotations and unreliable pseudo-label supervision. We propose UniSemAlign, a dual-modal semantic alignment framework that enhances visual segmentation by injecting explicit class-level structure into pixel-wise learning. Built upon a pathology-pretrained Transformer encoder, UniSemAlign introduces complementary prototype-level and text-level alignment branches in a shared embedding space, providing structured guidance that reduces class ambiguity and stabilizes pseudo-label refinement. The aligned representations are fused with visual predictions to generate more reliable supervision for unlabeled histopathology images. The framework is trained end-to-end with supervised segmentation, cross-view consistency, and cross-modal alignment objectives. Extensive experiments on the GlaS and CRAG datasets demonstrate that UniSemAlign substantially outperforms recent semi-supervised baselines under limited supervision, achieving Dice improvements of up to 2.6% on GlaS and 8.6% on CRAG with only 10% labeled data, and strong improvements at 20% supervision. Code is available at: https://github.com/thailevann/UniSemAlign

---

## 论文详细总结（自动生成）

这是一份关于论文《UniSemAlign: Text-Prototype Alignment with a Foundation Encoder for Semi-Supervised Histopathology Segmentation》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
在计算病理学领域，组织图像的像素级语义分割对于临床诊断至关重要。然而，获取高质量的像素级标注极其耗时且依赖病理专家的专业知识。
*   **挑战**：现有的半监督学习（SSL）方法（如一致性正则化和伪标签技术）在处理病理图像时，常因组织形态的高度异质性、边界模糊以及缺乏显式的语义引导，导致生成的伪标签质量低下，产生“确认偏差”并降低训练稳定性。
*   **核心动机**：利用病理学领域的大规模预训练基础模型（Foundation Models）提供的丰富先验知识，通过视觉-语言对齐（Vision-Language Alignment）为像素级预测提供显式的类级结构引导，从而在极少标注的情况下提升分割精度。

### 2. 方法论：核心思想与关键技术
UniSemAlign 提出了一个双模态语义对齐框架，其核心思想是将视觉特征与“类原型”和“文本语义”在共享嵌入空间中进行对齐。

*   **核心架构**：
    *   **编码器**：采用病理学专用基础模型 **UNI (ViT-B/16)** 提取多尺度特征。
    *   **解码器**：使用 **DeepLabV3+** 生成初步的视觉 Logits。
*   **双模态对齐分支**：
    1.  **原型分支（Prototype Branch）**：维护一组可学习的类原型（Class Prototypes），通过计算像素嵌入与原型之间的余弦相似度生成对齐 Logits。
    2.  **文本分支（Text Branch）**：使用冻结的 **CONCH 文本编码器**，配合 **CoOp 风格的可学习 Prompt**（Context Tokens）。将类别名称转化为文本嵌入，并与像素特征对齐。
*   **逻辑融合与伪标签生成**：
    *   将视觉解码器输出、原型对齐输出和文本对齐输出进行加权融合，生成最终的融合 Logits。
    *   在处理未标记数据时，基于融合后的 Logits 生成更可靠的伪标签，并采用 **CorrMatch** 风格的弱到强一致性框架进行训练。
*   **训练目标**：
    *   包含有监督分割损失（CE）、原型/文本分支的辅助监督损失、跨模态对齐损失（MSE，用于约束原型与文本嵌入的一致性）以及无监督的一致性损失（Hard Loss, Soft Consistency, Correlation Loss）。

### 3. 实验设计
*   **数据集**：
    *   **GlaS**：结肠腺体分割数据集（165张图像）。
    *   **CRAG**：高分辨率结肠组织病理学数据集（213张图像）。
*   **Benchmark 与对比方法**：
    *   设置了 **10%** 和 **20%** 两种标注比例。
    *   对比了 8 种主流及最新的半监督方法：UAMT, FixMatch, CPS, CT, XNet, CorrMatch, 以及针对医疗/病理的最新方法 DuSSS 和 CSDS。
*   **评价指标**：采用 mDice 和 mJaccard 指标。

### 4. 资源与算力
*   **硬件**：实验在单张 **NVIDIA TITAN Xp GPU (12GB 显存)** 上运行。
*   **训练细节**：
    *   训练时长为 80 个 Epoch。
    *   使用 SGD 优化器，初始学习率为 0.001。
    *   输入图像随机裁剪为 256×256 像素。

### 5. 实验数量与充分性
*   **实验规模**：论文在两个主流病理数据集上进行了完整的对比实验，涵盖了从早期到 2025 年最新的 SOTA 方法。
*   **消融实验**：作者进行了非常详尽的消融研究，包括：
    1.  编码器架构对比（ResNet101 vs. UNI）。
    2.  可学习 Context Token 数量的影响。
    3.  原型分支与文本分支各自的贡献。
    4.  不同对齐损失函数（Cosine, KL, MSE）的效果对比。
*   **客观性**：实验设计遵循了半监督学习的标准协议，对比方法涵盖了多种技术路线，结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **性能飞跃**：UniSemAlign 在所有设置下均显著优于基准方法。在 10% 标注下，CRAG 数据集的 mDice 提升了 **8.64%**，GlaS 提升了 **2.61%**。
*   **基础模型的力量**：使用病理预训练的 UNI 编码器比传统的 ResNet101 带来了显著的性能增益（mDice 提升约 1.77%）。
*   **双分支互补性**：实验证明原型分支有助于提高结构的完整性和边界一致性，而文本分支则能有效抑制背景中的歧义激活。
*   **语义引导的有效性**：通过引入显式的类级语义约束，模型能够生成更清晰、更连贯的分割结果，尤其是在组织重叠和形态复杂的区域。

### 7. 优点（亮点）
*   **双模态融合**：巧妙地结合了“数据驱动”的类原型和“知识驱动”的文本语义，提供了双重约束。
*   **利用基础模型**：成功将 UNI 和 CONCH 等病理学基础模型引入半监督分割任务，证明了领域特定预训练模型的巨大潜力。
*   **即插即用**：该对齐框架可以作为现有半监督框架（如 CorrMatch）的增强模块，具有较好的扩展性。
*   **定性效果好**：可视化结果显示，该方法在处理细长腺体结构和抑制孤立噪声方面表现优异。

### 8. 不足与局限
*   **计算开销**：虽然使用了冻结的文本编码器，但引入双分支对齐和基础模型编码器会增加推理和训练时的内存占用（尽管单卡可跑）。
*   **任务覆盖面**：实验主要集中在腺体分割（GlaS/CRAG），尚未在更多样化的病理任务（如多器官癌症分割、细胞核分割）上验证泛化性。
*   **Prompt 依赖性**：虽然使用了可学习的 Context Tokens，但模型对初始类别名称的定义可能仍具有一定的敏感性。
*   **超参数调节**：融合权重（$\eta_p, \eta_t$）和损失权重需要手动调优，不同数据集可能需要不同的配置。

（完）
