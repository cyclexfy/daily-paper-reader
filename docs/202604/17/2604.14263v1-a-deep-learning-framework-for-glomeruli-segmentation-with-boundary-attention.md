---
title: A deep learning framework for glomeruli segmentation with boundary attention
title_zh: 一种带有边界注意力的肾小球分割深度学习框架
authors: "Behnaz Elhaminia, Catherine King, Jiaqi Lv, Lorraine Harper, Paul Moss, Owen Cain, Dimitrios Chanouzas, Shan E Ahmed Raza"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.14263v1"
tags: ["query:cpath"]
score: 9.0
evidence: 利用病理基础模型进行分割
tldr: 针对肾脏组织中相邻肾小球难以精确分割的问题，本研究提出了一种基于边界注意力的深度学习框架。该模型在U-Net架构基础上，结合病理基础模型并引入专门的注意力解码器，旨在强化边界分离和实例级分割。实验证明，该方法在Dice和IoU指标上均优于现有最先进技术，显著提升了肾小球的勾画精度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.14263v1/fig-001.webp\", \"caption\": \"Fig. 3. Comparison of glomerulus segmentation results, with PAS images in the top row and HE images in the bottom row. (A) Ground truth, (B) Proposed model, (C) Eff-UNet, (D) TOM, and (E) WGO. The patches are taken from the test set of the REACTIVAS dataset and illustrate challenging cases with closely spaced glomeruli.\", \"page\": 4, \"index\": 1, \"width\": 1046, \"height\": 333}, {\"url\": \"assets/figures/arxiv/2604.14263v1/fig-002.webp\", \"caption\": \"Fig. 1. An illustration of the architecture and key processing stages. Deep features extracted by the Virchow2 foundation model are concatenated with the input image and fed into the encoder. The attention module exploits these features to extract critical boundary-related information, which helps the model segment boundaries with greater precision and focus.\", \"page\": 2, \"index\": 2, \"width\": 898, \"height\": 504}]"
motivation: 传统语义分割方法在处理相邻肾小球时难以精确描绘边界，导致实例分割效果不佳。
method: 提出一种结合病理基础模型与边界注意力解码器的改进型U-Net架构，通过强化边界特征来优化分割。
result: 该方法在Dice评分和交并比（IoU）指标上均超越了当前最先进的肾小球分割模型。
conclusion: 引入边界注意力机制能显著提升肾小球的勾画精度，为肾脏病理诊断提供了更有效的自动化工具。
---

## 摘要
肾脏组织中肾小球的准确检测与分割对于诊断应用至关重要。传统的深度学习方法主要依赖语义分割，这往往无法精确描绘相邻的肾小球。为了应对这一挑战，我们提出了一种强调边界分离的新型肾小球检测与分割模型。利用病理基础模型，所提出的基于 U-Net 的架构结合了一个专门的注意力解码器，旨在突出关键区域并改进实例级分割。实验评估表明，我们的方法在 Dice 分数和交并比（Intersection over Union）方面均优于现有最先进的方法，显示出在肾小球描绘方面的卓越性能。

## Abstract
Accurate detection and segmentation of glomeruli in kidney tissue are essential for diagnostic applications. Traditional deep learning methods primarily rely on semantic segmentation, which often fails to precisely delineate adjacent glomeruli. To address this challenge, we propose a novel glomerulus detection and segmentation model that emphasises boundary separation. Leveraging pathology foundation models, the proposed U-Net-based architecture incorporates a specialised attention decoder designed to highlight critical regions and improve instancelevel segmentation. Experimental evaluations demonstrate that our approach surpasses state-of-the-art methods in both Dice score and Intersection over Union, indicating superior performance in glomerular delineation.

---

## 论文详细总结（自动生成）

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
肾小球是肾脏的基本功能单位，其精确分割对于肾脏疾病的诊断、治疗方案制定及预后评估至关重要。然而，现有的深度学习方法在处理**相邻/重叠肾小球**时，往往难以精确勾画边界，导致无法有效进行实例计数。此外，不同染色协议（如HE和PAS染色）带来的形态差异也增加了分割难度。本研究旨在开发一种能够强化边界分离、在多种染色条件下均具有鲁棒性的肾小球分割框架。

### 2. 核心方法论
论文提出了一种基于 U-Net 架构的改进模型，核心思想是结合**病理基础模型**与**边界注意力机制**：
*   **特征提取（Backbone）：** 采用预训练的病理基础模型 **Virchow2** 作为冻结的特征提取器，并将其提取的深层特征与输入图像拼接，输入到基于 **EfficientNetV2** 的编码器中。
*   **级联注意力模块（CABs）：** 在解码阶段引入 CABs。该模块接收编码器不同层级的特征，通过学习参数自适应地关注具有挑战性的边界区域，而非简单的背景/前景转换。它通过卷积、上采样和元素级乘法，生成突出边界信息的注意力图。
*   **自适应边界加权损失函数：** 提出一种动态权重图 $W(x)$。与传统的静态距离变换不同，该函数通过指数衰减函数，对距离极近的相邻肾小球边界分配更高的权重，强制模型学习难以区分的缝隙。
*   **总损失函数：** 结合了加权二元交叉熵损失（WBCE）、Dice 损失以及针对注意力模块的监督损失（Attention Loss）。

### 3. 实验设计
*   **数据集：**
    1.  **HuBMAP（公开）：** 20个PAS染色的全扫描切片（WSI）。
    2.  **REACTIVAS（私有）：** 42个肾活检WSI，包含20个HE和22个PAS染色切片，由专家手动标注。
*   **对比基准（Benchmarks）：**
    *   Kaggle HuBMAP 竞赛前三名模型：**TOM**（SeResNeXt101）、**Gleb**（集成模型）、**WGO**（集成模型+FPN）。
    *   **Eff-UNet：** 移除注意力模块的基准模型，用于验证 CABs 的有效性。
*   **评估指标：** Dice 分数和交并比（IoU）。

### 4. 资源与算力
论文中**未明确说明**具体的硬件配置（如 GPU 型号、数量）以及训练时长。文中仅提到在训练过程中使用了强数据增强技术（如随机 RGB 偏移、高斯模糊、HSV 偏移等）以及五折交叉验证。

### 5. 实验数量与充分性
*   **实验规模：** 采用了五折交叉验证，并在两个独立的数据集（HuBMAP 和 REACTIVAS）上进行了测试。
*   **充分性评价：** 实验对比了当前最先进的竞赛级模型，涵盖了 HE 和 PAS 两种主流染色，具有较好的代表性。
*   **客观性：** 通过在未见过的测试集上进行定量评估（Dice/IoU），结果显示出一致的优越性。但作者也承认，由于测试集规模相对较小，其泛化能力的全面验证仍有提升空间。

### 6. 主要结论与发现
*   提出的模型在两个数据集上均取得了最优性能。在 HuBMAP 数据集上 Dice 达到 0.9630，在更复杂的 REACTIVAS 数据集（包含更多相邻肾小球）上，其表现显著优于其他 SOTA 模型。
*   **边界注意力的重要性：** 实验证明，通过给相邻边界分配更高权重并引入专门的注意力解码器，可以有效防止相邻结构的融合，提高实例分割的精度。
*   **基础模型的增益：** 利用 Virchow2 基础模型提取的特征增强了模型对复杂病理形态的理解能力。

### 7. 优点
*   **边界处理精准：** 创新的自适应加权损失函数解决了医学图像分割中常见的“粘连”问题。
*   **多染色兼容：** 模型在 HE 和 PAS 染色下均表现稳健，具有较强的临床实用潜力。
*   **架构先进：** 有效结合了最先进的病理基础模型（Foundation Model）与定制化的注意力机制。

### 8. 不足与局限
*   **算力细节缺失：** 缺乏训练成本和推理速度的具体数据。
*   **样本量限制：** 测试集规模较小，可能存在一定的过拟合风险或评估偏差。
*   **消融实验不完整：** 论文提到计划在未来进行更深入的消融研究，以明确 CABs、自适应权重和 Virchow2 特征各自的具体贡献比例。

（完）
