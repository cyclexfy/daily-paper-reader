---
title: Region-Graph Optimal Transport Routing for Mixture-of-Experts Whole-Slide Image Classification
title_zh: 用于专家混合全扫描切片图像分类的区域图最优传输路由
authors: "Xin Tian, Jiuliu Lu, Ephraim Tsalik, Bart Wanders, Colleen Knoth, Julian Knight"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07298v1"
tags: ["query:cpath"]
score: 10.0
evidence: 使用多实例学习和混合专家模型进行全扫描切片图像分类
tldr: 针对全切片图像（WSI）分类中多实例学习聚合器缺乏专业化以及混合专家模型（MoE）负载不均的问题，本文提出ROAM框架。该框架通过将空间区域令牌路由至专家池化器，并利用带容量约束的熵正则化最优传输确保专家利用率平衡，同时引入图正则化机制促进相邻区域的一致性路由。在多个基准测试中，ROAM显著提升了病理图像分类的性能与泛化能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.07298v1/fig-001.webp\", \"caption\": \"Fig. 1. Frozen patch embeddings are pooled into M spatial region tokens. A routing GNN on the region graph parameterises region-to-expert costs, and Sinkhorn optimal transport routes region mass to E experts under per-slide capacity constraints (with optional graph smoothing). Each expert performs gated-attention pooling over its routed regions, and expert outputs are fused for slide-level prediction.\", \"page\": 3, \"index\": 1, \"width\": 702, \"height\": 206}, {\"url\": \"assets/figures/arxiv/2604.07298v1/fig-002.webp\", \"caption\": \"Table 2. Ablation study on NSCLC (LUAD vs. LUSC) using UNI2-h embeddings (mean±std over 5 folds). Internal evaluation is on TCGA (int.); external evaluation is on CPTAC (ext.).\", \"page\": 7, \"index\": 2, \"width\": 714, \"height\": 212}, {\"url\": \"assets/figures/arxiv/2604.07298v1/fig-003.webp\", \"caption\": \"Table 1. WSI benchmark results with UNI2 patch encoders (mean±std over 5-folds). For NSCLC (LUAD/LUSC), we report internal AUC on TCGA (int.) and external AUC on CPTAC (ext.); for BRCA and CRC we report internal AUC; for PANDA we report internal QWK. Best per column in bold; second best underlined.\", \"page\": 7, \"index\": 3, \"width\": 725, \"height\": 264}]"
motivation: 现有的MIL方法难以应对病理异质性，而传统MoE方法在处理大规模WSI时常因路由不均导致专家利用率低下或模型坍塌。
method: 提出一种空间感知的MoE-MIL聚合器，通过Sinkhorn算法实现带容量约束的最优传输路由，并利用图正则化扩散路由分配以保持空间连贯性。
result: 在四个WSI基准测试中表现优异，特别是在NSCLC跨数据集泛化任务中达到了0.845的AUC，证明了其强大的鲁棒性。
conclusion: ROAM通过结合最优传输与空间图结构，有效实现了平衡且具有空间一致性的专家路由，为复杂病理图像分类提供了新思路。
---

## 摘要
多实例学习（MIL）是计算病理学中十亿像素全扫描切片图像（WSI）分类的主流框架。然而，目前的 MIL 聚合器通过共享路径路由所有实例，限制了它们针对每张切片固有病理异质性进行专门化的能力。专家混合（MoE）方法通过将实例划分到专门的专家子网络中提供了一种自然的补救措施；然而，无约束的 softmax 路由可能会导致高度不平衡的利用率，即一个或少数几个专家吸收了大部分路由权重，使混合模型坍缩回接近单一路径的方案。为了解决这些局限性，我们提出了 ROAM（区域图最优传输专家混合），这是一种空间感知的 MoE-MIL 聚合器，它通过容量受限的熵最优传输将区域令牌路由到专家池化器，从而在结构设计上促进均衡的专家利用。ROAM 运行在空间区域令牌上，这些令牌是通过将密集的补丁包压缩为空间分箱单元而获得的，使路由与局部组织邻域对齐，并引入了两个关键机制：（i）将区域到专家的分配表述为具有显式单张切片容量边际的熵最优传输（Sinkhorn），在无需辅助负载均衡损失的情况下强制实现均衡的专家利用；（ii）图正则化 Sinkhorn 迭代，在空间区域图上扩散路由分配，鼓励相邻区域一致地路由到相同的专家。在四个使用冻结基础模型补丁嵌入的 WSI 基准测试中，ROAM 取得了与强大的 MIL 和 MoE 基准线相当的性能，并在 NSCLC 泛化（TCGA-CPTAC）上达到了 0.845 +- 0.019 的外部 AUC。

## Abstract
Multiple Instance Learning (MIL) is the dominant framework for gigapixel whole-slide image (WSI) classification in computational pathology. However, current MIL aggregators route all instances through a shared pathway, constraining their capacity to specialise across the pathological heterogeneity inherent in each slide. Mixture-of-Experts (MoE) methods offer a natural remedy by partitioning instances across specialised expert subnetworks; yet unconstrained softmax routing may yield highly imbalanced utilisation, where one or a few experts absorb most routing mass, collapsing the mixture back to a near-single-pathway solution. To address these limitations, we propose ROAM (Region-graph OptimAl-transport Mixture-of-experts), a spatially aware MoE-MIL aggregator that routes region tokens to expert poolers via capacity-constrained entropic optimal transport, promoting balanced expert utilisation by construction. ROAM operates on spatial region tokens, obtained by compressing dense patch bags into spatially binned units that align routing with local tissue neighbourhoods and introduces two key mechanisms: (i) region-to-expert assignment formulated as entropic optimal transport (Sinkhorn) with explicit per slide capacity marginals, enforcing balanced expert utilisation without auxiliary load-balancing losses; and (ii) graph-regularised Sinkhorn iterations that diffuse routing assignments over the spatial region graph, encouraging neighbouring regions to coherently route to the same experts. Evaluated on four WSI benchmarks with frozen foundation-model patch embeddings, ROAM achieves performance competitive against strong MIL and MoE baselines, and on NSCLC generalisation (TCGA-CPTAC) reaches external AUC 0.845 +- 0.019.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **ROAM**（Region-graph OptimAl-transport Mixture-of-experts）的新型框架，旨在解决全扫描切片图像（WSI）分类中多实例学习（MIL）聚合器的局限性。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在计算病理学中，现有的 MIL 聚合器通常让所有图像补丁（patches）通过单一的共享路径进行处理，这限制了模型捕捉组织异质性的能力。
*   **研究动机**：
    *   **专家混合模型（MoE）的缺陷**：虽然 MoE 可以通过专门的子网络处理不同特征，但传统的 Softmax 路由容易导致“路由坍缩”（即少数专家承担了绝大部分任务），且需要复杂的辅助损失函数来平衡负载。
    *   **缺乏空间上下文**：现有的路由机制通常独立处理每个补丁，忽略了病理组织在空间上的连续性和相干性，导致路由结果碎片化。

### 2. 核心方法论
ROAM 提出了一种空间感知的 MoE-MIL 聚合器，其核心流程如下：
*   **区域令牌化（Region Tokenization）**：为了降低计算复杂度并捕捉局部上下文，ROAM 将成千上万个补丁通过网格分箱（Grid Binning）压缩为 $M$ 个“区域令牌”。
*   **容量受限的最优传输路由**：
    *   将区域到专家的分配建模为**熵正则化最优传输问题**。
    *   利用 **Sinkhorn 算法**求解，通过显式设置单张切片的容量边际（Marginals），在结构上强制实现专家负载均衡，无需额外的负载均衡损失。
*   **图正则化 Sinkhorn 迭代**：
    *   构建区域质心的 kNN 空间图。
    *   在 Sinkhorn 迭代过程中交织一个轻量级的**图扩散步骤**，平滑路由分配，鼓励空间相邻的区域被路由到相同的专家，从而保证病理结构的完整性。
*   **专家聚合与融合**：每个专家对分配到的区域进行门控注意力池化（Gated-Attention Pooling），最后通过门控机制融合各专家输出进行最终预测。

### 3. 实验设计
*   **数据集**：
    1.  **NSCLC（肺癌）**：使用 TCGA 数据集训练，并在 CPTAC 数据集上进行外部验证（LUAD vs. LUSC）。
    2.  **BRCA（乳腺癌）**：子型分类（IDC vs. non-IDC）。
    3.  **CRC（结直肠癌）**：子型分类（COAD vs. READ）。
    4.  **PANDA（前列腺癌）**：六级 ISUP 分级（大规模多分类任务）。
*   **基准方法（Benchmarks）**：对比了包括 ABMIL、CLAM、DSMIL、TransMIL、ILRA 等经典 MIL 方法，以及 MAMMOTH、PAMoE、OTSurv 等最新的 MoE 或基于最优传输的方法。
*   **特征提取**：所有方法均统一使用冻结的病理基础模型 **UNI2-h** 生成的补丁嵌入。

### 4. 资源与算力
*   **硬件**：所有实验均在单张 **NVIDIA A100 (40 GB)** GPU 上运行。
*   **训练细节**：使用 AdamW 优化器，余弦退火学习率，最高训练 200 轮（带早停机制）。
*   **效率**：通过区域令牌化将路由复杂度从 $O(NE)$ 降低到 $O(ME)$（其中 $M \ll N$），显著提升了处理超大 WSI 的效率。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了四个主流 WSI 基准任务，包含二分类、多分类及跨中心外部验证。
*   **充分性**：
    *   采用了**患者分层的 5 折交叉验证**，并报告了标准差，体现了结果的稳定性。
    *   **消融实验**：设计了 5 组对比实验，分别验证了 GNN 路由、图正则化、容量约束（对比 Softmax）以及 OT 引导池化的有效性。
*   **公平性**：所有对比方法均在相同的特征空间和实验协议下运行，确保了评估的客观性。

### 6. 主要结论与发现
*   **性能领先**：ROAM 在多个任务中表现优异，特别是在 NSCLC 的外部泛化测试中，AUC 达到 **0.845**，优于所有对比的 MoE 基准线。
*   **泛化能力强**：相比于无约束的 MoE 方法（如 PAMoE），ROAM 在跨中心数据集上的方差更小，证明了容量约束路由能有效防止过拟合。
*   **空间一致性**：定性可视化显示，ROAM 生成的专家路由图具有明显的空间连贯性，符合病理学上的组织形态分布。

### 7. 优点与亮点
*   **结构化均衡**：通过最优传输理论直接解决 MoE 的负载均衡问题，比传统的辅助损失函数更优雅且鲁棒。
*   **空间感知**：首次在 MoE 路由中引入图正则化 Sinkhorn 迭代，解决了病理图像中“空间相干性”被忽视的问题。
*   **高效性**：区域令牌化策略在保留局部信息的同时，极大缓解了 MoE 在处理超长序列时的计算压力。

### 8. 不足与局限
*   **令牌化策略单一**：目前采用固定的网格分箱进行区域划分，可能无法完美契合不规则的组织边界。
*   **编码器冻结**：实验基于冻结的基础模型特征，尚未探索端到端微调（End-to-End Fine-tuning）可能带来的进一步提升。
*   **超参数敏感性**：虽然减少了负载均衡损失的调参，但引入了图平滑强度 $\lambda_s$ 和区域数量 $M$ 等新参数，需针对不同任务进行微调。

（完）
