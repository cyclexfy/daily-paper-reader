---
title: "DSA-CycleGAN: A Domain Shift Aware CycleGAN for Robust Multi-Stain Glomeruli Segmentation"
title_zh: DSA-CycleGAN：一种用于鲁棒多染色肾小球分割的领域偏移感知 CycleGAN
authors: "Zeeshan Nisar, Friedrich Feuerhake, Thomas Lampert"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18368v1"
tags: ["query:cpath"]
score: 8.0
evidence: 使用CycleGAN进行数字组织病理学分割和染色迁移
tldr: 数字病理图像中不同染色间的差异会降低分割模型性能，且标注成本高。本文提出DSA-CycleGAN，通过感知领域偏移来减少传统CycleGAN在染色转换过程中因一对多映射产生的噪声。在多染色肾小球分割实验中，该方法显著提升了分割精度，并在处理生物学差异较大的染色时表现出更强的鲁棒性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.18368v1/fig-001.webp\", \"caption\": \"Fig. 2. IHC target patches translated to PAS (T→PAS). While all translations are visually realistic, differences are observed in the segmentation maps given by a pretrained PAS segmentation model (UNetPAS), reflecting the numerical results, DSA-CycleGAN offers the most robust segmentation maps.\", \"page\": 3, \"index\": 1, \"width\": 492, \"height\": 355}]"
motivation: 针对数字病理图像中多染色标注昂贵以及传统CycleGAN在染色转换时易产生噪声的问题。
method: 提出了一种领域偏移感知（DSA）的CycleGAN，旨在减少转换过程中的噪声并优化循环一致性。
result: 实验表明DSA-CycleGAN在肾小球分割任务中优于现有方法，能有效抑制噪声并提升跨染色分割性能。
conclusion: DSA-CycleGAN为多染色组织病理图像分割提供了一种更鲁棒的无监督域迁移解决方案。
---

## 摘要
数字组织病理学分割中的一个关键挑战是染色间和染色内的变异，因为这会降低模型性能。由于对每种染色进行标注既昂贵又耗时，因此开发了通过 CycleGAN 进行染色迁移的方法，以便利用单一染色的标签来训练多染色分割模型。然而，由于某些染色对具有一对多的特性，这与其循环一致性损失相冲突，CycleGAN 在转换过程中往往会引入噪声。为了解决这一问题，我们提出了领域偏移感知 CycleGAN（DSA-CycleGAN），旨在减少此类噪声的出现。此外，我们评估了机器学习领域旨在解决类似问题的几项进展，并在多染色肾小球分割的背景下将其有效性与 DSA-CycleGAN 进行了比较。实验表明，DSA-CycleGAN 不仅提高了肾小球分割的性能，而且在减少噪声方面也优于其他方法。这在生物学差异显著的染色之间进行转换时尤为明显。代码已在 https://github.com/zeeshannisar/DSA-CycleGAN 公开。

## Abstract
A key challenge in segmentation in digital histopathology is inter- and intra-stain variations as it reduces model performance. Labelling each stain is expensive and time-consuming so methods using stain transfer via CycleGAN, have been developed for training multi-stain segmentation models using labels from a single stain. Nevertheless, CycleGAN tends to introduce noise during translation because of the one-to-many nature of some stain pairs, which conflicts with its cycle consistency loss. To address this, we propose the Domain Shift Aware CycleGAN, which reduces the presence of such noise. Furthermore, we evaluate several advances from the field of machine learning aimed at resolving similar problems and compare their effectiveness against DSA-CycleGAN in the context of multi-stain glomeruli segmentation. Experiments demonstrate that DSA-CycleGAN not only improves segmentation performance in glomeruli segmentation but also outperforms other methods in reducing noise. This is particularly evident when translating between biologically distinct stains. The code is publicly available at https://github.com/zeeshannisar/DSA-CycleGAN.

---

## 论文详细总结（自动生成）

这是一份关于论文《DSA-CycleGAN: A Domain Shift Aware CycleGAN for Robust Multi-Stain Glomeruli Segmentation》的深度结构化总结：

### 0. 源代码链接
*   [https://github.com/zeeshannisar/DSA-CycleGAN](https://github.com/zeeshannisar/DSA-CycleGAN)

### 1. 核心问题与整体含义（研究动机和背景）
*   **背景**：在数字病理学中，不同染色（如PAS、H&E、IHC等）会导致组织外观差异巨大。为每种染色手动标注肾小球等结构极其耗时且昂贵。
*   **核心问题**：现有的无监督域迁移方法（如 CycleGAN）常被用于将目标染色转换为已标注的源染色。然而，由于染色对之间存在“一对多”的映射关系（例如，信息丰富的PAS染色包含的信息多于信息贫乏的IHC染色），CycleGAN 为了满足循环一致性损失，往往会在生成的图像中嵌入肉眼不可见的“隐写噪声”。这种噪声会导致严重的**领域偏移（Domain Shift）**，从而降低下游分割模型的准确性。
*   **研究动机**：开发一种能够感知并抑制这种转换噪声的 CycleGAN 变体，以实现更鲁棒的跨染色肾小球分割。

### 2. 方法论
*   **核心思想**：引入**领域偏移度量（Domain Shift Metric, DSM）**作为额外的损失函数，引导生成器产生在特征分布上更接近真实源域的图像，从而抑制噪声。
*   **关键技术细节**：
    *   **DSM 度量**：利用预训练的分割网络提取特征，计算真实源域图像与转换后图像在特征激活层上的均值分布差异，通过 Wasserstein 距离进行量化。
    *   **DSA-CycleGAN 损失函数**：在原始 CycleGAN 的对抗损失、循环一致性损失和恒等映射损失基础上，增加了 **$L_{dsl}$ 损失**。
    *   **多位置约束**：$L_{dsl}$ 不仅应用于目标到源的转换（$T \to S$），还应用于源域的循环重建过程和恒等映射过程，全方位约束特征分布。
*   **算法流程**：通过最小化 DSM 损失，强制生成器在转换过程中保留生物学特征，而非通过嵌入高频噪声来“欺骗”循环一致性约束。

### 3. 实验设计
*   **数据集**：来自汉诺威医学院的 10 名患者的肾组织样本，包含 5 种染色：PAS（源域，有标注）、Jones H&E、Sirius Red、CD68、CD34（目标域）。
*   **Benchmark 任务**：多染色肾小球分割（MDS1 框架）。即：使用在 PAS 上训练的 UNet，对转换后的 $T \to PAS$ 图像进行分割。
*   **对比方法**：
    *   原始 CycleGAN（Baseline）。
    *   CycleGAN w/ Extra-channels（通过增加通道分离噪声）。
    *   CycleGAN w/ Gaussian-noise（通过添加高斯噪声破坏隐写信息）。
    *   CycleGAN w/ Self-supervision（利用分割掩码一致性进行自监督）。
*   **泛化性验证**：在公开的 KidneyArtPathology 数据集上进行了额外测试。

### 4. 资源与算力
*   **算力支持**：文中提到使用了 Nvidia GPU，并获得了斯特拉斯堡大学计算中心和 GENCI-IDRIS 的资助。
*   **具体细节**：未明确说明具体的 GPU 型号（如 A100 或 V100）、数量及具体的训练时长。

### 5. 实验数量与充分性
*   **实验规模**：
    *   每种 CycleGAN 变体针对每个目标染色训练 3 次。
    *   每个 CycleGAN 对应训练 5 个 UNet 模型。
    *   每个目标染色共进行 15 次实验重复，以确保统计显著性。
*   **消融实验**：对 $L_{dsl}$ 的不同组成部分（仅 fake、仅 cyc+id、全部集成）进行了消融研究。
*   **充分性评价**：实验设计较为严谨，包含了统计学显著性检验（p < 0.05），并跨越了组织化学（HC）和免疫组化（IHC）两类差异巨大的染色，实验结果具有说服力。

### 6. 主要结论与发现
*   **性能提升**：DSA-CycleGAN 在所有染色任务中均表现优异，尤其是在生物学差异较大的 IHC 染色（CD68, CD34）转换中，F1 分数显著高于 Baseline。
*   **噪声抑制**：相比于其他机器学习领域的去噪策略，DSA-CycleGAN 在减少领域偏移方面最有效。
*   **适用场景**：
    *   对于生物学相似的染色（HC ↔ HC），添加高斯噪声的方法效果较好。
    *   对于生物学差异大的染色（HC ↔ IHC），DSA-CycleGAN 和自监督方法更具优势。
*   **泛化能力**：DSA-CycleGAN 在未见过的外部数据集上表现出最强的鲁棒性。

### 7. 优点
*   **针对性强**：直接解决了 CycleGAN 在病理图像迁移中存在的“隐写噪声”这一痛点。
*   **无需额外标注**：DSM 损失利用预训练模型，在 CycleGAN 训练阶段不需要目标域的地面真值（Ground Truth）。
*   **灵活性高**：该损失项可以轻松集成到其他生成模型（如 StarGAN 或编解码模型）中。

### 8. 不足与局限
*   **依赖预训练模型**：DSM 的效果取决于预训练源域分割模型的质量，如果预训练模型本身特征提取能力不足，可能会影响约束效果。
*   **计算开销**：虽然文中未详述，但在训练过程中引入额外的特征提取和 Wasserstein 距离计算会增加一定的计算负担。
*   **应用范围**：目前仅在肾小球分割上验证，对于更复杂的组织结构或其他器官的适用性有待进一步验证。

（完）
