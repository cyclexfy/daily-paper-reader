---
title: Distilling Vision Transformers for Distortion-Robust Representation Learning
title_zh: 蒸馏视觉 Transformer 以实现抗失真表征学习
authors: "Konstantinos Alexis, Giorgos Giannopoulos, Dimitrios Gunopulos"
date: 2026-04-24
pdf: "https://arxiv.org/pdf/2604.22529v1"
tags: ["query:cpath"]
score: 6.0
evidence: 从预训练模型中蒸馏视觉Transformer以获得鲁棒表示
tldr: 针对在缺乏清晰数据时自监督学习表现不佳的问题，本文提出一种非对称知识蒸馏框架，利用预训练ViT模型学习抗畸变表征。通过让教师模型处理清晰图像、学生模型处理畸变图像，并进行多层级特征对齐，使学生模型在不直接接触清晰数据的情况下获得鲁棒性，在多项分类任务中表现优于现有方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.22529v1/fig-001.webp\", \"caption\": \"Fig. 1. Overview of our multi-target distillation framework. A frozen teacher ViT processes clean images while a student ViT of identical architecture receives their distorted counterparts, being trained to align with the teacher at three complementary levels: global embeddings, patch-level features, and attention maps. In this way, the student learns to recover clean semantic representations directly from corrupted inputs, producing robust encoders that are effective for downstream tasks on distorted images.\", \"page\": 6, \"index\": 1, \"width\": 725, \"height\": 381}, {\"url\": \"assets/figures/arxiv/2604.22529v1/fig-002.webp\", \"caption\": \"Fig. 4. Attention maps of our distilled student models and the supervised baselines on ImageNet-100 validation images, under different distortion types. For each example, we show the original image, the distorted input, and the attention maps predicted by the supervised baseline and the encoder trained by our method.\", \"page\": 13, \"index\": 2, \"width\": 710, \"height\": 258}]"
motivation: 旨在解决自监督学习在缺乏清晰观测数据或数据存在畸变时难以学习到鲁棒视觉表征的问题。
method: 采用非对称知识蒸馏框架，通过对齐教师与学生模型在全局、补丁及注意力层级的特征，使学生模型学习清晰图像的表征。
result: 在多个数据集和多种畸变类型的图像分类任务中，该方法在相同监督强度下一致优于现有的替代方案。
conclusion: 证明了利用预训练模型进行多层级蒸馏能有效提升模型对畸变数据的表征能力，为处理低质量图像提供了新思路。
---

## 摘要
自监督学习在从干净数据中学习视觉表征方面取得了显著成功，但在干净观测数据稀缺或完全不可用时仍然面临挑战。在本文中，我们证明了可以利用预训练的视觉模型来学习抗失真的表征，这些表征随后可以有效地应用于处理失真观测数据的下游任务。具体而言，我们提出了一种非对称知识蒸馏框架，其中教师和学生模型均初始化自同一个预训练的视觉 Transformer（Vision Transformer），但接收每张图像的不同视图：教师处理干净图像，而学生则观察其失真版本。我们引入了多级蒸馏，对齐了全局嵌入、补丁级特征和注意力图，并证明了尽管学生模型从未直接访问干净数据，但仍能逼近干净图像的表征。我们在多个数据集和各种失真情况下的图像分类任务上评估了我们的方法，在相同的人力监督量下，其表现始终优于现有的替代方案。

## Abstract
Self-supervised learning has achieved remarkable success in learning visual representations from clean data, yet remains challenging when clean observations are sparse or not available at all. In this paper, we demonstrate that pretrained vision models can be leveraged to learn distortion-robust representations, which can then be effectively applied to downstream tasks operating on distorted observations. In particular, we propose an asymmetric knowledge distillation framework in which both teacher and student are initialized from the same pretrained Vision Transformer but receive different views of each image: the teacher processes clean images, while the student sees their distorted versions. We introduce multi-level distillation that aligns global embeddings, patch-level features, and attention maps and show that the student is able to approximate clean-image representations despite never directly accessing clean data. We evaluate our approach on image classification tasks across several datasets and under various distortions, consistently outperforming existing alternatives for the same amount of human supervision.