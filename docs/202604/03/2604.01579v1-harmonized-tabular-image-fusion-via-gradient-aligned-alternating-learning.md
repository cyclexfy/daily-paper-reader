---
title: Harmonized Tabular-Image Fusion via Gradient-Aligned Alternating Learning
title_zh: 通过梯度对齐交替学习实现协调的表格-图像融合
authors: "Longfei Huang, Yang Yang"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01579v1"
tags: ["query:cpath"]
score: 8.0
evidence: 通过梯度对齐进行多模态表格-图像融合
tldr: 针对多模态表格-图像融合中存在的模态间梯度冲突问题，本文提出了一种梯度对齐交替学习（GAAL）范式。该方法通过交替的单模态学习和共享分类器来解耦多模态梯度，并引入基于不确定性的跨模态梯度手术，选择性地对齐梯度以优化共享参数。实验证明，GAAL在多个数据集上优于现有SOTA方法，并在表格数据缺失的情况下表现出更强的鲁棒性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.01579v1/fig-001.webp\", \"caption\": \"Fig. 2. The framework of the GAAL method. (a) Gradient-Aligned Alternating Learning that only one modality’s learner is updated at each step. Uncertaintybased cross-modal gradient surgery utilizes gradients from cross-modal hard samples to guide the optimization of the shared classifier for the current modality. (b) Uncertainty-based Gradient Guidance that samples hard examples from another modality to provide cross-modal gradient guidance.\", \"page\": 3, \"index\": 1, \"width\": 748, \"height\": 416}]"
motivation: 现有的多模态融合方法常受到模态间梯度冲突的干扰，导致单模态学习器的优化方向出现偏差。
method: 提出GAAL范式，通过交替单模态学习解耦梯度，并结合基于不确定性的梯度手术实现跨模态梯度对齐。
result: 在多个主流数据集上的实验结果表明，GAAL在融合性能和处理表格数据缺失方面均优于现有最先进方法。
conclusion: GAAL通过协调模态间的梯度一致性，有效提升了表格-图像融合模型的整体性能和鲁棒性。
---

## 摘要
多模态表格-图像融合是一项新兴任务，在各个领域受到了越来越多的关注。然而，现有方法可能会受到模态间梯度冲突的阻碍，从而误导单模态学习器的优化。在本文中，我们提出了一种新颖的梯度对齐交替学习（GAAL）范式，通过对齐模态梯度来解决这一问题。具体而言，GAAL 采用交替单模态学习和共享分类器来解耦多模态梯度并促进交互。此外，我们设计了基于不确定性的跨模态梯度手术，以选择性地对齐跨模态梯度，从而引导共享参数使所有模态受益。因此，GAAL 可以提供有效的单模态辅助，并有助于提升整体融合性能。在广泛使用的数据集上进行的实证实验表明，通过与各种最先进的（SoTA）表格-图像融合基准和测试时表格缺失基准进行比较，证明了我们方法的优越性。源代码可在 https://github.com/njustkmg/ICME26-GAAL 获取。

## Abstract
Multimodal tabular-image fusion is an emerging task that has received increasing attention in various domains. However, existing methods may be hindered by gradient conflicts between modalities, misleading the optimization of the unimodal learner. In this paper, we propose a novel Gradient-Aligned Alternating Learning (GAAL) paradigm to address this issue by aligning modality gradients. Specifically, GAAL adopts an alternating unimodal learning and shared classifier to decouple the multimodal gradient and facilitate interaction. Furthermore, we design uncertainty-based cross-modal gradient surgery to selectively align cross-modal gradients, thereby steering the shared parameters to benefit all modalities. As a result, GAAL can provide effective unimodal assistance and help boost the overall fusion performance. Empirical experiments on widely used datasets reveal the superiority of our method through comparison with various state-of-the-art (SoTA) tabular-image fusion baselines and test-time tabular missing baselines. The source code is available at https://github.com/njustkmg/ICME26-GAAL.

---

## 论文详细总结（自动生成）

### 论文总结：通过梯度对齐交替学习实现协调的表格-图像融合 (GAAL)

#### 1. 核心问题与研究动机
*   **核心问题**：在多模态表格-图像（Tabular-Image）融合任务中，传统的联合学习（Joint Learning）由于统一的优化目标，容易导致不同模态之间产生**梯度冲突**（即梯度向量夹角余弦值为负）。
*   **研究背景**：这种冲突会误导单模态编码器的优化，导致模型无法充分挖掘单模态的潜力。实验观察发现，现有方法在处理此类任务时，图像模态的性能甚至可能低于纯单模态训练的结果。
*   **目标**：提出一种能够解耦模态梯度、解决共享层冲突并增强跨模态交互的学习范式。

#### 2. 方法论
论文提出了 **GAAL (Gradient-Aligned Alternating Learning)** 框架，核心包含以下三个部分：
*   **梯度对齐交替学习 (GAAL)**：采用交替训练策略，每一轮迭代仅更新一个模态的编码器。这种方式在编码器层面自然解耦了多模态梯度，避免了直接的冲突。
*   **基于不确定性的跨模态梯度手术 (CGS)**：虽然编码器解耦了，但共享分类器（Interaction Layer）仍面临冲突。CGS 通过**二次规划 (QP)** 调整当前模态的梯度。当当前梯度与前一模态梯度方向相反时，将其投影到前一模态梯度的方向上，在最小化欧氏距离的同时确保梯度对齐（满足约束 $g_p^\top \tilde{g} \ge \epsilon$）。
*   **不确定性引导梯度 (UGG)**：为了提供更有效的引导信号，模型利用**信息熵**衡量样本不确定性，从前一模态中筛选出“硬样本”（高熵样本）来计算引导梯度。这使得模型能够专注于边缘案例，提升跨模态交互的质量。

#### 3. 实验设计
*   **数据集**：
    1.  **DVM**：大型汽车营销数据集（145万张图像及对应表格参数）。
    2.  **SUNAttribute**：场景属性数据集（717个类别）。
    3.  **CelebA**：人脸属性数据集（约20万张图像）。
*   **Benchmark 与对比方法**：
    *   **表格-图像融合类**：DAFT, TIP, CF, MF。
    *   **测试时表格缺失类**：MMCL, CHARMS, KD, FMR 等。
    *   **梯度冲突解决类**：OGM, MMPareto, MLA, DI-MML, ReconBoost 等。
*   **评估指标**：分类准确率 (Accuracy)。

#### 4. 资源与算力
*   **硬件**：实验明确指出在 **NVIDIA RTX A6000** GPU 上运行。
*   **软件/策略**：使用了网格搜索（Grid Search）确定超参数，并采用早停法（Early Stopping）选择最佳模型。
*   **未说明项**：论文未明确提及具体的训练总时长、使用的 GPU 数量（推测为单机单卡或少量卡）以及具体的显存占用情况。

#### 5. 实验数量与充分性
*   **实验规模**：在三个不同规模和领域的数据集上进行了验证，实验结果给出了多模态、图像单模态、表格单模态以及表格缺失场景下的多项指标。
*   **消融实验**：针对交替学习、梯度手术 (CGS) 和不确定性引导 (UGG) 分别做了消融，验证了各组件的增益。
*   **参数敏感性**：分析了硬样本比例 $\lambda$ 和约束裕度 $\epsilon$ 对性能的影响。
*   **评价**：实验设计较为全面，对比了从传统融合到最新解决梯度冲突的各类 SOTA 方法，实验结果具有较强的说服力和客观性。

#### 6. 主要结论与发现
*   **性能领先**：GAAL 在所有三个数据集上均达到了 SOTA 性能，不仅提升了多模态融合效果，也显著增强了单模态的表征能力。
*   **鲁棒性强**：在“测试时表格数据缺失”的严苛条件下，GAAL 依然保持了极高的准确率，证明其通过梯度引导成功将表格知识迁移到了图像分支。
*   **解决冲突**：通过可视化证明，GAAL 有效将原本负相关的模态梯度转变为正相关或正交，缓解了优化过程中的“拉锯”现象。

#### 7. 优点与亮点
*   **创新范式**：将交替学习与梯度投影手术结合，既保留了单模态的独立优化空间，又通过共享层实现了协同。
*   **硬样本聚焦**：引入不确定性采样，避免了简单样本产生的微弱梯度稀释有效的引导信号。
*   **通用性**：该方法不依赖于特定的模型架构（如 Transformer 或 CNN 均可适用），具有较好的普适性。

#### 8. 不足与局限
*   **任务覆盖面**：目前仅在**分类任务**上进行了验证。对于回归（如价格预测）或目标检测等更复杂的多模态任务，其梯度对齐策略的有效性尚待证明。
*   **计算开销**：虽然交替学习降低了单次更新的计算量，但引入二次规划（QP）求解和高熵样本筛选会增加额外的训练计算负担。
*   **超参数依赖**：硬样本比例 $\lambda$ 和梯度对齐强度 $\epsilon$ 对结果有一定影响，可能需要针对不同数据集进行精细调优。

（完）
