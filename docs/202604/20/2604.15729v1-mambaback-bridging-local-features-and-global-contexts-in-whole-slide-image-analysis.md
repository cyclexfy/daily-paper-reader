---
title: "MambaBack: Bridging Local Features and Global Contexts in Whole Slide Image Analysis"
title_zh: MambaBack：在全切片图像分析中桥接局部特征与全局上下文
authors: "Sicheng Chen, Chad Wong, Tianyi Zhang, Enhui Chai, Zeyu Liu, Fei Xia"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.15729v1"
tags: ["query:cpath"]
score: 10.0
evidence: 使用基于Mamba的多实例学习进行全切片图像癌症诊断
tldr: 本研究针对全扫描病理图像（WSI）分析中Mamba模型存在的空间局部性缺失、局部特征建模不足及推理内存占用高的问题，提出了MambaBack架构。该模型结合了Mamba与MambaOut的优势，通过希尔伯特采样保留空间信息，利用1D门控CNN提取局部细胞特征，并结合BiMamba2聚合全局上下文。此外，采用非对称分块设计降低了推理内存峰值，在五个数据集上均优于现有SOTA方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.15729v1/fig-001.webp\", \"caption\": \"Figure 1: Overview of MambaBack. a. MIL pipeline with MambaBack. b. MambaBack structure. c. Heatmap visualization. d. Inference memory usage comparison.\", \"page\": 2, \"index\": 1, \"width\": 1068, \"height\": 802}, {\"url\": \"assets/figures/arxiv/2604.15729v1/fig-002.webp\", \"caption\": \"Table 1: The performance comparison with SOTA methods.\", \"page\": 5, \"index\": 2, \"width\": 1064, \"height\": 359}, {\"url\": \"assets/figures/arxiv/2604.15729v1/fig-003.webp\", \"caption\": \"Figure 2: Analysis of WSI tile distributions and ablation studies on key components. a. Distribution of tile counts across datasets. b. Ablation study on sampling strategies on PANDA dataset. c. Ablation study on model architectures on PANDA dataset.\", \"page\": 5, \"index\": 3, \"width\": 1068, \"height\": 463}]"
motivation: 解决现有Mamba模型在WSI分析中破坏2D空间局部性、局部特征提取不佳以及边缘设备推理内存占用过高的问题。
method: 提出一种混合架构，包含希尔伯特采样策略、结合门控CNN与BiMamba2的分层结构，以及支持高效推理的非对称分块设计。
result: 在五个公开数据集上的实验结果表明，MambaBack的性能全面超越了包括Transformer和纯Mamba在内的七种最先进方法。
conclusion: MambaBack成功融合了局部与全局特征建模，在提升病理图像分析准确性的同时，显著优化了模型的部署效率。
---

## 摘要
全切片图像（WSI）分析在计算病理学中至关重要，它通过整合不同放大倍率下的形态学和结构线索来实现癌症诊断。多实例学习（MIL）是 WSI 分析的标准框架。最近，Mamba 已成为 MIL 中极具前景的主干网络，凭借其源自自然语言处理（NLP）的高效性和全局上下文建模能力，超越了 Transformer。然而，现有的基于 Mamba 的 MIL 方法面临三个关键挑战：（1）在 1D 序列展平过程中破坏了 2D 空间局部性；（2）对细粒度局部细胞结构的建模效果欠佳；（3）在资源受限的边缘设备上进行推理时存在高内存峰值。诸如 MambaOut 等研究表明，Mamba 的 SSM 组件在局部特征提取方面是冗余的，而门控卷积神经网络（Gated CNNs）已足够。认识到 WSI 分析既需要类似于自然图像的细粒度局部特征提取，又需要类似于 NLP 的全局上下文建模，我们提出了 MambaBack，这是一种融合了 Mamba 和 MambaOut 优势的新型混合架构。首先，我们提出了希尔伯特（Hilbert）采样策略，以保留 1D 序列中图块的 2D 空间局部性，从而增强模型的空间感知能力。其次，我们设计了一个分层结构，包含一个基于 MambaOut 的 1D 门控 CNN 模块用于捕获局部细胞特征，以及一个 BiMamba2 模块用于聚合全局上下文，共同增强多尺度表示。最后，我们实现了一种非对称分块设计，允许在训练期间进行并行处理，并在推理期间进行分块流式累积，从而最大限度地降低部署时的峰值内存占用。在五个数据集上的实验结果表明，MambaBack 的性能优于七种最先进的方法。源代码和数据集已公开。

## Abstract
Whole Slide Image (WSI) analysis is pivotal in computational pathology, enabling cancer diagnosis by integrating morphological and architectural cues across magnifications. Multiple Instance Learning (MIL) serves as the standard framework for WSI analysis. Recently, Mamba has become a promising backbone for MIL, overtaking Transformers due to its efficiency and global context modeling capabilities originating from Natural Language Processing (NLP). However, existing Mamba-based MIL approaches face three critical challenges: (1) disruption of 2D spatial locality during 1D sequence flattening; (2) sub-optimal modeling of fine-grained local cellular structures; and (3) high memory peaks during inference on resource-constrained edge devices. Studies like MambaOut reveal that Mamba's SSM component is redundant for local feature extraction, where Gated CNNs suffice. Recognizing that WSI analysis demands both fine-grained local feature extraction akin to natural images, and global context modeling akin to NLP, we propose MambaBack, a novel hybrid architecture that harmonizes the strengths of Mamba and MambaOut. First, we propose the Hilbert sampling strategy to preserve the 2D spatial locality of tiles within 1D sequences, enhancing the model's spatial perception. Second, we design a hierarchical structure comprising a 1D Gated CNN block based on MambaOut to capture local cellular features, and a BiMamba2 block to aggregate global context, jointly enhancing multi-scale representation. Finally, we implement an asymmetric chunking design, allowing parallel processing during training and chunking-streaming accumulation during inference, minimizing peak memory usage for deployment. Experimental results on five datasets demonstrate that MambaBack outperforms seven state-of-the-art methods. Source code and datasets are publicly available.

---

## 论文详细总结（自动生成）

这是一份关于论文《MambaBack: Bridging Local Features and Global Contexts in Whole Slide Image Analysis》的结构化深度总结：

### 1. 核心问题与研究动机
全切片图像（WSI）分析是计算病理学的核心，但面临“吉像素”级别的尺度挑战。目前主流的 Mamba 模型虽然在处理长序列上比 Transformer 更高效，但在 WSI 任务中存在三个痛点：
*   **空间局部性缺失**：将 2D 图像块展平为 1D 序列时，传统的扫描方式破坏了组织结构的 2D 空间关系。
*   **局部特征建模不足**：研究表明 Mamba 的状态空间模型（SSM）在提取细粒度局部特征（如细胞形态）时不如卷积神经网络（CNN）有效。
*   **推理内存瓶颈**：在资源受限的临床边缘设备上，处理超长序列容易导致内存溢出（OOM）。

### 2. 方法论
MambaBack 提出了一种混合架构，旨在结合 CNN 的局部感知能力和 Mamba 的全局建模能力：
*   **希尔伯特采样策略（Hilbert Sampling）**：利用希尔伯特空间填充曲线的特性，将 2D 坐标映射到 1D 索引。相比行优先扫描，希尔伯特曲线能最大限度地保留 1D 序列中相邻图块的 2D 空间邻域关系。
*   **分层混合结构**：
    *   **局部阶段**：采用基于 MambaOut 的 **1D 门控 CNN 模块**。它通过深度卷积（DWConv）和门控机制捕获局部细胞特征，随后通过局部门控注意力机制将每个分块（Chunk）压缩为单个代表性 Token。
    *   **全局阶段**：采用 **双向 Mamba2（BiMamba2）模块**。在压缩后的 Token 序列上进行双向建模，聚合全片级别的全局上下文信息。
*   **非对称分块设计（Asymmetric Design）**：
    *   **训练时**：全序列并行处理，利用 GPU 算力实现高吞吐量。
    *   **推理时**：采用“分块与累积”策略，在 CPU 上分块并逐个输入模型，立即丢弃中间特征图，使内存占用不随 WSI 尺寸线性增长，实现常数级的内存峰值。

### 3. 实验设计
*   **数据集与任务**：涵盖 5 个公开数据集，包括二分类（CAMELYON16）、多分类（CAMELYON17）、癌症分级（PANDA）、亚型分类（TCGA-NSCLC）以及生存分析（TCGA-BRCA）。
*   **Benchmark 与对比方法**：对比了 7 种最先进（SOTA）的方法，包括：
    *   传统 MIL：ABMIL, CLAM, DTFD-MIL。
    *   Transformer 类：TransMIL, Prov-GigaPath。
    *   SSM/RNN 类：MambaMIL, PathRWKV。
*   **特征提取**：所有方法均统一使用预训练的 Prov-GigaPath 基础模型作为特征提取器。

### 4. 资源与算力
*   **硬件环境**：使用了 **4 张 NVIDIA RTX 4090 GPU**。
*   **软件环境**：Python 3.12, PyTorch 2.9, CUDA 12.8。
*   **训练细节**：使用 AdamW 优化器，学习率在 $10^{-4}$ 到 $10^{-2}$ 之间搜索，采用早停机制（Patience=10）。

### 5. 实验数量与充分性
*   **实验规模**：在总计 **17,828 张 WSI** 上进行了验证。
*   **验证方式**：所有实验均采用 **5 折交叉验证**，并报告了平均值和标准差，确保了结果的统计可靠性。
*   **消融实验**：针对采样策略（对比随机、顺序、Z-order）、模型架构（对比纯 Mamba、纯 CNN、反向混合结构）以及推理内存占用进行了详细对比。
*   **客观性**：实验覆盖了从分类到回归（生存分析）的不同任务，对比基准包含了最新的基础模型（GigaPath），实验设计较为公平、客观。

### 6. 主要结论与发现
*   **性能领先**：MambaBack 在所有 5 个数据集上的各项指标（AUC, F1, C-Index）均达到 SOTA。
*   **空间感知的重要性**：希尔伯特采样在 PANDA 数据集上表现出最低的方差和最高的准确度，证明了保留 2D 空间局部性对病理诊断的价值。
*   **混合架构的优越性**：CNN 负责“微观”特征，Mamba 负责“宏观”上下文，这种分工比单一架构更符合病理学家的诊断逻辑。
*   **部署友好**：非对称设计使 MambaBack 的推理内存占用接近轻量级模型（如 CLAM），远低于 Transformer 类模型。

### 7. 优点与亮点
*   **架构创新**：巧妙地将 MambaOut（证明了 SSM 在局部任务的冗余性）与 Mamba2 结合，解决了 SSM 在视觉任务中的固有短板。
*   **空间保留**：引入希尔伯特曲线解决 1D 序列化带来的拓扑破坏问题，比传统的 Z-order 更适合不规则的组织区域。
*   **工程实用性**：非对称分块设计直接解决了 WSI 模型在临床边缘设备（如 FPGA 或低显存工作站）上部署的 OOM 痛点。

### 8. 不足与局限
*   **硬件验证缺失**：虽然论文强调了边缘设备部署，但实验主要在高性能 GPU（RTX 4090）上完成，未在实际的低功耗边缘硬件（如 Jetson 或 FPGA）上展示运行效率。
*   **依赖预训练模型**：模型性能高度依赖于第一阶段冻结的特征提取器（Prov-GigaPath），若基础模型在某些罕见病种上表现不佳，MambaBack 的上限会受限。
*   **超参数敏感性**：分块大小（Chunk Size $L$）对局部特征提取和全局聚合的平衡至关重要，论文中未深入探讨不同 $L$ 值对不同放大倍率图像的影响。

（完）
