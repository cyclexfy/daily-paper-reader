---
title: "SegMix:Shuffle-based Feedback Learning for Semantic Segmentation of Pathology Images"
title_zh: SegMix：基于打乱反馈学习的病理图像语义分割
authors: "Zhiling Yan, Sicheng Chen, Tianyi Zhang, Nan Ying, Yanli Lei, Guanglei Zhang"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.15777v1"
tags: ["query:cpath"]
score: 9.0
evidence: 利用图像级标签进行病理图像语义分割
tldr: 本文针对病理图像语义分割中像素级标注成本高昂的问题，提出了一种名为SegMix的弱监督学习框架。该方法仅利用图像级标签，通过受课程学习启发的打乱反馈机制生成高质量伪掩码。SegMix对图像进行块级打乱，并根据模型反馈动态调整打乱策略，从而引导模型关注更广泛的病理特征。实验结果显示，该方法在三个主流数据集上均取得了优于现有最先进技术的分割性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.15777v1/fig-001.webp\", \"caption\": \"Figure 1: Overview of the proposed method. The model adaptively adjusts the schedulers in the left blue box based on the feedback from previous learning. The patch size scheduler and shuffle ratio scheduler regulate the patch size and shuffle ratio. Then, relation patches are in-place shuffled across the same batch, maintaining their relative positions the same in images. Lastly, the mixed and original images are fed into the Weekly Supervised Segmentation Network for twins’ steps training in the green boxes.\", \"page\": 2, \"index\": 1, \"width\": 1068, \"height\": 511}, {\"url\": \"assets/figures/arxiv/2604.15777v1/fig-002.webp\", \"caption\": \"Table 1: Dice score (%) and IoU (%) of methods for weakly supervised semantic segmentation. Ours w/o FL denotes the proposed method without the feedback learning. The best results are in bold.\", \"page\": 4, \"index\": 2, \"width\": 826, \"height\": 394}]"
motivation: 针对病理图像像素级标注困难及现有CAM方法生成的伪掩码激活区域不足的问题，探索利用图像级标签实现高质量分割。
method: 提出一种基于打乱的反馈学习方法，通过对图像块进行自适应打乱并结合模型反馈来优化伪掩码的生成过程。
result: 实验结果表明，该方法在三个不同的病理数据集上均显著优于现有的最先进弱监督分割方法。
conclusion: SegMix通过创新的自适应打乱策略有效提升了弱监督病理图像分割的准确性，为降低标注成本提供了新思路。
---

## 摘要
分割是计算病理学中的一项关键任务，因为它能识别受疾病或异常生长影响的区域，对于诊断和治疗至关重要。然而，获取高质量的像素级监督分割数据需要经验丰富的病理学家投入大量工作，这限制了深度学习的应用。为了克服这一挑战，将标注条件放宽至图像级分类标签，可以利用更多数据并启用更多应用场景。一种方法是利用类激活图（CAM），在仅有图像级标签的情况下生成用于语义分割的伪像素级标注。然而，该方法未能深入挖掘病理图像的本质特征，导致仅能识别出较小的区域，不足以生成有效的伪掩码。在本文中，我们受课程学习的启发，提出了一种新颖的基于打乱的反馈学习方法，用于生成更高质量的伪语义分割掩码。具体而言，我们对病理图像进行补丁级（patch-level）打乱，模型根据先前学习的反馈自适应地调整打乱策略。实验结果表明，我们提出的方法在三个不同的数据集上均优于现有最先进的方法。

## Abstract
Segmentation is a critical task in computational pathology, as it identifies areas affected by disease or abnormal growth and is essential for diagnosis and treatment. However, acquiring high-quality pixel-level supervised segmentation data requires significant workload demands from experienced pathologists, limiting the application of deep learning. To overcome this challenge, relaxing the label conditions to image-level classification labels allows for more data to be used and more scenarios to be enabled. One approach is to leverage Class Activation Map (CAM) to generate pseudo pixel-level annotations for semantic segmentation with only image-level labels. However, this method fails to thoroughly explore the essential characteristics of pathology images, thus identifying only small areas that are insufficient for pseudo masking. In this paper, we propose a novel shuffle-based feedback learning method inspired by curriculum learning to generate higher-quality pseudo-semantic segmentation masks. Specifically, we perform patch level shuffle of pathology images, with the model adaptively adjusting the shuffle strategy based on feedback from previous learning. Experimental results demonstrate that our proposed approach outperforms state-of-the-arts on three different datasets.

---

## 论文详细总结（自动生成）

这是一份关于论文《SegMix: Shuffle-based Feedback Learning for Semantic Segmentation of Pathology Images》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
在计算病理学中，精确的语义分割对疾病诊断至关重要。然而，获取高质量的**像素级标注**（Pixel-level annotations）极其耗时且依赖资深病理医生的经验。
*   **研究动机**：为了降低标注成本，研究者转向**弱监督语义分割（WSSS）**，即仅利用图像级标签（分类标签）来生成分割掩码。
*   **现有挑战**：传统的类激活图（CAM）方法往往只能识别出最具辨识度的小区域，无法覆盖完整的病理区域，且忽略了病理图像特有的局部特征（如细胞核）、全局特征（组织排列）及多尺度关系。

### 2. 核心方法论
论文提出了一种名为 **SegMix** 的框架，核心思想是通过**基于打乱（Shuffle）的反馈学习机制**，模拟病理学家从粗到细的观察过程。

*   **反馈学习模块（Feedback Learning Module）**：
    *   受课程学习（Curriculum Learning）启发，设计了**补丁大小调度器**（Patch-size scheduler）和**打乱比例调度器**（Shuffle ratio scheduler）。
    *   **自适应调整**：模型根据训练损失（Loss）的反馈动态调整策略。当 Loss 低于阈值 $T$ 时，减小补丁大小 $p$（从组织级细化到细胞级），并增加打乱比例 $f$（增加图像混合复杂度）。
*   **补丁打乱机制（Shuffle Patches）**：
    *   将图像分割为补丁，在同批次（Batch）内的不同图像间进行**原位打乱（In-place shuffle）**。
    *   这种机制强制模型学习跨样本的实例关系，并能有效抑制病理图像中的噪声干扰。
*   **像素相关性模块（PCM）**：
    *   利用像素间的余弦相似度来优化原始 CAM，通过加权求和提升预测结果的一致性。

### 3. 实验设计
*   **数据集**：
    1.  **ROSE**：胰腺癌细胞病理图像（1773张癌症，3315张正常）。
    2.  **WBC**：白细胞数据集（包含5类细胞，共约1.4万张图像）。
    3.  **MARS**：胃癌图像（1770张，包含正常、管状腺癌、粘液腺癌）。
*   **Benchmark（对比方法）**：对比了经典的 **CAM** 以及先进的弱监督方法 **SEAM** 和 **CPN**。
*   **评价指标**：Dice Score (DSC) 和 IoU。

### 4. 资源与算力
*   **硬件**：Ubuntu 服务器，配置 Intel Xeon Platinum 8350C CPU 和 **2 张 Nvidia RTX 3090 GPU**。
*   **软件**：PyTorch 1.10.0, CUDA 11.3。
*   **训练细节**：实验中每次仅使用 **1 张 GPU**。优化器训练 8 个 iteration，Batch Size 为 4。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个不同类型的病理数据集上进行了验证，涵盖了细胞级和组织级的分割任务。
*   **消融实验**：对比了“无反馈学习（Ours w/o FL）”、“组内打乱（Ours-Group）”和“回退策略（Ours-Back）”等变体。
*   **充分性评价**：实验设计较为充分，通过跨数据集的对比和可视化分析，客观地证明了该方法在处理病理图像多尺度特征和噪声干扰方面的优越性。

### 6. 主要结论与发现
*   **性能领先**：SegMix 在所有三个数据集上均达到了 SOTA（最先进）水平。例如在 ROSE 数据集上，平均 IoU 达到 50.2%，显著优于 SEAM (43.7%)。
*   **反馈学习的价值**：消融实验证明，自适应调整补丁大小和打乱比例能显著提升分割精度（DSC 提升高达 6.9%）。
*   **鲁棒性**：打乱机制能有效减少病理图像人工处理带来的噪声干扰，使模型更关注目标实例本身。

### 7. 优点
*   **自适应性**：引入反馈机制，使模型能根据学习进度自动从简单任务（粗粒度）过渡到复杂任务（细粒度）。
*   **领域针对性**：专门针对病理图像的局部/全局特征和多尺度关系设计了打乱策略。
*   **标注高效**：仅需图像级标签即可生成高质量的像素级伪掩码。

### 8. 不足与局限
*   **维度限制**：目前仅针对 2D 病理图像进行了验证，尚未扩展到 3D 医疗影像。
*   **超参数敏感性**：反馈机制中的阈值 $T$ 和增长因子 $\alpha$ 可能需要针对不同数据集进行精细调优。
*   **计算开销**：虽然仅需图像级标签，但反馈学习和多次打乱可能会增加训练阶段的逻辑复杂度和收敛时间。

（完）
