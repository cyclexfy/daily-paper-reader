---
title: "OkanNet: A Lightweight Deep Learning Architecture for Classification of Brain Tumor from MRI Images"
title_zh: OkanNet：一种用于 MRI 图像脑肿瘤分类的轻量级深度学习架构
authors: "Okan Uçar, Murat Kurt"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.01264v1"
tags: ["query:cpath"]
score: 8.0
evidence: 脑肿瘤的自动检测与分类
tldr: "针对脑肿瘤MRI图像分析耗时且易出错的问题，本研究对比了两种深度学习方法：自研的轻量级CNN架构OkanNet和基于迁移学习的ResNet-50。在包含7023张图像的数据集上，ResNet-50实现了96.49%的高精度，而OkanNet虽精度稍低（88.10%），但训练速度快3.2倍，为计算资源受限的移动和嵌入式医疗设备提供了高效的替代方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.01264v1/fig-001.webp\", \"caption\": \"Table I PERFORMANCE METRICS OF MODELS\", \"page\": 3, \"index\": 1, \"width\": 518, \"height\": 368}, {\"url\": \"assets/figures/arxiv/2604.01264v1/fig-002.webp\", \"caption\": \"Figure 1. Training Processes of Models. The blue line (OkanNet) represents learning from scratch, while the red dashed line (ResNet-50) represents the adaptation of transferred knowledge.\", \"page\": 3, \"index\": 2, \"width\": 504, \"height\": 306}, {\"url\": \"assets/figures/arxiv/2604.01264v1/fig-003.webp\", \"caption\": \"Figure 3. Sample Predictions Performed with ResNet-50 Model (Green: Correct).\", \"page\": 4, \"index\": 3, \"width\": 504, \"height\": 506}]"
motivation: 旨在解决放射科医生手动分析MRI图像效率低且易受疲劳影响导致误诊的问题。
method: 提出了自定义轻量级架构OkanNet，并将其与预训练的ResNet-50迁移学习模型在多类脑肿瘤分类任务中进行对比。
result: "ResNet-50准确率达96.49%，而OkanNet在保持88.10%准确率的同时，训练时间仅为前者的约三分之一。"
conclusion: 研究揭示了模型深度与计算效率之间的权衡，证明了轻量级模型在资源受限的医疗诊断场景中的应用潜力。
---

## 摘要
医学成像技术，特别是磁共振成像（MRI），被公认为神经系统疾病诊断和治疗规划的金标准。然而，放射科医生对 MRI 图像进行人工分析是一个耗时的过程，且容易因疲劳导致人为错误。在本研究中，开发并对比分析了两种不同的深度学习方法，用于脑肿瘤（胶质瘤、脑膜瘤、垂体瘤和无肿瘤）的自动检测与分类。在第一种方法中，从零开始设计了一个名为“OkanNet”的自定义卷积神经网络（CNN）架构，该架构具有较低的计算成本和较快的训练时间。在第二种方法中，采用了迁移学习（Transfer Learning）方法，使用了在 ImageNet 数据集上预训练的 50 层 ResNet-50 架构。在由 Masoud Nickparvar 编译的包含总计 7,023 张 MRI 图像的扩展数据集上进行的实验中，基于迁移学习的 ResNet-50 模型表现出优异的分类性能，达到了 96.49% 的准确率和 0.963 的精确率。相比之下，自定义的 OkanNet 架构达到了 88.10% 的准确率；然而，在训练时间方面，它比 ResNet-50 快约 3.2 倍（311 秒），证明了它是计算能力有限的移动和嵌入式系统的有力替代方案。本研究通过实验数据展示了医学图像分析中模型深度与计算效率之间的权衡。

## Abstract
Medical imaging techniques, especially Magnetic Resonance Imaging (MRI), are accepted as the gold standard in the diagnosis and treatment planning of neurological diseases. However, the manual analysis of MRI images is a time-consuming process for radiologists and is prone to human error due to fatigue. In this study, two different Deep Learning approaches were developed and analyzed comparatively for the automatic detection and classification of brain tumors (Glioma, Meningioma, Pituitary, and No Tumor). In the first approach, a custom Convolutional Neural Network (CNN) architecture named "OkanNet", which has a low computational cost and fast training time, was designed from scratch. In the second approach, the Transfer Learning method was applied using the 50-layer ResNet-50 [1] architecture, pre-trained on the ImageNet dataset. In experiments conducted on an extended dataset compiled by Masoud Nickparvar containing a total of $7,023$ MRI images, the Transfer Learning-based ResNet-50 model exhibited superior classification performance, achieving $96.49\%$ Accuracy and $0.963$ Precision. In contrast, the custom OkanNet architecture reached an accuracy rate of $88.10\%$; however, it proved to be a strong alternative for mobile and embedded systems with limited computational power by yielding results approximately $3.2$ times faster ($311$ seconds) than ResNet-50 in terms of training time. This study demonstrates the trade-off between model depth and computational efficiency in medical image analysis through experimental data.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **OkanNet** 的轻量级深度学习架构，旨在解决脑肿瘤 MRI 图像自动分类中的效率与精度权衡问题。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：放射科医生手动分析大量 MRI 切片不仅耗时，且容易因疲劳导致误诊。现有的深度学习模型虽然精度高，但往往计算资源消耗大，难以在移动或嵌入式医疗设备上运行。
*   **研究动机**：开发一种既能保持可接受的分类精度，又能显著降低计算成本和训练时间的轻量级 CNN 架构，并将其与成熟的迁移学习模型（ResNet-50）进行对比，探讨医疗影像分析中“深度”与“效率”的平衡。

### 2. 核心方法论
论文提出了两种主要的分类策略：
*   **OkanNet（自研架构）**：
    *   **核心思想**：从零开始设计的轻量级 CNN，模仿 LeNet-5 和 AlexNet 的简洁性。
    *   **技术细节**：包含三个连续的卷积块。每个块使用 3×3 卷积核（滤波器数量分别为 16、32、64）提取局部特征，随后接 ReLU 激活函数和 2×2 的最大池化层（Max Pooling）。
    *   **分类层**：最后接一个 128 单元的全连接层，并使用 50% 的 Dropout 技术防止过拟合，最终通过 Softmax 层输出 4 类结果。
*   **ResNet-50（迁移学习）**：
    *   **核心思想**：利用在 ImageNet 上预训练的 50 层残差网络进行微调（Fine-tuning）。
    *   **技术细节**：保留残差块（Skip Connections）结构，将原有的 1000 类输出层替换为针对本任务的 4 类输出层。

### 3. 实验设计
*   **数据集**：使用 Masoud Nickparvar 编译的“Brain Tumor MRI Dataset”，包含 **7,023 张** 图像（涵盖 T1、T2 和 FLAIR 序列）。
*   **分类目标**：四类——胶质瘤（Glioma）、脑膜瘤（Meningioma）、垂体瘤（Pituitary）和无肿瘤（No Tumor）。
*   **预处理**：图像统一缩放至 224×224，进行灰度转 RGB 处理，并应用随机旋转、水平翻转和平移等数据增强手段。
*   **对比基准（Benchmark）**：以预训练的 ResNet-50 作为性能上限基准，对比 OkanNet 在速度和精度上的表现。

### 4. 资源与算力
*   **硬件环境**：NVIDIA GeForce **RTX 2060** GPU。
*   **软件环境**：MATLAB R2023b 及深度学习工具箱。
*   **训练时长**：
    *   **OkanNet**：约 **311 秒**（约 5 分钟）。
    *   **ResNet-50**：约 **1000 秒**（约 16 分钟）。
*   **超参数**：训练 8 个 Epoch，Mini-batch 大小为 32，学习率为 0.0001。

### 5. 实验数量与充分性
*   **实验规模**：论文对两种模型在相同的数据集分割和超参数下进行了对比实验。
*   **充分性评价**：
    *   **数据量充足**：7,023 张图像在医学影像分类中属于中大规模，具有一定的代表性。
    *   **评估指标全面**：使用了准确率（Accuracy）、精确率（Precision）、召回率（Recall）、F1 分数和混淆矩阵。
    *   **局限性**：实验仅在单一数据集上进行，且仅对比了 ResNet-50 一种成熟架构，缺乏与其他轻量级模型（如 MobileNet 或 EfficientNet）的横向对比。

### 6. 主要结论与发现
*   **性能对比**：ResNet-50 准确率更高（**96.49%**），而 OkanNet 准确率为 **88.10%**。
*   **效率优势**：OkanNet 的训练速度比 ResNet-50 快约 **3.2 倍**，且参数量更少，更适合硬件受限的场景。
*   **分类特征**：两种模型在识别“无肿瘤”类别上几乎完美（>98%），但 OkanNet 在区分组织特征相似的胶质瘤和脑膜瘤时错误率较高。
*   **权衡结论**：若追求极致精度，首选 ResNet-50；若追求在移动设备上的实时性或低功耗运行，OkanNet 是更优选择。

### 7. 优点
*   **实用性强**：针对移动医疗和嵌入式 MRI 设备提出了具体的轻量化方案。
*   **透明度高**：论文附录中提供了完整的 MATLAB 源代码，便于其他研究者复现和改进。
*   **分析客观**：没有盲目追求高准确率，而是深入探讨了计算成本与性能之间的权衡。

### 8. 不足与局限
*   **精度差距**：OkanNet 与迁移学习模型之间仍有约 8% 的准确率差距，在严苛的临床诊断中可能存在风险。
*   **类别混淆**：对于相似肿瘤类型的区分能力有待加强。
*   **实验覆盖面**：未探讨不同 Epoch 数对模型收敛的影响，且未在移动端实际部署测试（仅为理论推断）。
*   **未来工作**：作者提到未来需引入 GAN 等方法解决类别不平衡问题，并尝试更现代的混合架构。

（完）
