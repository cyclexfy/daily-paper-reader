---
title: "PDMP: Rethinking Balanced Multimodal Learning via Performance-Dominant Modality Prioritization"
title_zh: PDMP：通过性能主导模态优先化重新思考平衡多模态学习
authors: "Shicai Wei, Chunbo Luo, Qiang Zhu, Yang Luo"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05773v1"
tags: ["query:cpath"]
score: 8.0
evidence: 通过性能主导模态优先级优化多模态学习
tldr: 针对多模态学习中模型性能往往不如单模态的问题，本文挑战了传统的“平衡学习”观念，提出性能主导模态优先（PDMP）策略。该方法通过识别单模态性能最优的模态，并利用非对称梯度调节系数使其主导优化过程，从而解决主导模态学习不足导致的欠优化问题。实验证明，PDMP在多种数据集上均优于现有平衡学习方法，具有较强的通用性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.05773v1/fig-001.webp\", \"caption\": \"TABLE I COMPARISON WITH EXISTING MODULATION STRATEGIES ON CREMA-D, KINETICS-SOUNDS, AND AVE DATASETS. THE PROPOSED PDMP ACHIEVES THE BEST PERFORMANCE. BOLD AND UNDERLINE MEAN THE BEST AND SECOND-BEST RESULTS, RESPECTIVELY.\", \"page\": 6, \"index\": 1, \"width\": 744, \"height\": 275}, {\"url\": \"assets/figures/arxiv/2604.05773v1/fig-002.webp\", \"caption\": \"Fig. 1. The visualization on two audio-visual datasets: CREMA-D and AVE. (a) and (b) present the performance curves of the multimodal models under different values of w. w is the optimization dependency coefficient (see Eq.5), which measures the relative contribution of the audio and visual modalities during training. ‘vanilla’ means the default w without any gradient modulation. (c) and (d) present the unimodal performance curves.\", \"page\": 2, \"index\": 2, \"width\": 1076, \"height\": 240}, {\"url\": \"assets/figures/arxiv/2604.05773v1/fig-003.webp\", \"caption\": \"Fig. 2. Illustration of back-propagation processing for balanced learning and the proposed performance-dominant modality prioritization. Balanced learning mines the optimization-dominant modality and aligns its gradient go with the gradient gr of the remaining modality. In contrast, performance-dominant modality prioritization mines the performance-dominant modality and makes its gradient gp larger than the gradient gr of the remaining modality, guiding it to dominate the optimization.\", \"page\": 3, \"index\": 3, \"width\": 860, \"height\": 496}, {\"url\": \"assets/figures/arxiv/2604.05773v1/fig-004.webp\", \"caption\": \"Fig. 4. Visualization of optimization dependency coefficient w on CREMAD (a) and AVE (b) dataset, respectively. Here, s0 and s1 in Eq. 5 represent the logit output of audio and visual modalities, respectively. ‘Vanilla’ denotes the conventional multimodal model trained without PDMP.‘PDMP’ denotes the model trained with PDMP, respectively.\", \"page\": 8, \"index\": 4, \"width\": 529, \"height\": 233}, {\"url\": \"assets/figures/arxiv/2604.05773v1/fig-005.webp\", \"caption\": \"Fig. 3. The visualization on the CREMA-D datasets. (a) presents the performance of the audio and visual branches in the multimodal model. (b) presents the performance of the audio and visual modality in the unimodal model.\", \"page\": 5, \"index\": 5, \"width\": 539, \"height\": 229}]"
motivation: 研究发现多模态学习的欠优化并非源于模态不平衡，而是因为性能主导模态在训练中未能得到充分学习。
method: 通过单模态性能排序确定主导模态，并引入非对称梯度调节系数，使主导模态在多模态训练中占据优化主导地位。
result: 在多个基准数据集上的广泛实验验证了PDMP的优越性，证明其性能显著超过了现有的梯度调节方法。
conclusion: 研究表明，让性能更优的模态主导优化过程比追求模态间的平衡学习更能有效提升多模态模型的整体表现。
---

## 摘要
多模态学习因其广泛的实用性而受到越来越多的关注。然而，它经常面临优化不足的问题，导致多模态模型的表现甚至不如其对应的单模态模型。现有方法将此问题归因于模态间的不平衡学习，并通过梯度调节来解决。本文认为，平衡学习并非多模态学习的最佳设置。相反，由具有优异单模态性能的性能主导模态驱动的不平衡学习，有助于提升多模态性能。且优化不足的问题实际上是由性能主导模态的学习不充分引起的。为此，我们提出了性能主导模态优先化（PDMP）策略来辅助多模态学习。具体而言，PDMP 首先通过独立训练的单模态模型的性能排名来挖掘性能主导模态。随后，PDMP 引入非对称系数来调节各模态的梯度，使性能主导模态在优化中占据主导地位。由于 PDMP 仅依赖于单模态性能排名，它独立于多模态模型的结构和融合方法，在实际场景中具有巨大潜力。最后，在各种数据集上的广泛实验验证了 PDMP 的优越性。

## Abstract
Multimodal learning has attracted increasing attention due to its practicality. However, it often suffers from insufficient optimization, where the multimodal model underperforms even compared to its unimodal counterparts. Existing methods attribute this problem to the imbalanced learning between modalities and solve it by gradient modulation. This paper argues that balanced learning is not the optimal setting for multimodal learning. On the contrary, imbalanced learning driven by the performance-dominant modality that has superior unimodal performance can contribute to better multimodal performance. And the under-optimization problem is caused by insufficient learning of the performance-dominant modality. To this end, we propose the Performance-Dominant Modality Prioritization (PDMP) strategy to assist multimodal learning. Specifically, PDMP firstly mines the performance-dominant modality via the performance ranking of the independently trained unimodal model. Then PDMP introduces asymmetric coefficients to modulate the gradients of each modality, enabling the performance-dominant modality to dominate the optimization. Since PDMP only relies on the unimodal performance ranking, it is independent of the structures and fusion methods of the multimodal model and has great potential for practical scenarios. Finally, extensive experiments on various datasets validate the superiority of PDMP.

---

## 论文详细总结（自动生成）

这篇论文对多模态学习中的“平衡优化”传统观念提出了挑战，并提出了一种新的梯度调节策略。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：多模态模型在训练中经常出现“欠优化”现象，即融合后的模型性能甚至不如单模态模型。
*   **研究动机**：现有研究通常认为这是由于模态间“不平衡”导致的（即强模态抑制了弱模态），因此致力于通过梯度调节实现模态间的平衡。本文作者发现，**平衡学习并不总是最优的**。在某些数据集上，强制平衡反而会降低性能。
*   **核心观点**：多模态学习的瓶颈在于“性能主导模态”（即单模态表现最好的模态）没有得到充分学习。有效的多模态优化应当让性能主导模态在训练中占据主导地位。

### 2. 方法论：PDMP 策略
PDMP（Performance-Dominant Modality Prioritization）包含两个关键阶段：
*   **模态分析（Modality Analysis）**：
    *   通过独立训练单模态模型并对其性能进行排名，确定谁是“性能主导模态”（$m_p$）。
    *   识别“优化主导模态”（$m_o$），即在多模态模型训练初期收敛最快、贡献梯度最大的模态。
*   **梯度调节（Gradient Modulation）**：
    *   引入非对称系数调节梯度。对于性能主导模态 $m_p$，其梯度 $g_p$ 被放大（乘以 $\gamma_p > 1$）。
    *   对于剩余模态 $m_r$，如果它是优化主导的但非性能主导，则抑制其梯度（乘以 $1/\gamma_p$）；如果它既非性能主导也非优化主导，则同样放大其梯度以加速整体收敛。
*   **理论支撑**：从信息论角度证明，为了最大化模型输出与标签之间的互信息，模型应当给预测相关性更强（性能更好）的模态分配更大的权重。

### 3. 实验设计
*   **数据集**：涵盖了 6 个主流多模态数据集，包括音频-视觉（CREMA-D, AVE, Kinetics-Sounds, VGGSound）、动作识别（UCF-101）和人脸防伪（CEFA，包含 RGB/深度/红外三模态）。
*   **对比方法**：
    *   **梯度调节类**：OGM-GE, AGM, PMR。
    *   **正则化/蒸馏类**：Grad-Blending, MLA, MMPareto, D&R。
*   **Benchmark**：以传统的特征拼接（Concatenation）作为基础基准。

### 4. 资源与算力
*   **模型架构**：主干网络采用 ResNet-18，同时也验证了在 Transformer（mmFormer）上的有效性。
*   **训练参数**：Batch size 64，使用 SGD 优化器，学习率 1e-3，训练 100 个 epoch。
*   **算力说明**：论文**未明确提及**具体的 GPU 型号、数量及总训练时长。但提到为了降低模态分析的开销，可以使用数据集的子集（如 40%）来确定模态优先级。

### 5. 实验数量与充分性
*   **实验规模**：在 6 个不同规模和任务的数据集上进行了测试，实验覆盖面广。
*   **消融实验**：非常充分。包括了不同融合方法（拼接、求和、Film、Gated）、不同网络架构（CNN vs Transformer）、超参数 $\gamma_p$ 的敏感性分析、子集采样比例的影响，以及与直接增加学习率的对比。
*   **客观性**：通过在相同硬件/软件环境下重新运行基准方法，确保了对比的公平性。

### 6. 主要结论与发现
*   **打破迷思**：证明了“平衡学习”在 AVE 等数据集上会导致性能下降，而 PDMP 在所有数据集上均实现了性能提升。
*   **性能提升**：在 CREMA-D 上比次优方法提升了 6.25%，在大型数据集 VGGSound 上也保持了领先。
*   **通用性**：PDMP 是一个即插即用的模块，不依赖于特定的融合架构，且能自然扩展到三模态及以上场景。

### 7. 优点
*   **视角独特**：挑战了领域内公认的“平衡学习”假设，提供了新的理论解释（互信息视角）。
*   **简单高效**：不需要复杂的辅助网络或知识蒸馏，仅通过梯度缩放即可实现显著提升。
*   **灵活性强**：对 CNN 和 Transformer 架构均有效，且能处理多于两个模态的情况。

### 8. 不足与局限
*   **额外开销**：虽然可以使用子集，但仍需预先训练单模态模型进行“模态分析”，增加了训练流水线的复杂度。
*   **超参数依赖**：梯度缩放系数 $\gamma_p$ 需要针对不同数据集进行调优（实验显示通常在 10-15 之间最优），缺乏一种完全自适应的确定方法。
*   **应用限制**：主要针对分类任务进行了验证，在生成式多模态任务或多模态检索任务中的表现尚待观察。

（完）
