---
title: "CGRL: Concept-Guided Pruning and Representation Learning for Whole-Slide Image Classification"
title_zh: CGRL：面向全切片图像分类的概念引导剪枝与表示学习
authors: "Thuc Huynh, Tuan Le, Doanh C. Bui"
date: 2026-07-14
pdf: "https://arxiv.org/pdf/2607.12556v1"
tags: ["query:cpath"]
score: 9.0
evidence: 提出概念引导剪枝与表示学习进行弱监督全切片图像分类使用多实例学习
tldr: 弱监督全切片图像分类中，现有MIL方法聚合大量非信息性补丁且特征与类语义对齐不足。提出CGRL，引入基于疾病提示的类概念原型，通过概念相关性剪枝保留top-K相关补丁，并设计概念引导对比学习正则化投影空间。在TCGA-BRCA和NSCLC上，CGRL提升多种MIL模型准确率和macro-F1，同时降低计算成本。表明类语义概念可有效指导补丁选择和表示学习。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.12556v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 854, \"height\": 547, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12556v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 868, \"height\": 504, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12556v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1849, \"height\": 899, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.12556v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1880, \"height\": 698, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12556v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 914, \"height\": 323, \"label\": \"Table\"}]"
motivation: 现有MIL方法依赖视觉线索聚合大量非信息性补丁，导致实例特征与疾病语义对齐弱。
method: 提出CGRL，利用疾病提示类概念原型，通过概念相关性剪枝保留top-K补丁，并用概念引导对比学习优化目标类、对称辅助和跨类分离目标。
result: 在TCGA-BRCA和TCGA-NSCLC上，CGRL提升多种MIL模型的准确率和macro-F1，并减少计算成本。
conclusion: 类语义概念为弱监督病理学中的补丁选择和表示学习提供了有效且实用的先验。
---

## 摘要
弱监督全切片图像分类在计算病理学中广泛应用，因为切片级标签比密集区域标注更容易获取。现有的多实例学习方法通常主要利用视觉线索聚合大量块嵌入，这可能保留许多非信息性块，并且实例特征与类别级疾病语义之间的对齐较弱。我们提出概念引导剪枝与表示学习（CGRL），这是一个简单框架，将从疾病提示中导出的类别级概念原型引入MIL流程。首先，概念相关性剪枝根据块实例与类概念的相似度对其进行排序，并保留前K个概念相关块用于下游MIL聚合。其次，概念引导的对比表示学习从相同的相似度矩阵构建类别的正负块集合，并优化目标类、对称辅助类和跨类分离目标，从而规范投影概念空间。我们使用多种代表性MIL方法在TCGA-BRCA和TCGA-NSCLC上评估CGRL。实验结果表明，CGRL改进了多个模型-数据集组合，增益取决于下游MIL模型和数据集。它在准确率和宏F1分数上取得了特别明显的提升，同时通过概念相关性剪枝降低了计算成本。这些发现表明，在弱监督计算病理学中，类别级语义概念为块选择和表示学习提供了有效且实用的先验。

## Abstract
Weakly supervised whole-slide image (WSI) classification is widely used in computational pathology because slide-level labels are easier to obtain than dense region annotations. Existing multiple instance learning (MIL) methods often aggregate large bags of patch embeddings using mainly visual cues, which can retain many non-informative patches and provide weak alignment between instance features and class-level disease semantics. We propose Concept-Guided Pruning and Representation Learning (CGRL), a simple framework that introduces class-level concept prototypes derived from disease prompts into the MIL pipeline. First, concept-relevance pruning ranks patch instances by their similarity to class concepts and retains the top-K concept-relevant patches for downstream MIL aggregation. Second, concept-guided contrastive representation learning constructs class-wise positive and negative patch sets from the same similarity matrix and optimizes target-class, symmetric auxiliary, and cross-class separation objectives, thereby regularizing the projected concept space. We evaluate CGRL on TCGA-BRCA and TCGA-NSCLC using multiple representative MIL methods. Experimental results show that CGRL improves several model-dataset combinations, with gains depending on the downstream MIL model and dataset. It achieves particularly clear improvements in accuracy and macro-F1 while reducing computational cost through concept-relevance pruning. These findings demonstrate that class-level semantic concepts provide an effective and practical prior for patch selection and representation learning in weakly supervised computational pathology.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 0. 源代码链接
- https://github.com/ThucHuynh44/CGRL

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **背景**：全切片图像（WSI）在计算病理学中广泛使用，但WSI尺寸极大（千兆像素级），端到端监督学习困难，区域级标注昂贵。弱监督多实例学习（MIL）成为主流范式，仅需切片级标签。
- **核心问题**：现有MIL方法主要依赖视觉特征聚合大量图像块嵌入，存在两个关键局限：
  1. 冗余：大量非信息性或弱信息性补丁增加了计算负担，并可能稀释诊断相关信号。
  2. 特征对齐弱：实例特征与高层疾病语义之间缺乏显式对齐，尤其在亚型区分依赖细微形态线索时。
- **整体含义**：本文旨在通过引入类别的语义概念原型，同时实现信息补丁选择和表示学习正则化，提升弱监督WSI分类的性能和效率。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 核心思想
- 利用预训练的视觉-语言模型（CONCH、TITAN）获取冻结的图像嵌入和文本概念原型，构建一个轻量级的**概念引导剪枝与表示学习框架（CGRL）**，以“概念相关性”为桥梁，统一补丁选择和特征空间优化。

### 关键技术细节
1. **概念原型构建**：
   - 对每个类别c，使用疾病相关的文本提示（如“An H&E image of {subtype}”）通过TITAN文本编码器得到概念原型向量 \( t_c \in \mathbb{R}^{D_c} \)。
   - 所有原型和投影后的补丁嵌入均进行L2归一化。

2. **投影头与相似度矩阵**：
   - 使用可学习投影头 \( g_\phi \) 将补丁嵌入 \( x_i \in \mathbb{R}^D \) 映射到概念空间：\( u_i = g_\phi(x_i) \)。
   - 计算补丁与所有概念原型的余弦相似度矩阵 \( S \in \mathbb{R}^{N \times C} \)，其中 \( S_{i,c} = \frac{u_i^\top t_c}{\|u_i\|\|t_c\|} \)。

3. **概念相关性剪枝**：
   - 每个补丁的相关性分数 \( r_i = \max_c S_{i,c} \)。
   - 保留前 \( K_p \) 个（实验中 \( K_p = 300 \)）最高分数对应的补丁，构建剪枝后的包 \( \tilde{X} \) 送入下游MIL模型。
   - 保留原始补丁嵌入（非投影特征）用于MIL聚合，确保不破坏原有特征分布。

4. **概念引导对比表示学习**（训练阶段）：
   - 基于同一相似度矩阵S，对每个类别c，选取相似度最高的前 \( K_{pos} \) 个补丁作为正集 \( P_c \)，最低的 \( K_{neg} \) 个作为负集 \( N_c \)（实验中均为20）。
   - 采用InfoNCE损失，设计三个互补目标：
     - **目标类损失** \( \mathcal{L}_y \)：锚定目标类别原型 \( t_y \)，将正集 \( P_y \) 拉近，负集 \( N_y \) 推远。
     - **对称辅助损失** \( \mathcal{L}_{\text{sym}} \)：对每个非目标类别c（c≠y），同样做原型对齐，增强非目标类内部的内聚性。
     - **跨类分离损失** \( \mathcal{L}_{\text{cross}} \)：对每个非目标类c，计算负集 \( N_c \) 的均值作为“反原型” \( p_{y \to c} \)，让目标类正集远离非目标类正集。
   - 总对比损失：\( \mathcal{L}_{\text{CGRL}} = \mathcal{L}_y + \lambda_{\text{sym}} \mathcal{L}_{\text{sym}} + \lambda_{\text{cross}} \mathcal{L}_{\text{cross}} \)，实验设置 \( \lambda_{\text{CGRL}}=0.01,\ \lambda_{\text{sym}}=\lambda_{\text{cross}}=1 \)。
   - 最终总损失：\( \mathcal{L} = \mathcal{L}_{\text{cls}} + \lambda_{\text{CGRL}} \mathcal{L}_{\text{CGRL}} \)，其中 \( \mathcal{L}_{\text{cls}} \) 是MIL模型的分类损失。

5. **推理阶段**：仅保留概念相关性剪枝和MIL模型，移除对比学习组件，实现轻量推理。

## 3. 实验设计

### 数据集
- **TCGA-BRCA**：871张WSI，两个亚型（723例IDC、148例ILC），5折交叉验证。
- **TCGA-NSCLC**：958张WSI，两个亚型（492例LUAD、466例LUSC），5折交叉验证。

### 基准方法（Baselines）
- 四种代表性MIL方法：ABMIL（注意力池化）、CLAM-SB（聚类约束）、TransMIL（Transformer）、DS-MIL（双流聚合）。
- 对比的采样策略：**Full**（全包）、**Random**（随机采样300张）、**Kmeans**（K均值聚类采样300张）、**CGRL**（本文方法，剪枝至300张）。

### 评估指标
- AUC（ROC曲线下面积）、ACC（准确率）、macro-F1（宏平均F1分数）。

## 4. 资源与算力
- **文中明确提及**：所有实验在**单块NVIDIA GeForce RTX 2080 Ti GPU**上运行。
- **未说明**：具体训练时长、总实验时间、内存消耗等细节。

## 5. 实验数量与充分性

### 实验数量
- **主结果（表1）**：4种MIL方法 × 4种策略（Full/Random/Kmeans/CGRL）× 2个数据集 = 32组实验，每组报告均值±标准差（5折交叉验证）。
- **消融实验（表2）**：在TCGA-BRCA上以ABMIL为基线，测试单个损失组件(\( \mathcal{L}_y, \mathcal{L}_{\text{sym}}, \mathcal{L}_{\text{cross}} \))及其组合，共7种配置。
- 总实验量约40组，覆盖了主要变量。

### 充分性与公平性
- **充分**：对比了多种采样策略（随机、K均值）而非仅全包基线，证明了概念引导剪枝的独特价值。
- **公平**：所有采样方法均使用相同数量（K=300）补丁，且MIL模型和超参数保持一致；使用了5折交叉验证报告均值和标准差，降低了随机性。
- **客观**：结果报告了AUC、ACC、F1三个互补指标，并分析了方法依赖性和数据集依赖性。
- **可提升之处**：未与其他概念引导方法（如CATE、PTCMIL等）直接比较，也未在更多样化的数据集上验证。

## 6. 论文的主要结论与发现
1. **剪枝普遍有益**：多数设定下，采样策略（Random/Kmeans/CGRL）优于全包基线，表明减少非信息性补丁能提升判别鲁棒性。
2. **CGRL在ACC和F1上表现突出**：尤其在TCGA-BRCA上，ABMIL+CGRL、CLAM-SB+CGRL取得最佳ACC/F1；在TCGA-NSCLC上，DS-MIL+CGRL相比全包提升明显（ACC 0.9266→0.9490）。
3. **效果与模型和数据集相关**：某些场景下随机或Kmeans采样仍具竞争力，但CGRL在类判别指标上展现最一致的增益。
4. **消融验证**：完整CGRL（三损失结合）优于任何子集组合，表明各组件互补；特别是\( \mathcal{L}_y + \mathcal{L}_{\text{cross}} \)显著提升AUC。
5. **类级语义概念是有效先验**：文本引导的概念原型为弱监督病理学习提供了一种廉价且实用的语义注入方式。

## 7. 优点
- **轻量插件式设计**：CGRL作为插件模块，可加装于任意MIL模型，仅需冻结的视觉和文本编码器，训练可学习投影头。
- **统一框架**：单一相似度矩阵同时服务于剪枝和对比学习，无需额外计算。
- **显式语义对齐**：通过概念原型将临床知识（疾病亚型文本）引入MIL，提升特征空间的可解释性。
- **效率提升**：将补丁数量从数万降低至300，大幅减少MIL聚合的计算量（如图1a所示FLOPs降低）。
- **推理时无额外开销**：对比学习仅在训练时使用，推理时仅需剪枝一步。
- **实验结果透明**：报告了不同方法组合的性能波动，并讨论了方法依赖性问题，展现了学术诚实性。

## 8. 不足与局限
- **对比不够充分**：未与同时期的概念引导工作（如CATE、PTCMIL等）进行直接比较。
- **数据集有限**：仅使用两个TCGA数据集（均为常见癌种），缺乏稀有癌种、多类分类或非TCGA数据的验证，泛化性未知。
- **概念原型依赖粗粒度提示**：提示模板简单（仅包含疾病名称），未利用更丰富的临床描述或病理特征，概念分辨率可能不足。
- **超参数敏感性未探索**：\( K_p, K_{pos}, K_{neg}, \lambda_{\text{CGRL}} \)等超参数仅在单一值下测试，未分析其对性能的影响。
- **偏差风险**：TCGA数据集具有特定采集规范和标注标准，模型可能在非标准WSI（不同染色、扫描仪）上性能下降。
- **方法依赖性未解释**：论文指出CGRL效果因MIL模型而异，但未深入分析原因（如注意力机制与概念剪枝的交互）。
- **计算资源细节缺失**：未报告训练时间、显存占用等实际部署成本，仅提及GPU型号。

（完）
