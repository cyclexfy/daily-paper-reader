---
title: A Hierarchical Framework for Graph Structure Learning in Histopathology Image Classification
title_zh: 组织病理学图像分类中图结构学习的层次化框架
authors: "Sudipta Paul, Amanda W. Lund, Bülent Yener"
date: 2026-07-28
pdf: "https://arxiv.org/pdf/2607.26153v1"
tags: ["query:cpath"]
score: 9.0
evidence: 用于全切片图像分类的图结构学习
tldr: "组织病理学图像中细胞和组织的空间结构对诊断至关重要，但现有图方法多依赖固定图结构。本文提出G2*-Net，一种两级图学习框架：先分块并构建细胞级图，再将每个块作为节点构建可学习的图像级图。通过双层优化学习图结构，并用DARTS近似加速。在三个数据集上验证了有效性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.26153v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 830, \"height\": 220, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.26153v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1347, \"height\": 542, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.26153v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 912, \"height\": 387, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.26153v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 910, \"height\": 485, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.26153v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1437, \"height\": 397, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.26153v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 568, \"height\": 126, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.26153v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 755, \"height\": 262, \"label\": \"Table\"}]"
motivation: 现有图方法多使用固定或启发式图结构，未能准确表示组织连接，限制分类性能。
method: "提出G2*-Net，先分块构建细胞级图，再通过双层优化学习图像级图结构，采用DARTS一阶近似估计超梯度。"
result: 在三个不同组织病理学数据集上，该方法均取得优于基线方法的分类性能。
conclusion: "G2*-Net通过可学习的图结构有效建模组织空间关系，提升了大规模病理图像分类的准确性。"
---

## 摘要
细胞和组织的空间排布为组织病理学图像提供了重要的诊断线索。尽管基于图的方法可以模拟这些关系，但许多方法依赖于固定或启发式的图结构，可能无法准确表示组织连接性。在这项工作中，我们提出了$G_2^*$-Net，一个优化的两层图学习框架，用于对大规模组织病理学图像（如全切片图像或大感兴趣区域）进行分类。其中，$G_2$表示两层层次图表示，上标$*$表示从所提出框架中学习到的优化图像级图结构。该方法首先将每个全切片图像或大感兴趣区域划分为图像块，在每个块内构建细胞级图以捕获局部组织结构，然后将每个块表示为可学习图像级图中的一个节点。$G_2^*$-Net将图像级图结构学习表述为一个二阶双层优化问题，将图连通性学习与分类器优化分离，同时通过验证驱动的反馈将它们耦合起来。为了使这一公式在计算上可行，我们采用了一种受DARTS启发的单步展开近似方法，以实现高效的超梯度估计。在三个不同的组织病理学数据集上的实验验证了所提出方法的有效性。

## Abstract
The spatial organization of cells and tissues provides important diagnostic cues in histopathology images. Although graph-based approaches can model these relationships, many rely on fixed or heuristic graph structures that may not accurately represent tissue connectivity. In this work, we propose $G_2^*$-Net, an optimized two-level graph learning framework for classifying large-scale histopathology images, such as whole-slide images (WSIs) or large regions of interest (ROIs). Here, $G_2$ denotes the two-level hierarchical graph representation, and the superscript $*$ indicates the optimized image-level graph structure learned from the proposed framework. The method first divides each WSI or large ROI into image patches, constructs cell-level graphs within each patch to capture local tissue architecture, and then represents each patch as a node in a learnable image-level graph. $G_2^*$-Net formulates image-level graph structure learning as a second-order bilevel optimization problem, separating graph connectivity learning from classifier optimization while coupling them through validation-driven feedback. To make this formulation computationally practical, we adopt a DARTS-inspired one-step unrolled approximation for efficient hypergradient estimation. Experimental validation on three distinct histopathology datasets demonstrates the effectiveness of our proposed method.

---

## 论文详细总结（自动生成）

好的，以下是根据您提供的论文内容生成的结构化、详细的中文总结。

### 0. 论文的源代码链接
无。论文中未提供任何公开的源代码链接或仓库地址。

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：在组织病理学图像分析中，细胞和组织的空间结构是重要的诊断线索。现有基于图的方法（如GCN）虽能建模空间关系，但多数依赖**固定的、启发式的图结构**（如基于欧氏距离或KNN），这些预设的连接规则可能引入归纳偏置，无法准确反映病理背景下复杂的、上下文相关的组织结构，限制了分类性能。
- **整体含义**：作者旨在提出一种能够**从数据中自适应学习图结构**的框架，使其能更好地捕获全切片图像（WSI）中全局的组织连通性，从而提升大规模病理图像的分类准确率。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：提出 **$G_2^*$-Net**，一个两层次的层次图学习框架。
    - **局部层面**：将WSI划分为重叠的图像块，在每个块内构建细胞级图（固定结构），提取丰富的结构特征。
    - **全局层面**：将每个图像块作为节点，构建一个**可学习**的图像级图。该图结构的学习被形式化为一个**二阶双层优化问题**，从而将图连通性学习与分类器优化分离，并引入验证集反馈。
- **关键技术细节**：
    1. **Patch级图构建（固定）**：使用StarDist2D检测细胞核，基于欧氏距离半径（rp=64像素）构建块内细胞图，并提取69维特征向量（包括细胞图特征、Voronoi图、Delaunay三角剖分、最小生成树、核最近邻等）。
    2. **图像级图学习（可学习）**：每个Patch的特征向量作为节点，通过一个MLP生成边分数矩阵S。使用**Gumbel-Sigmoid重新参数化**得到可微的、随机性的邻接矩阵，实现端到端训练。温度随训练退火。
    3. **二阶双层优化公式**：
        - **上层目标**：最小化验证损失 $L_{val}(\theta^*(\psi), \psi)$，优化图结构参数 $\psi$。
        - **下层目标**：最小化训练损失 $L_{train}(\theta, \psi)$，优化分类器参数 $\theta$。
        - $\theta^*(\psi)$ 代表依赖于图结构的分类器最优解。
    4. **DARTS式单步展开近似**：为避免昂贵的精确求解内层问题，采用单步更新近似：
        - 虚拟更新分类器参数：$\tilde{\theta}_t = \theta_{t-1} - \xi \nabla_\theta L_{train}(\theta_{t-1}, \psi_{t-1})$
        - 计算超梯度：$g_\psi = d_\psi - \rho_t \xi h_\psi$，其中 $d_\psi$ 是直接梯度，$h_\psi$ 是混合Hessian-向量积（通过有限差分近似），$\rho_t$ 是可靠性系数（基于余弦相似度自适应调节二阶矫正项的权重）。
    5. **分类器架构**：使用GCN（跳跃知识（Jumping Knowledge）风格读出 + MLP头部）对可学习邻接矩阵进行处理，输出图像级预测。

### 3. 实验设计：数据集、基准与对比方法
- **数据集**：
    - **Dataset I (Extended CRC)**：来自华威大学，300张H&E染色的结直肠癌组织学图像，三分类（正常/低级别/高级别）。
    - **Dataset II (Colon Cancer)**：来自浙江大学，717个ROI（355癌症/362正常），排除细胞过少图像后保留669张。
    - **Dataset III (Melanoma)**：来自纽约大学朗格尼健康中心，153个WSI，包含1072个ROI（淋巴细胞丰富/肿瘤丰富）。
- **对比方法**：
    - **CNN基线**：ResNet50、MobileNet、InceptionV3、Xception、ViT（仅Dataset I）。
    - **GCN基线**：CGC-Net、HAT-Net、C2P-GCN。
- **评估方式**：采用交叉验证（Dataset I：3折；Dataset II：5折；Dataset III：5折，按患者划分）。指标为平均准确率±标准差。

### 4. 资源与算力
- **未明确说明**：论文中详细描述了模型参数量（约0.86M可训练参数）和内存占用（邻接矩阵等因素在示例中约为MB级别），但**没有提及**所使用的GPU型号、数量以及具体训练时长。仅说明使用PyTorch实现，训练至验证集收敛。

### 5. 实验数量与充分性
- **实验数量**：
    - **主实验**：在3个不同组织病理学数据集上与8-9种基线方法对比。
    - **消融实验**（均在Dataset I上进行）：
        - 图像级图结构消融：比较固定（余弦相似度）邻接矩阵 vs 学习到的邻接矩阵。
        - Patch级特征组消融：分别测试细胞图、Voronoi、Delaunay、MST、NN等不同特征组对性能的影响。
- **充分性评估**：
    - **优点**：数据集多样（结直肠癌、结肠癌、黑色素瘤），覆盖不同组织类型和分类任务；与多个强基线（CNN + GCN）对比；消融实验验证了关键组件（学习图结构、特征组合）的贡献。
    - **不足**：仅在一个数据集（Dataset I）上进行了全面的消融实验，未在另外两个数据集上验证图结构或特征组的泛化性。未进行超参数敏感性分析（如对温度退火策略、有限差分半径r、可靠性系数阈值的鲁棒性实验）。实验设置（如交叉验证方案）合理但未提供不确定性量化（如显著性子检验）。

### 6. 论文的主要结论与发现
- $G_2^*$-Net在三个数据集上均取得了**具有竞争力或最佳的性能**：
    - Dataset I：**96.33%**（优于所有基线，比最佳GCN HAT-Net高1.00%）。
    - Dataset II：**97.03%**（虽略低于CGC-Net的97.68%，但仍在同一水平线上）。
    - Dataset III：**97.29%**（优于所有基线）。
- **消融实验证实**：可学习的图像级图结构优于固定余弦相似度的图结构；完整特征集优于任何单一特征组，表明不同特征组捕获了互补的组织结构信息。
- **计算可行性**：通过DARTS式近似和有限差分，将二阶双层优化的复杂度降低到实际可接受范围（内存和参数量适中）。

### 7. 优点：方法或实验设计上的亮点
- **方法论创新**：将图结构学习明确建模为**二阶双层优化**，与常见的单级联合优化相比，能显式地利用验证集反馈来引导图结构演化，并捕获分类器参数对于图结构的依赖性（超梯度中的二阶项）。这是与[18]（一阶近似）的关键区别。
- **实用近似策略**：采用DARTS式单步展开，结合有限差分与可靠性系数，使得二阶超梯度的计算在实践中可行且稳定。
- **层次化表示合理**：底层固定细胞图保留了局部组织微观形态；上层可学习图实现了全局上下文的自适应建模，设计直观且有效。
- **实验覆盖全面**：使用三个来自不同机构、不同癌种的数据集进行评价，增强了结论的泛化性。

### 8. 不足与局限：实验覆盖、偏差风险、应用限制
- **实验局限**：
    - **计算资源未报告**：缺乏对训练时间、GPU需求的量化，难以评估方法的实际部署成本。
    - **消融实验不完全**：只在Dataset I上进行了消融，且未对超参数（如温度退火速率、有限差分半径、MLP结构等）进行系统敏感性分析，可能存在过拟合并未充分暴露。
    - **统计显著性缺失**：未给出置信区间或显著性检验结果，无法判断性能提升是否具有统计意义。
    - **缺少跨数据集泛化测试**：未在跨数据集（如用Dataset I训练、Dataset II测试）评估方法的迁移能力。
- **方法局限**：
    - **图构建依赖核检测**：Patch级细胞图完全依赖于StarDist2D的核检测质量，检测误差会向下游传播。
    - **固定Patch级图**：当前框架不学习局部细胞图结构（保持固定），限制了局部拓扑的自适应性，作者将其列为未来工作。
    - **二次复杂度**：图像级图的边评分是$O(n_i^2)$，对于包含大量Patch的图像（如Dataset II中可达1369个Patch）可能成为瓶颈，尽管作者认为内存可行，但未讨论时间延展。
    - **未解决WSI全图分类的典型挑战**：如如何处理WSI中的背景区域、如何整合多分辨率信息等。

（完）
