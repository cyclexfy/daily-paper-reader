---
title: "PathSelect: Sequential Token Selection for Whole Slide Pathology"
title_zh: PathSelect：全切片病理学的顺序令牌选择
authors: "Jingzhi Chen, Landi He, Zehong Chen, Peihang Wu, Lijian Xu"
date: 2026-07-26
pdf: "https://arxiv.org/pdf/2607.23631v1"
tags: ["query:cpath"]
score: 9.0
evidence: 解决病理全切片图像的令牌选择问题用于视觉语言模型
tldr: "整张幻灯片图像（WSI）因极长序列给视觉语言模型带来计算瓶颈，现有采样或固定剪枝易丢失弱但关键的病理信号。本文提出PathSelect，将WSI token剪枝重构为可学习的顺序选择过程，通过可微分Soft Top-K和方差保持噪声门控实现端到端训练，推理时仅用确定性Hard Top-K加速。在SlideBench上仅用平均44.86个token达到74.00%准确率，压缩约36.6倍且优于采样方法，证明动态选择策略的有效性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.23631v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1709, \"height\": 589, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23631v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1645, \"height\": 526, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23631v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1623, \"height\": 1209, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23631v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1592, \"height\": 1551, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23631v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1685, \"height\": 645, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.23631v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1711, \"height\": 777, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.23631v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 831, \"height\": 450, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.23631v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1710, \"height\": 514, \"label\": \"Table\"}]"
motivation: 解决WSI中因极长序列导致计算瓶颈，并避免现有采样或固定剪枝丢失弱但关键的诊断信号。
method: 提出PathSelect，使用方差保持噪声门控和可微分Soft Top-K算子训练剪枝策略，推理时采用确定性Hard Top-K加速。
result: "在SlideBench（TCGA）上平均仅用44.86个token达到74.00%准确率，压缩约36.6倍并优于所有采样方法。"
conclusion: WSI token的顺序选择可有效替代静态采样，显著提升计算效率且不损失诊断性能。
---

## 摘要
十亿像素级别的全切片图像(WSIs)由于极长的序列长度，给视觉语言模型(VLMs)带来了根本性的计算瓶颈。现有方法主要依赖空间采样或无训练剪枝，这些方法可能会稀释微弱但包含信息的信号，由于病理线索在空间上分散的特性，导致关键诊断证据的丢失。我们将WSI令牌剪枝重新定义为顺序选择过程，使模型能够自主学习最优路由策略，而非依赖静态启发式方法。我们在此提出一种解耦路由框架，作为主动插件集成到完全预训练的SlideChat基础模型中，同时保持切片编码器和大型语言模型冻结。为了在训练期间为非可微的剪枝操作提供连续梯度，我们引入了PathSelect。PathSelect采用方差保持噪声门控，通过可微的Soft Top-K算子调节每个patch的信息流，并辅以对角注意力去噪器(Denoiser)，在无语义泄露的情况下恢复扰动表示。在推理时，PathSelect模块完全分离。仅依靠训练好的Scorer，一个确定性的Hard Top-K算子执行自适应、数据依赖的轨迹终止，以极低的顺序令牌选择延迟显著加速下游生成处理。在最大约束K=128下，基于平均仅44.86个令牌的经验驱动，我们的框架在SlideBench(TCGA)上实现了74.00%的总体准确率，相对于未压缩基线平均实现了约36.6倍的空间令牌缩减，同时持续优于基于采样的对应方法。

## Abstract
Gigapixel Whole-Slide Images (WSIs) present a fundamental computational bottleneck for vision-language models (VLMs) due to extreme sequence lengths. Existing approaches predominantly rely on spatial sampling or training-free pruning, which risk diluting weak but informative signals, leading to the loss of critical diagnostic evidence due to the spatially diffuse nature of pathological cues. We reformulate WSI token pruning as a sequential selection process, enabling the model to autonomously learn an optimal routing strategy rather than relying on static heuristics. We herein propose a decoupled routing framework integrated as an active plugin into the fully pre-trained SlideChat base model, leaving both the slide encoder and large language model frozen. To provide continuous gradients for the non-differentiable pruning operation during training, we introduce PathSelect. PathSelect employs a variance-preserving noise gate to modulate each patch's information flow via a differentiable Soft Top-K operator, paired with a diagonal-attention Denoiser that recovers the perturbed representations without semantic leakage. At inference, the PathSelect module is entirely detached. Relying solely on the trained Scorer, a deterministic Hard Top-K operator executes adaptive, data-dependent trajectory termination, significantly accelerating downstream generative processing with exceptionally low sequential token selection latency. Driven by an empirical average of only 44.86 tokens under a maximum constraint of K = 128, our framework achieves 74.00% overall accuracy on SlideBench (TCGA), representing an approximate 36.6x spatial token reduction relative to the uncompressed baseline average while consistently outperforming sampling-based counterparts.

---

## 论文详细总结（自动生成）

### 0. 源代码链接
无（论文中未提供公开源码链接）。

---

### 1. 核心问题与整体含义（研究动机和背景）
- **问题**：十亿像素级全切片图像（WSI）在视觉-语言模型（VLM）中产生极长的序列长度（通常超10⁵个patch），导致LLM预填充阶段的二次自注意力复杂度和显存开销变得不可承受。
- **现有不足**：主流方法采用空间采样或训练‑无关的剪枝（如根据注意力幅度过滤），这些方法将剪枝视为单纯去除背景噪声，容易稀释微弱但关键的诊断信号（如＜1%面积的微转移灶）。静态剪枝策略无法捕获病理线索的空间扩散性和动态边际效用。
- **本文目标**：将WSI token剪枝重构为**可学习的数据依赖顺序选择过程**，使模型能够自主学习最优路由策略，而非依赖静态启发式。

---

### 2. 方法论：核心思想、关键技术细节
- **核心思想**：在预训练SlideChat（冻结图像编码器与LLM）中插入PathSelect插件，通过**解耦训练‑推理通路**实现可微分顺序剪枝。训练时使用连续化的Soft Top‑K与噪声门控产生可传导梯度；推理时完全移除噪声门控与去噪器，仅用确定性Hard Top‑K高效执行。

- **关键技术细节**：
  1. **条件指针解码器**：将视觉token与文本指令联合编码为记忆矩阵M（含可学习停止token s_stop）。采用自回归指针解码，每一步输出概率分布p_t，通过argmax选择token，并用直通估计器（STE）保持梯度。动态更新掩码防止重复选择。
  2. **可微选择公式**：利用存活概率A_t和几何深度衰减β，为每个视觉token i累积加权软分数s_i，再通过可微分的Soft Top‑K算子极化得到α_i ∈ [0,1]。
  3. **方差保持（VP）噪声门控**：混合原始特征与各向同性高斯噪声：`˜x^i_v = √α_i x^i_v + √(1-α_i) ϵ_i`，使低α的token被噪声淹没，而高α的token保持清晰。
  4. **对角注意力去噪器（Denoiser）**：使用对角注意力掩码（禁止跨token聚合）恢复扰动后的表示，防止未选中的背景token通过注意力学到诊断信息（语义泄露）。  
  5. **长度惩罚正则化**：在语言建模损失基础上加入`L = L_LM + λ·K/N`，鼓励模型自动压缩token数，避免固定预算。

- **推理阶段**：噪声门控和去噪器被移除，仅有训练好的Scorer（指针解码器）执行确定性Hard Top‑K选择，并将选中的少量token送入LLM，极大降低预填充开销。

---

### 3. 实验设计
- **数据集与Benchmark**：
  - 主要训练与评估：**SlideBench（TCGA）**，报告Overall Accuracy及Microscopy、Diagnosis、Clinical三个子集。
  - 零样本泛化：**SlideBench（BCNB）** 和 **WSI-VQA***（外部VQA数据集）。
- **对比方法**：
  - 理论上界（未压缩）：SlideChat（平均1641.87 token，最大4096）。
  - 通用VLM：GPT‑4o、LLaVA‑Med、Quilt‑LLaVA、MedDr。
  - 随机基线。
  - 计算量匹配（FLOPs约1.7T）的采样/压缩模型。

---

### 4. 资源与算力
- **训练硬件**：单张 NVIDIA A6000 GPU（48 GB显存）。
- **训练协议**：轻量级单阶段训练，模型参数仅更新PathSelect模块和线性投影桥P，图像编码器与LLM完全冻结。
- **未明确说明**：具体训练轮数、总训练时长未在文中提供。

---

### 5. 实验数量与充分性
- **主实验结果**：表1展示了在SlideBench（TCGA）上的总体及子集准确率，并在两个零样本数据集上报告。对比了7种方法。
- **消融实验**：
  - **表2(a)**：去噪器注意力机制（全局 vs 对角），验证语义泄露防御。
  - **表2(b)**：Scorer分数聚合策略（最后一步、均匀求和、仅存活概率、完整公式），验证累计概率与几何衰减的必要性。
  - **表3**：长度惩罚系数λ（0~6）和最大预算K（64/128/256）的扫描，包括多次独立运行（2或3次）的均值和标准差。
- **可视化分析**：图3（token效率对比）、图4（不同token数下的精度稳定性）、图5（UMAP特征空间分布，展示选中的token位于拓扑边缘）。
- **评价**：实验覆盖主要基准、多个消融维度、超参数搜索，且报告了方差，设计较为充分、客观。但零样本仅两个外部数据集，泛化性验证仍有限。

---

### 6. 主要结论与发现
- 在K=128最大约束下，PathSelect平均仅保留**44.86个token**（约36.6倍压缩），在SlideBench（TCGA）上达到**74.00%** 总体准确率，优于所有对比的压缩方法（如MedDr +6.3%）。
- 在Clinical子集上甚至超过未压缩的SlideChat（+3.06%），证明选择性压缩可去除噪声、提升复杂临床推理。
- 推理延迟极低：噪声门控与去噪器被移除后，仅需轻量级Scorer做顺序选择，下游LLM处理仅需极短上下文。
- 自适应动态终止使精度在不同压缩率下保持稳定（图4），验证了指针策略不丢失关键诊断信息。
- 消融证实**对角注意力去噪器**和**完整存活‑衰减概率聚合**是性能关键。

---

### 7. 优点
- **方法创新**：首次将WSI token剪枝重构为**可端到端训练的连续顺序选择**，通过VP噪声门控+Soft Top‑K巧妙绕过非可微问题，无需辅助损失或外部标注。
- **解耦训练‑推理**：推理时零额外开销（移除噪声门控与去噪器），实现极低延迟。
- **高效性**：单GPU、单阶段训练，冻结预训练主干的参数，训练成本低。
- **性能优异**：在高度压缩下（平均44.86 token）达到甚至超越未压缩模型的部分指标，展现出选择性注意力带来的去噪增益。
- **理论解释**：通过UMAP可视化验证选中token位于特征空间边际，说明策略捕获了互补的稀有诊断结构。

---

### 8. 不足与局限
- **预训练依赖**：框架嵌入于SlideChat，泛化至其他VLM或独立使用的效果未探索。
- **算力报告不完整**：虽使用单A6000，但未报告训练迭代次数、耗时，难以与其他方法进行算力公平对比。
- **零样本验证有限**：仅两个外部数据集（BCNB、WSI-VQA*），且它们与TCGA同属病理领域，是否能在其他器官系统或染色方式上保持优势尚需验证。
- **最大预算K固定为128**：虽然自适应平均远低于128，但硬上限可能限制某些复杂病例的保留能力；文中未展示无上限动态终止（如K=∞）的实验。
- **序列选择延迟分析不足**：文中只说“极低延迟”，但未给出具体时间对比（如与直接全输入LLM推理对比的wall‑clock时间）。
- **诊断类别的偏差风险**：虽然Clinical子集提升，但Microscopy子集略低于SlideChat（0.26%），可能存在某些低层形态特征依赖冗余token的问题。

---

（完）
