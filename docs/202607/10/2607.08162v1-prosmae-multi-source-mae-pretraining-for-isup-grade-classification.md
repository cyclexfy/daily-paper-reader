---
title: "ProsMAE: Multi-Source MAE Pretraining for ISUP Grade Classification"
title_zh: ProsMAE：用于ISUP分级的多源MAE预训练
authors: "Anna Jung, Kyeonghun Kim, Youngung Han, Eunseob Choi, Jiwon Yang, Ken Ying-Kai Liao, Hyuk-Jae Lee, Nam-Joon Kim"
date: 2026-07-09
pdf: "https://arxiv.org/pdf/2607.08162v1"
tags: ["query:cpath"]
score: 9.0
evidence: 多源MAE预训练用于基于WSI的前列腺癌分级
tldr: 针对全切片图像尺度大、染色差异、扫描差异及标注有限等问题，提出多源掩码自编码器ProsMAE，用PANDA、CAMELYON17和BRACS三个数据集预训练，使编码器学习多样组织形态和采集条件。迁移到ISUP分级分类任务时，冻结编码器加线性分类头，在PANDA数据集上验证QWK高于基线。该方法利用多源数据增强了特征鲁棒性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.08162v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1735, \"height\": 814, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08162v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1371, \"height\": 485, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.08162v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 843, \"height\": 283, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08162v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 899, \"height\": 796, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08162v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 885, \"height\": 244, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08162v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 532, \"height\": 335, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08162v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 819, \"height\": 282, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08162v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 859, \"height\": 224, \"label\": \"Table\"}]"
motivation: WSI尺度大、染色差异和标注不足，单一数据源预训练对下游分级任务鲁棒性有限。
method: 使用三个公开病理数据集进行多源MAE预训练，下游用冻结编码器加线性分类头做ISUP分级。
result: 在PANDA数据集上，ProsMAE冻结线性探针QWK高于vanilla MAE基线。
conclusion: 多源MAE预训练能提升病理图像表示学习鲁棒性，重复分割评估尚需验证。
---

## 摘要
全切片图像（WSI）为计算病理学提供了丰富的诊断信息，但其千兆像素规模、染色差异、扫描仪差异、组织伪影以及有限的专家注释使得稳健模型训练具有挑战性。本文提出了一种名为ProsMAE的多源掩码自编码器（MAE）框架，用于组织病理学表示学习。来自前列腺癌分级评估（PANDA）、2017年淋巴结转移癌挑战赛（CAMELYON17）和乳腺癌亚型分类（BRACS）的切片被用于ProsMAE预训练，以使编码器接触多样化的组织形态和采集条件。通过ProsCLS，使用冻结编码器和线性分类头将学习到的编码器迁移到国际泌尿病理学会（ISUP）分级分类中。在评估的不相交PANDA划分下，ProsMAE实现了比普通MAE冻结线性探测基线更高的平均验证二次加权卡帕（QWK）。为了进一步验证跨划分组成的稳健性，仍需进行重复划分评估。

## Abstract
Whole slide images (WSIs) provide rich diagnostic information for computational pathology, but their gigapixel scale, stain variation, scanner differences, tissue artifacts, and limited expert annotation make robust model training challenging. This paper presents a multi-source Masked Autoencoder (MAE) framework, named ProsMAE, for histopathology representation learning. Tiles from Prostate cANcer graDe Assessment (PANDA), CAncer MEtastases in LYmph nOdes challeNge 2017 (CAMELYON17), and BReAst Carcinoma Subtyping (BRACS) are used for ProsMAE pretraining to expose the encoder to diverse tissue morphology and acquisition conditions. The learned encoder is transferred for International Society of Urological Pathology (ISUP) grade classification through ProsCLS, using a frozen encoder and a linear classification head. ProsMAE achieved a higher mean validation quadratic weighted kappa (QWK) than the vanilla MAE frozen linear-probe baseline under the evaluated disjoint PANDA split. Repeated-split evaluation remains necessary to further establish robustness across split compositions.

---

## 论文详细总结（自动生成）

## ProsMAE: Multi-Source MAE Pretraining for ISUP Grade Classification – 详细中文总结

### 0. 论文的源代码链接
无（原文未提供公开代码仓库链接）。

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：全切片图像（WSI）用于计算病理学分析时面临巨大挑战：图像规模达千兆像素、染色方案差异、扫描仪差异、组织伪影以及专家标注严重不足。这些问题导致监督学习难以获得鲁棒的模型。
- **研究动机**：现有自监督学习方法（如MAE）已能从未标注的病理切片中学习可迁移表示，但单一数据源的预训练模型在面对跨领域差异（如不同器官、染色条件）时鲁棒性不足。
- **整体含义**：本文旨在通过多源MAE预训练，让编码器学习到对多样化组织形态和采集条件具有不变性的特征，从而提升下游ISUP分级任务的性能，降低对大型标注数据集的依赖。

### 2. 论文提出的方法论
- **核心思想**：提出ProsMAE（多源MAE预训练框架）和ProsCLS（下游线性探测分类器）两阶段方法。先利用来自前列腺癌、乳腺癌、淋巴结转移癌三个不同病理数据集的切片块进行MAE掩码重建预训练，然后冻结编码器，仅训练一个线性分类头进行ISUP分级。
- **关键技术细节**：
  - **MAE预训练**：使用Vision Transformer (ViT-B/16)作为骨干，掩码比例设为0.75。输入切片块尺寸缩放至224×224，将WSI的1024×1024区域降采样后切块。无染色归一化，以保留原始染色差异。
  - **多源数据混合**：PANDA（前列腺癌）、CAMELYON17（淋巴结转移癌）、BRACS（乳腺癌）三数据集同时用于预训练，使编码器暴露于不同组织形态和采集条件。
  - **下游分类（ProsCLS）**：对每张WSI均匀采样N个切片块（默认100个），冻结预训练编码器提取每个块的特征，然后通过均值池化聚合为全切片级特征向量，最后接线性分类头（6类：良性+ISUP 1-5级），使用交叉熵损失训练。
  - **噪声注入消融**：在MAE预训练输入中添加高斯噪声（σ∈{0.02,0.05,0.10,0.20}），重建目标为原始干净图像，探索噪声鲁棒性；但最终的主设置不添加噪声。
- **公式/算法流程简述**：
  1. 输入切片块x，若使用噪声则生成˜x = N_σ(x)。
  2. 随机掩盖75%的patch，编码器仅处理可见patch，解码器重建被掩码区域的像素值。
  3. 重建损失为掩码patch的MSE。
  4. 预训练后舍弃解码器，用冻结编码器提取切片块特征z_i = f_θ(x_i)。
  5. 所有块特征均值池化得到z_slide = (1/N) Σ z_i。
  6. 线性分类头输出预测ˆy = Softmax(W z_slide + b)，用交叉熵损失训练。

### 3. 实验设计
- **数据集**：
  - 预训练数据集：PANDA（前列腺癌，241张WSI用于预训练）、CAMELYON17（淋巴结转移癌）、BRACS（乳腺癌亚型）。总计3个公开病理数据集。
  - 下游评估数据集：PANDA（82张训练，80张验证），采用严格的不相交划分（确保预训练和下游数据无重叠）。
- **基准（Benchmark）**：下游ISUP分级任务（6类：良性+ISUP Grade 1-5）。
- **对比方法**：
  - 重建性能对比：Autoencoder (AE)、Variational Autoencoder (VAE)、单源MAE、ProsMAE。
  - 下游分类对比：Vanilla MAE（单源PANDA预训练） vs. ProsMAE（多源预训练）。
  - 消融实验：不同掩码比例（0.25/0.50/0.75）、不同噪声水平、不同切片采样数量（50/100/150块/张）、不同PANDA划分敏感度。
- **评估指标**：二次加权卡帕（QWK，主指标）、准确率、宏F1分数，以及重建质量用LPIPS、SSIM、PSNR。

### 4. 资源与算力
- 文中提到：预训练总GPU时间约为10-11小时（比AE/VAE快，与普通MAE相当），但未明确说明GPU型号、数量。仅提及使用AdamW优化器，batch size 64，学习率5e-5，预训练5000步（最多20 epoch）。

### 5. 实验数量与充分性
- **实验数量**：约5组主要实验（重建性能对比、下游分类主结果、掩码比例消融、噪声注入消融、划分敏感度消融、切片采样敏感度消融），每组包含多个种子或配置。总计约10余个表格结果。
- **充分性与客观性**：
  - 优点：进行了多维度消融（数据源、掩码比例、噪声、采样数、划分敏感度），且对基线（Vanilla MAE）做了跨分割重复，揭示了基线的不稳定性。
  - 不足：下游仅用单一的PANDA数据集评估，缺乏外部独立验证集；验证集规模较小（80张）；种子数量有限（Vanilla MAE 11个种子？ProsMAE 10个种子？），且种子范围不完全配对，未做正式统计显著性检验。实验设计尚不足以证明跨数据集的泛化性。

### 6. 论文的主要结论与发现
1. **多源MAE预训练优于单源**：在PANDA不相交划分下，ProsMAE（多源）的平均验证QWK（0.4736）比Vanilla MAE（0.4084）提升约0.0652。
2. **掩码比例0.75最佳**：在ISUP分级任务中，QWK最优，尽管准确率略低。
3. **噪声注入未提升性能**：高噪声（σ=0.20）与无噪声性能相近，低噪声反而下降，因此主设置未使用噪声。
4. **基线对划分敏感**：Vanilla MAE在不同PANDA划分下QWK波动大（均值0.3572，标准差0.1088），说明当前结果依赖特定划分，需谨慎推广。
5. **切片采样数量影响不大**：50/100/150块/张的QWK均值接近，100块标准差最低。

### 7. 优点
- **方法简洁高效**：仅用5000步预训练、冻结编码器+线性探测，计算成本低，易于部署。
- **多源数据策略**：整合三个不同器官/疾病的病理数据集进行预训练，有效增强了特征对染色、扫描等域变化的鲁棒性。
- **全面的消融实验**：对掩码比例、噪声、采样数、划分敏感度均进行了系统分析，揭示基线波动性，使结论更可靠。
- **关注临床相关指标**：采用QWK（序数加权）而非简单准确率，更符合ISUP分级任务的特点。

### 8. 不足与局限
- **缺乏外部验证**：下游仅在一个数据集（PANDA）上评估，且只使用一个标准划分，未在其他独立前列腺癌队列上验证，泛化性存疑。
- **样本量小**：验证集仅80张WSI，种子数量有限，统计稳健性不足。作者也承认未做正式显著性检验。
- **未与最新病理基础模型对比**：没有与当前SOTA的病理自监督模型（如UNI、CONCH等）比较，仅与Vanilla MAE基线对比，说服力有限。
- **未使用更强下游分类器**：仅用线性探测，未尝试微调编码器或更复杂的聚合方法（如注意力MIL），可能低估了预训练表示的潜力。
- **噪声注入的视角有限**：仅用了高斯噪声，未探索其他数据增强（如颜色抖动、模糊等），噪声鲁棒性探究深度不足。
- **未提供代码**：实验可复现性受影响。

（完）
