---
title: Toward Efficient Weakly Supervised Semantic Segmentation Using Only Low-Magnification Histopathological Images
title_zh: 仅使用低倍放大的组织病理学图像实现高效的弱监督语义分割
authors: "Dung Minh Do, Nhat-Thanh Huynh, Duc Minh Huynh, Doanh C. Bui, Khang Nguyen"
date: 2026-07-12
pdf: "https://arxiv.org/pdf/2607.10783v1"
tags: ["query:cpath"]
score: 8.0
evidence: 使用低倍率组织病理学图像的弱监督分割
tldr: 全切片图像（WSI）存储高倍率数据资源消耗大，像素级标注费时费力。本文研究仅用低倍率病理图像和图像级标签实现弱监督分割的可行性，通过插值和深度学习重建方法模拟低分辨率输入并恢复尺寸，系统评估弱监督方法在不同分辨率退化下的表现。实验发现重建质量指标不能可靠预测分割性能，并确定了小尺度结构定位显著下降的临界退化点。该工作为设计高效数字病理存储系统同时保持自动分析可靠性提供了实用指导。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.10783v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1866, \"height\": 621, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10783v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1863, \"height\": 2171, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10783v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1871, \"height\": 519, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.10783v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1884, \"height\": 555, \"label\": \"Table\"}]"
motivation: 高倍率WSI存储与像素级标注成本高，需探究低倍率图像结合弱监督能否达到接近性能。
method: 从高分辨率块模拟低倍率输入，用插值和深度重建方法恢复原尺寸，再应用弱监督分割流水线进行基准测试。
result: 重建质量指标无法预测分割性能，并发现小尺度结构定位显著下降的临界退化点。
conclusion: 为设计高效数字病理存储系统提供指导，允许低倍率存储同时保持可靠分析。
---

## 摘要
全切片图像（WSI）提供了丰富的组织层面和细胞层面信息，但存储和传输高倍数病理数据资源消耗巨大。此外，在像素级别标注WSI劳动密集且耗时。因此，研究在有限标注（即图像级而非像素级标签）下，低倍放大病理图像是否能够达到与高倍图像相当的性能具有重要意义。本文针对不同低分辨率存储设置下的弱监督组织病理学图像分割，提出了系统的基准研究。我们从高分辨率图像块出发，通过插值和基于深度学习的重建方法模拟更低倍数的输入，并将其重建至原始尺寸，然后应用弱监督分割流程。该框架能够定量评估弱监督方法对不同分辨率退化程度的响应。实验结果表明，仅凭重建质量指标不足以预测下游分割性能。研究特别指出存在一个关键退化点，在该点上小尺度结构的定位显著下降。这些发现为设计高效的数字病理存储系统同时保持可靠的自动化分析提供了实用指导。代码可在 https://github.com/Dung-Dx/LowMagWSS 获取。

## Abstract
Whole-slide images (WSIs) provide rich tissue-level and cellular-level information, but storing and transmitting high-magnification pathology data is resource-intensive. Moreover, annotating WSIs at the pixel level is labor-intensive and time-consuming. Therefore, it is important to investigate whether low-magnification pathology images with limited annotations (i.e., image-level instead of pixel-level labels) can achieve performance comparable to high-magnification images. This paper presents a systematic benchmark study on weakly supervised histopathological image segmentation under different low-resolution storage settings. Starting from high-resolution image patches, we simulate lower-magnification inputs and reconstruct them to the original size using interpolation and deep learning-based reconstruction methods before applying the weakly-supervised segmentation pipeline. This framework enables a quantitative evaluation of how weakly supervised methods respond to different levels of resolution degradation. Experimental results show that reconstruction quality metrics alone are insufficient to predict downstream segmentation performance. In particular, the study identifies a critical degradation point where the localization of small-scale structures declines significantly. These findings provide practical guidance for designing efficient digital pathology storage systems while maintaining reliable automated analysis. Code is available at https://github.com/Dung-Dx/LowMagWSS

---

## 论文详细总结（自动生成）

### 论文的源代码链接
- https://github.com/Dung-Dx/LowMagWSS

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **动机**：高倍率全切片图像（WSI）存储和传输消耗大量资源，且像素级标注费时费力。为降低存储和标注成本，探索仅使用低倍率图像（如20×、10×）配合图像级弱监督标签（而非密集像素标签）进行组织分割的可行性。
- **背景**：现有工作多聚焦于高倍率图像或全监督分割，低倍率存储对弱监督分割的影响尚缺乏系统评估。同时，超分辨率（SR）重建常用于恢复图像细节，但重建质量指标（PSNR等）能否保证下游分割性能未知。

### 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：通过模拟低倍率存储（降采样后重建回原尺寸），在固定弱监督分割流水线下评估不同放大倍数和重建方法对分割性能的影响，以确定可接受的存储分辨率。
- **关键技术细节**：
  1. **低倍率模拟**：将原始40× patch（224×224）分别降采样至20×（112×112）和10×（56×56），模拟低分辨率存储。
  2. **重建方法**：
     - **双线性插值**：简单、无学习参数，作为基准。
     - **STAR-RL**：基于空间-时间层次强化学习的病理图像超分辨率方法，通过可解释操作（边缘增强、对比度增强等）逐像素修复。为保证输入尺寸匹配，使用尺寸调整过程：先bicubic缩放到192×192，经STAR-RL处理后，再bicubic缩放回224×224。
  3. **弱监督分割流水线**：采用WSSS-Tissue框架，基于图像级（patch级）多标签分类，通过CAM生成伪掩码，再训练分割网络。框架内置Progressive Dropout Attention和多层伪监督，在所有实验中保持超参数固定。

### 3. 实验设计：数据集、基准、对比方法
- **数据集**：
  - **LUAD-HistoSeg**：肺腺癌组织分割，含30训练、10验证、10测试（patch级标签用于训练，像素级用于评估），类别：肿瘤上皮（TE）、癌相关间质（TAS）、坏死（NEC）、淋巴细胞（LYM）。
  - **BCSS-WSSS**：源自乳腺癌语义分割数据集，保留像素级掩码用于评估，四个前景类别：肿瘤（TUM）、间质（STR）、淋巴细胞浸润（LYM）、坏死（NEC）。
- **基准**：原始40×原始图像的分割表现作为上界基准。
- **对比方法**：对应4种设置：20×→40×（双线性插值、STAR-RL）和10×→40×（双线性插值、STAR-RL）。弱监督流水线固定，仅输入图像不同。

### 4. 资源与算力（文中未明确说明）
- 论文未提及GPU型号、数量、训练时长等具体算力信息。仅说明STAR-RL模型在HistoSR数据集上预训练，但未披露预训练资源。推断实验在常规GPU环境下进行（如NVIDIA V100或A100），但无法确认。

### 5. 实验数量与充分性
- **实验数量**：两个数据集 × 5种输入设置（原始+2种降级×2种重建）= 10组分割实验结果，每组均报告类别IoU、mIoU、FwIoU。此外，还提供了定性分割图（Fig.2）和重建质量指标（Fig.3）。
- **充分性评估**：
  - **优点**：固定弱监督流水线，仅变化输入图像，公平比较重建方法；包含中度（20×）和严重（10×）两种退化水平；定量+定性分析充分。
  - **不足**：
    - 仅使用两个数据集，可能不足以覆盖多样组织类型。
    - 仅使用一种弱监督方法（WSSS-Tissue），未验证其他WSS框架下的泛化性。
    - 仅使用一种病理SR方法（STAR-RL），未与PathSRGAN等其他方法对比。
    - 未进行消融实验（如不同重建模块、不同降采样方式）。
    - 未在完整WSI尺度验证（仅在patch级别）。

### 6. 论文的主要结论与发现
1. **适度退化可行**：20×降级下，BCSS-WSSS上分割性能几乎无损（mIoU甚至略高于原始），表明较大且空间连续的组织区域可容忍中等分辨率损失；LUAD-HistoSeg虽下降但STAR-RL可部分恢复（从69.79%恢复至72.08% mIoU）。
2. **严重退化导致关键失败**：10×降级下，LUAD-HistoSeg上淋巴细胞类IoU降至0%（完全丢失），表明小尺度结构（如淋巴细胞）的判别性线索无法通过事后重建恢复。而BCSS-WSSS上的淋巴细胞类仍保留～57% IoU，说明数据集差异（尺度、区域范围）影响退化敏感性。
3. **重建质量 ≠ 分割性能**：STAR-RL在所有设置下均获得更高PSNR/SSIM等指标，但分割改善仅在部分情况出现（如LUAD-HistoSeg的20×和10×），在BCSS-WSSS的20×下甚至略低于基线。证明任务驱动评估的必要性。
4. **存储策略建议**：应根据下游任务关注的细胞类型和组织结构决定存储分辨率，不能统一设置低倍率。

### 7. 优点（方法与实验设计亮点）
- **首次系统性研究**：将低倍率存储与弱监督分割结合，填补领域空白，提出评估低分辨率存储效率的新角度。
- **任务驱动评估**：强调下游分割性能而非仅重建指标，更贴近实际临床需求。
- **双层退化设计**：20×和10×涵盖中等与极端情况，有助于发现关键退化点（小尺度结构失效）。
- **代码开源**：促进可重复研究。

### 8. 不足与局限
- **弱监督方法单一**：仅用WSSS-Tissue，可能无法代表所有弱监督分割方法（如SEAM、SC-CAM、HisynSeg等）。
- **SR方法局限**：仅对比插值与STAR-RL，未包含其他病理SR方法（如PathSRGAN）或通用SR（如ESPCN、EDSR），结论泛化性受限。
- **数据集多样性不足**：仅两个器官（肺、乳腺），且均来自公开数据集，可能忽略其他组织（如肝脏、结肠）的退化敏感性差异。
- **缺乏全切片验证**：实验在patch级别进行，低倍率在整体WSI上的存储节省与分割一致性未评估（如如何选取patch、多分辨率融合策略等）。
- **储存节省量化缺失**：未给出实际存储空间节省的具体数值（如20×减少75%像素，但SR重建后的存储成本忽略）。
- **淋巴细胞失效原因浅析**：仅归因于小尺度结构，但未系统分析CAM的激活区域是否因低分辨率而消失。
- **计算成本未讨论**：STAR-RL推理需要额外调整步骤（缩放+SR+再缩放），其时间或内存开销未与插值对比。

（完）
