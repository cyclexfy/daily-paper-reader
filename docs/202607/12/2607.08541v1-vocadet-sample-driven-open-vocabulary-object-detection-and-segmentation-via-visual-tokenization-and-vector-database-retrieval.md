---
title: "VocaDet: Sample-Driven Open-Vocabulary Object Detection and Segmentation via Visual Tokenization and Vector Database Retrieval"
title_zh: "VocaDet: 基于视觉标记化和向量数据库检索的样本驱动开集目标检测与分割"
authors: ZhiXin Sun
date: 2026-07-09
pdf: "https://arxiv.org/pdf/2607.08541v1"
tags: ["query:cpath"]
score: 8.0
evidence: 基于视觉令牌化和检索的样本驱动开放词汇检测
tldr: 现有开放词汇检测方法依赖文本提示或有限视觉样本，难以扩展。VocaDet提出样本驱动范式，将连续视觉表示转化为离散视觉令牌，通过可扩展向量数据库进行高效检索识别，无需模型重训练。利用DINOv3提取特征，凝聚聚类生成多粒度令牌，结合位置去偏和空间拓扑存储为物体记忆；推理时匹配检索并引入背景过滤。在UA-DETRAC上取得有效检测性能，支持随正负样本积累持续扩展识别能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.08541v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1451, \"height\": 433, \"label\": \"Figure\"}]"
motivation: 现有开放词汇检测依赖文本提示或有限视觉样本，难以扩展到大规模持续增长的物体库。
method: 将视觉特征转化为离散视觉令牌，存入向量数据库；推理时匹配检索，结合背景过滤实现高效识别。
result: 在UA-DETRAC数据集上无需传统检测训练即实现有效开放词汇检测。
conclusion: VocaDet支持持续扩展的物体识别，无需重训练，为开放场景提供可扩展方案。
---

## 摘要
开集目标检测与分割旨在识别超出预定义类别的任意目标。尽管近期视觉-语言和基于参考的方法显著推进了该领域，但它们通常依赖于文本提示、有限的视觉示例或昂贵的特征匹配过程，难以扩展到大型且持续增长的目标库。本文提出VocaDet，一种样本驱动的开集目标检测与分割框架，该框架直接从用户提供的正负样本集合中学习目标概念，无需模型重训练。关键思想是将连续的视觉表示转化为离散的视觉词汇，并通过可扩展的向量数据库进行高效的基于检索的识别。具体而言，我们采用DINOv3作为视觉特征提取器，并应用具有自适应聚类敏感性的凝聚聚类生成多粒度视觉标记。这些视觉标记连同位置去偏表示和空间拓扑信息作为可扩展的目标记忆存储在向量数据库中。推理时，查询图像被转换为视觉标记，并与存储的目标记忆进行高效匹配以实现目标定位和分割。此外，引入背景过滤机制以移除频繁出现的背景模式，并减少实际固定摄像头场景中的冗余检索操作。在UA-DETRAC数据集上的实验表明，VocaDet无需传统检测器训练即可实现有效的开集检测性能，同时支持随着更多正负样本积累而持续扩展的识别能力。

## Abstract
Open-vocabulary object detection and segmentation aim to recognize arbitrary objects beyond predefined categories. Although recent vision-language and reference-based approaches have significantly advanced this field, they often rely on text prompts, limited visual examples, or expensive feature matching procedures, making them difficult to scale to large and continuously expanding object repositories. In this work, we propose VocaDet, a sample-driven open-vocabulary object detection and segmentation framework that learns object concepts directly from user-provided positive and negative sample collections without model retraining. The key idea is to transform continuous visual representations into discrete visual vocabularies and perform efficient retrieval-based recognition through a scalable vector database. Specifically, we employ DINOv3 as the visual feature extractor and apply agglomerative clustering with adaptive clustering sensitivity to generate multi-granularity visual tokens. These visual tokens, together with position-debiased representations and spatial topology information, are stored as expandable object memories in a vector database. During inference, query images are converted into visual tokens and efficiently matched against the stored object memories for object localization and segmentation. Furthermore, a background filtering mechanism is introduced to remove frequently occurring background patterns and reduce redundant retrieval operations in practical fixed-camera scenarios. Experiments on the UA-DETRAC dataset demonstrate that VocaDet achieves effective open-vocabulary detection performance without conventional detector training, while supporting continuously expandable recognition capability as additional positive and negative samples are accumulated.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 0. 论文的源代码链接
https://github.com/sunzx97/VocaDet

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **问题**：现有开放词汇目标检测与分割方法（如 Grounding DINO、T-Rex2、DINO-X 等）通常依赖文本提示、有限视觉样本或昂贵的特征匹配过程，难以扩展到大规模且持续增长的目标库；用户若提供大量正负样本集合，系统无法高效自动学习并实现快速检索。
- **背景**：传统检测器（如 YOLO）需针对不同场景重新训练；近期方法（如 Training-free）虽无需训练，但仅依赖少量参考图像，对大规模样本场景支持不足。实际应用（如固定摄像头监控）中，背景频繁出现导致冗余计算。
- **整体含义**：提出一种样本驱动的范式，无需模型重训练，通过视觉标记化和向量数据库检索，实现可扩展的开放词汇检测与分割，并支持随着样本积累持续提升性能。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
### 核心思想
将连续视觉表示离散化为视觉词汇（visual tokens），存储于可扩展的向量数据库中；推理时，查询图像的视觉标记与数据库中的目标记忆进行高效相似度匹配和拓扑一致性验证，实现目标定位与分割。

### 关键技术细节
- **视觉特征提取**：使用 DINOv3 作为骨干网络，提取密集语义特征。
- **视觉词汇构建**：对提取的特征应用凝聚聚类（agglomerative clustering），并引入自适应聚类敏感性参数（clustering sensitivity）以控制粒度，生成多粒度视觉标记。对于同一对象中空间不连通的区域，根据空间连通性分离为独立实例。
- **位置去偏（Position Debiasing）**：采用 INSID3 中的位置去偏策略，将特征转换为位置无关表示，减少空间偏置影响。
- **向量数据库构建**：使用 Milvus 向量数据库。用户可通过点、边界框、掩码三种方式标注目标。对于边界框/掩码，仅保留被标注区域覆盖超过90%的聚类区域特征；若多个聚类区域在同一标注内且空间相邻，则记录其拓扑关系（indexList 字段）。不同聚类敏感性设置对应多个数据库。
- **推理流程**：
  1. 输入图像经 DINOv3 提取特征，凝聚聚类生成视觉词汇。
  2. **背景过滤层**：将视觉词汇与背景特征记忆库比较，相似度超过阈值的视为背景并移除，其余送入目标向量数据库。
  3. 在目标数据库中进行相似度检索，同时检查拓扑一致性（若查询的两个聚类在存储库中对应条目具有相邻关系，则接受匹配）。
  4. 匹配的视觉聚类生成边界框，经 NMS 合并重叠框得到最终检测结果。
- **背景过滤层在线更新**：对每一帧，比较当前视觉词汇与背景记忆库，匹配则保留，未匹配的背景向量被移除；同时，若当前帧中未匹配的视觉词汇被检索为负样本或与正样本相似度超过阈值，则作为新背景特征加入记忆库。

### 算法流程简述
1. 离线阶段：用 DINOv3 提取正负样本特征 → 凝聚聚类生成多粒度视觉词汇 → 添加位置去偏和拓扑信息 → 存入向量数据库。
2. 在线推理：输入图像 → DINOv3 特征 → 凝聚聚类 → 背景过滤 → 向量数据库检索（相似度+拓扑验证） → 生成候选框 → NMS → 输出检测结果。

## 3. 实验设计：使用了哪些数据集 / 场景，它的 benchmark 是什么，对比了哪些方法
- **数据集**：UA-DETRAC（多目标检测与跟踪基准）。
- **Benchmark**：在 UA-DETRAC 上评估检测性能。
- **对比方法**：论文未明确列出对比方法，仅说明 VocaDet 无需传统检测器训练即可取得竞争性性能，并与常规固定类别检测器对比其可扩展性。未提供定量对比表格（论文仅3页正文，实验部分简短）。

## 4. 资源与算力
- **未明确说明**：论文未提及训练/推理使用的 GPU 型号、数量、训练时长等算力信息。仅提及使用 DINOv3 作为预训练特征提取器（冻结），无需模型训练，故算力需求主要来自特征提取和向量数据库检索。作者未给出具体硬件配置。

## 5. 实验数量与充分性
- **实验数量**：仅在一个数据集（UA-DETRAC）上进行了定性或简要定量评估。论文未包含消融实验、不同组件贡献分析、超参数敏感性实验、与多种对比方法的公平定量比较表格。
- **充分性与客观性**：实验严重不足。缺乏标准检测指标（如 mAP、AP@50）的数值报告；没有与现有开放词汇方法（如 Grounding DINO、T-Rex2、Training-free 等）的直接对比；消融研究未呈现（如背景过滤层效果、聚类敏感性影响、拓扑约束的作用等）。实验设计不充分，不足以充分验证方法的有效性。作者在结论中也坦承冷启动问题，但未通过实验量化。

## 6. 论文的主要结论与发现
- VocaDet 无需传统检测器训练，即可在 UA-DETRAC 上实现有效的开放词汇检测。
- 通过不断扩展正负样本集，识别性能可逐步提升（性能随样本积累而改善）。
- 背景过滤机制可有效减少固定摄像头场景下的冗余查询，提升效率。
- 方法存在一个固有局限：当多个空间相邻且外观相似的同类别目标出现时，可能被错误合并为一个检测实例（由于仅依赖特征相似性和空间聚类，缺乏区分性信息）。

## 7. 优点：方法或实验设计上的亮点
- **样本驱动、无需重训练**：用户只需提供正负样本（点、框、掩码），无需模型重新训练，适应性强。
- **可扩展性**：向量数据库支持动态增删样本，识别能力可随样本积累持续提升，适合大规模持续增长的目标库。
- **多粒度视觉词汇**：通过自适应聚类敏感性生成不同粒度表示，兼顾粗粒度和细粒度匹配。
- **拓扑一致性约束**：利用空间拓扑关系提升匹配准确性，减少误检。
- **背景过滤在线更新**：针对固定摄像头场景，动态维护背景记忆，显著降低检索计算量。
- **位置去偏策略**：减小特征空间偏置，增强匹配鲁棒性。

## 8. 不足与局限
- **实验覆盖严重不足**：仅在一个数据集上做简单评估，缺少标准指标（mAP等），无消融实验，无与同类方法定量对比，结论可靠性低。
- **冷启动问题**：初始阶段样本少时，难以准确刻画类别分布和特征边界（作者自述）。
- **邻近同类目标合并**：无法区分相邻且外观相似的目标，实例级区分能力弱。
- **依赖特征质量**：性能受限于 DINOv3 特征的判别能力，对极度相似物体或复杂背景可能失效。
- **聚类敏感性依赖人工设定**：虽然引入自适应参数，但具体设置和影响未实验分析。
- **缺乏计算效率定量分析**：未报告推理速度、数据库检索延迟等实际效率指标。
- **未在多样化场景验证**：仅限于 UA-DETRAC（车辆检测），未在其他通用数据集（如 COCO、LVIS）或开放场景测试。

（完）
