---
title: "MedCausalX: Adaptive Causal Reasoning with Self-Reflection for Trustworthy Medical Vision-Language Models"
title_zh: MedCausalX：面向可信医疗视觉语言模型的自反思自适应因果推理
authors: "Jianxin Lin, Chunzheng Zhu, Peter J. Kneuertz, Yunfei Bai, Yuan Xue"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23085v1"
tags: ["query:cpath"]
score: 8.0
evidence: 可信医学视觉语言模型的自适应因果推理
tldr: 针对医疗视觉语言模型在推理中缺乏因果机制、易受虚假相关性影响的问题，本文提出MedCausalX框架。该框架通过构建包含因果链和反事实样本的CRMed数据集，引入具备自反思能力的双阶段自适应架构，并结合强化学习优化因果一致性。实验证明，该方法显著提升了诊断一致性，减少了幻觉，为可信医疗推理树立了新标准。
selection_source: fresh_fetch
motivation: 现有的医疗思维链模型缺乏显式的因果推理机制，导致其容易受到虚假相关性的干扰，限制了临床应用的可靠性。
method: 提出MedCausalX框架，利用CRMed数据集引导因果学习，并通过自适应反思架构和轨迹级因果校正强化学习来优化推理链。
result: 实验显示该方法在诊断一致性上提升了5.4个百分点，幻觉率降低超过10个百分点，并在空间定位任务中表现优异。
conclusion: MedCausalX通过显式建模因果关系，有效区分了真实因果依赖与捷径关联，显著增强了医疗视觉语言模型的可信度与准确性。
---

## 摘要
视觉语言模型（VLMs）通过整合视觉感知与语言推理，实现了可解释的医疗诊断。然而，现有的医疗思维链（CoT）模型缺乏显式机制来表示和执行因果推理，使其容易受到伪相关性的影响，并限制了其临床可靠性。我们指出了医疗 CoT 推理中的三个核心挑战：如何自适应地触发因果修正、如何构建高质量的因果-伪相关对比样本，以及如何在推理轨迹中保持因果一致性。为了应对这些挑战，我们提出了 MedCausalX，这是一个在医疗 VLMs 中显式建模因果推理链的端到端框架。我们首先引入了 CRMed 数据集，该数据集提供了细粒度的解剖学标注、结构化因果推理链以及反事实变体，引导模型学习超越表层相关性的因果关系。基于 CRMed，MedCausalX 采用了配备 $\langle$causal$\rangle$ 和 $\langle$verify$\rangle$ 标记的两阶段自适应反思架构，使模型能够自主决定何时以及如何进行因果分析和验证。最后，通过错误归因强化学习优化的轨迹级因果修正目标细化了推理链，使模型能够区分真实的因果依赖与捷径关联。在多个基准测试上的广泛实验表明，MedCausalX 始终优于现有最先进的方法，将诊断一致性提高了 5.4 个百分点，将幻觉减少了 10 个百分点以上，并获得了顶尖的空间定位 IoU，从而为基于因果的医疗推理树立了新标准。

## Abstract
Vision-Language Models (VLMs) have enabled interpretable medical diagnosis by integrating visual perception with linguistic reasoning. Yet, existing medical chain-of-thought (CoT) models lack explicit mechanisms to represent and enforce causal reasoning, leaving them vulnerable to spurious correlations and limiting their clinical reliability. We pinpoint three core challenges in medical CoT reasoning: how to adaptively trigger causal correction, construct high-quality causal-spurious contrastive samples, and maintain causal consistency across reasoning trajectories. To address these challenges, we propose MedCausalX, an end-to-end framework explicitly models causal reasoning chains in medical VLMs. We first introduce the CRMed dataset providing fine-grained anatomical annotations, structured causal reasoning chains, and counterfactual variants that guide the learning of causal relationships beyond superficial correlations. Building upon CRMed, MedCausalX employs a two-stage adaptive reflection architecture equipped with $\langle$causal$\rangle$ and $\langle$verify$\rangle$ tokens, enabling the model to autonomously determine when and how to perform causal analysis and verification. Finally, a trajectory-level causal correction objective optimized through error-attributed reinforcement learning refines the reasoning chain, allowing the model to distinguish genuine causal dependencies from shortcut associations. Extensive experiments on multiple benchmarks show that MedCausalX consistently outperforms state-of-the-art methods, improving diagnostic consistency by +5.4 points, reducing hallucination by over 10 points, and attaining top spatial grounding IoU, thereby setting a new standard for causally grounded medical reasoning.

---

## 论文详细总结（自动生成）

### MedCausalX：面向可信医疗视觉语言模型的自反思自适应因果推理——论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
现有的医疗视觉语言模型（VLMs）虽然引入了思维链（CoT）推理，但普遍缺乏显式的因果建模机制。这导致模型在诊断时容易依赖“虚假相关性”（如背景特征、语言模型先验或训练分布偏差），而非遵循临床医学中“解剖定位 $\rightarrow$ 病理表征 $\rightarrow$ 诊断结论”的因果逻辑。这种“捷径学习”限制了模型在临床实践中的可靠性和可解释性。本文旨在解决如何让模型自适应地触发因果修正、构建高质量对比样本以及保持推理轨迹的因果一致性。

#### 2. 方法论
MedCausalX 提出了一个端到端的因果推理框架，核心包含以下三个部分：
*   **结构化因果建模（SCM）：** 将诊断过程形式化为 $A \rightarrow P \rightarrow Y$ 的因果链，即解剖标注（Anatomy）决定病理特征（Pathology），两者共同决定诊断结论（Diagnosis）。
*   **CRMed 数据集构建：** 基于现有基准（如 SA-Med2D-20M）构建，包含细粒度的解剖边界框、结构化因果链，以及通过局部扰动生成的“反事实”样本（如修改解剖位置或病理属性），用于区分因果关系与虚假相关。
*   **自适应反思架构：** 引入特殊的 `<causal>` 和 `<verify>` 标记。模型先生成初步因果分析，随后由 `<verify>` 触发自反思，自主检测并修正推理中的逻辑中断或虚假关联。
*   **双阶段强化学习优化：**
    1.  **Causal SFT：** 在 CRMed 上进行监督微调，建立基础因果推理能力。
    2.  **错误归因优化：** 结合离线 DPO（直接偏好优化）处理错误轨迹，以及在线 GRPO（组相对策略优化）进行自适应精炼。奖励函数综合了准确性、格式规范性和因果一致性（通过 LLM-as-judge 评估）。

#### 3. 实验设计
*   **数据集/场景：** 涵盖了 5 个主流医疗影像基准：MIMIC-CXR（胸部 X 片报告生成）、SLAKE 和 VQA-RAD（放射影像问答）、PathVQA（病理影像问答）、SA-Med2D-20M（区域中心化任务）。
*   **对比方法（Baselines）：**
    *   通用 VLM：InternVL, GPT-4V, Qwen2.5-VL。
    *   医疗专用 VLM：Med-Flamingo, LLaVA-Med, RadFM, MedDr, MedRegA。
    *   医疗 CoT 模型：MedVLM-R1, Med-R1。
*   **评估指标：** 语言质量（BLEU、F1）、空间精度（IoU、Region Acc）、幻觉率（Hallucination Rate）以及诊断一致性。

#### 4. 资源与算力
*   **硬件：** 使用了 6 台 NVIDIA A100 (40GB) GPU。
*   **时长：** 每个数据集的训练时间约为 56 至 72 小时。
*   **模型规模：** 基于 Qwen2.5-VL 架构，实验涵盖了 3B、14B 和 32B 参数版本，主实验使用 32B 版本。

#### 5. 实验数量与充分性
*   **实验规模：** 进行了大规模的跨基准测试，并采用了 5 折交叉验证以确保统计可靠性。
*   **消融实验：** 针对 CRMed 数据集、反思标记、SFT、强化学习阶段以及超参数（如错误定位阈值 $\tau$、GRPO 组大小 $G$）进行了详尽的消融研究。
*   **专家评估：** 邀请了拥有 20 年经验的资深放射科医生对空间定位和诊断质量进行 3 分制盲评。
*   **充分性评价：** 实验设计非常充分，不仅涵盖了自动化指标，还引入了人类专家评价和零样本（Zero-shot）跨领域泛化测试，体现了高度的客观性和公平性。

#### 6. 主要结论与发现
*   **性能领先：** MedCausalX 在所有基准上均优于 SOTA 方法，诊断一致性提升 5.4%，幻觉率降低超过 10%。
*   **因果修正有效：** 自适应反思机制能有效识别并纠正推理轨迹中的“捷径”，使模型从模式匹配转向因果推理。
*   **空间-语义对齐：** 在多病灶场景下，MedCausalX 的空间定位精度（Region-F1）显著高于仅追求语言流畅度的模型。
*   **泛化能力：** 在眼科（OVQA）和皮肤科（Skin-VQA）等未见领域表现出极强的零样本迁移能力。

#### 7. 优点
*   **显式因果约束：** 突破了传统 CoT 仅作为“后验解释”的局限，将因果逻辑嵌入模型生成过程。
*   **自适应修正：** `<verify>` 标记的设计模拟了临床医生复核诊断的思维过程，增强了系统的鲁棒性。
*   **高质量数据：** CRMed 提供的反事实样本为解决 VLM 的虚假相关性问题提供了有效的监督信号。
*   **强化学习策略：** 结合 DPO 和 GRPO 的双阶段优化，既保证了错误修正的效率，又兼顾了探索的灵活性。

#### 8. 不足与局限
*   **计算开销：** 32B 规模的模型结合复杂的强化学习训练，对算力资源要求较高，限制了其在资源受限环境下的部署。
*   **评估依赖：** 因果一致性奖励部分依赖于 LLM-as-judge（如 GPT-4o），可能存在模型偏见或评估不一致的风险。
*   **维度限制：** 目前主要集中在 2D 医疗影像，对于 3D 序列（如完整的 CT/MRI 卷）的复杂时空因果建模尚待进一步扩展。

（完）
