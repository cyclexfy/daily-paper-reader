---
title: "RVLM: Recursive Vision-Language Models with Adaptive Depth"
title_zh: RVLM：具有自适应深度的递归视觉语言模型
authors: "Nicanor Mayumu, Zeenath Khan, Melodena Stephens, Patrick Mukala, Farhad Oroumchian"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24224v1"
tags: ["query:cpath"]
score: 8.0
evidence: 用于医疗AI可审计性的递归视觉语言模型
tldr: 针对医疗AI模型推理过程不透明及固定迭代步数效率低的问题，本文提出RVLM框架。该框架通过“生成-执行”循环，利用Python代码驱动视觉子代理进行迭代推理，使诊断过程可审计。同时引入RRouter实现自适应深度控制。在脑部MRI和胸部X射线任务中，RVLM展现了高一致性、跨模态差异检测能力及高效的报告生成能力。
selection_source: fresh_fetch
motivation: 传统医疗视觉语言模型存在推理过程不可审计以及固定迭代步数导致计算资源分配不均的问题。
method: 提出RVLM框架，通过Python代码驱动的迭代推理循环实现诊断证据的可追溯性，并利用RRouter根据任务复杂度自适应调整迭代深度。
result: 在BraTS 2023和MIMIC-CXR数据集上的实验表明，该模型在病灶检测、跨模态差异识别及结构化报告生成方面具有高度一致性和准确性。
conclusion: RVLM通过结合可执行代码的迭代推理与自适应深度控制，显著提升了医疗AI系统的透明度、可解释性与推理效率。
---

## 摘要
医疗人工智能系统面临两个根本性的局限。首先，传统的视觉语言模型（VLM）执行单次推理，产生黑盒预测，无法在临床术语层面进行审计或解释。其次，展示中间步骤的迭代推理系统依赖于固定的迭代预算，在简单案例上浪费计算资源，而在复杂案例上深度不足。我们通过一个统一的框架解决了这两个局限。RVLM 用迭代的“生成-执行”循环取代了单次推理：在每一步中，模型编写 Python 代码，调用视觉子代理，处理图像并积累证据。每一个诊断结论都基于可执行代码，满足了临床 AI 治理框架的审计要求。RRouter 使迭代深度具备自适应性：一个轻量级控制器根据任务复杂度特征预测最佳预算，随后监控进度并在推理停滞时提前终止。我们在 BraTS 2023 脑膜瘤（脑部 MRI）和 MIMIC-CXR（胸部 X 光）数据集上，使用未经微调的 Gemini 2.5 Flash 进行了评估。在多次重复运行中，RVLM 在显著发现（如肿块存在和增强）上表现出高度一致性，并能检测出液体衰减反转恢复（FLAIR）信号特征与分割边界之间的跨模态差异。在 MIMIC-CXR 上，它能生成结构化报告并正确识别特定视图的伪影。代码：https://github.com/nican2018/rvlm。

## Abstract
Medical AI systems face two fundamental limitations. First, conventional vision-language models (VLMs) perform single-pass inference, yielding black-box predictions that cannot be audited or explained in clinical terms. Second, iterative reasoning systems that expose intermediate steps rely on fixed iteration budgets wasting compute on simple cases while providing insufficient depth for complex ones. We address both limitations with a unified framework. RVLM replaces single-pass inference with an iterative generate-execute loop: at each step, the model writes Python code, invokes vision sub-agents, manipulates images, and accumulates evidence. Every diagnostic claim is grounded in executable code, satisfying auditability requirements of clinical AI governance frameworks. RRouter makes iteration depth adaptive: a lightweight controller predicts the optimal budget from task-complexity features, then monitors progress and terminates early when reasoning stalls. We evaluate on BraTS 2023 Meningioma (brain MRI) and MIMIC-CXR (chest X-ray) using Gemini 2.5 Flash without fine-tuning. Across repeated runs, RVLM shows high consistency on salient findings (e.g., mass presence and enhancement) and can detect cross-modal discrepancies between Fluid-Attenuated Inversion Recovery (FLAIR) signal characteristics and segmentation boundaries. On MIMIC-CXR, it generates structured reports and correctly recognises view-specific artefacts. Code: https://github.com/nican2018/rvlm.

---

## 论文详细总结（自动生成）

### RVLM：具有自适应深度的递归视觉语言模型论文总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
医疗 AI 系统在临床应用中面临两大核心瓶颈：
*   **解释性与审计困境**：传统的视觉语言模型（VLM）多采用“单次推理（Single-pass）”模式，直接输出诊断结果。这种“黑盒”预测缺乏中间推理步骤，无法满足临床医学对证据链的要求，也难以符合《欧盟 AI 法案》等监管框架中关于透明度和可追溯性的规定。
*   **计算资源分配不均**：现有的迭代推理系统通常设定固定的迭代步数（Fixed Budget）。这导致简单病例（如边界清晰的肿瘤）浪费计算资源和时间，而复杂病例（如多发性、浸润性病变）则因推理深度不足而影响准确性。

#### 2. 论文提出的方法论
论文提出了一个结合递归推理与自适应深度控制的统一框架：
*   **RVLM（递归视觉语言模型）**：将单次推理替换为“生成-执行”循环。模型运行在持久化的 Python REPL 环境中，每一步推理都会生成可执行代码，调用视觉子代理（如 `describe_image`、`llm_query_with_images`）进行图像处理、特征提取和证据积累。所有诊断结论均由可执行代码支撑，形成了可审计的推理轨迹。
*   **RRouter（递归路由）**：实现自适应迭代深度。
    *   **复杂度评估**：提取分割掩码的特征，包括标签熵（Label Entropy）、肿瘤总体积、子区域数量及微小区域指标，计算综合复杂度分数 $s$。
    *   **预算预测**：根据分数 $s$ 将迭代预算映射为 3 到 6 步不等。
    *   **停滞检测**：在推理过程中监控进度，若连续两次迭代未产生有效输出，则提前终止，以节省算力。

#### 3. 实验设计
*   **数据集**：
    1.  **BraTS 2023 Meningioma**：脑部 MRI 脑膜瘤数据，包含四种模态（T1n, T1ce, T2w, T2-FLAIR）及分割掩码。
    2.  **MIMIC-CXR**：胸部 X 射线影像，包含 PA/Lateral/AP 等多视角图像。
*   **基准模型（Backbone）**：使用 **Gemini 2.5 Flash**，且**无需任何任务特定的微调（Zero-shot）**。
*   **对比方法**：主要对比了传统的单次推理（Single-pass）VLM 表现，并进行了多次独立运行以测试一致性。

#### 4. 资源与算力
*   **算力说明**：论文未提及具体的本地 GPU 型号或数量。由于该研究基于商用大模型 API（Gemini 2.5 Flash），其核心开销在于 API 调用产生的 Token 消耗和网络延迟。
*   **效率数据**：论文记录了推理时间。例如，脑部 MRI 案例平均耗时约 67-95 秒，消耗约 1.3k-1.5k 个输入 Token。通过 RRouter，简单案例的迭代次数从固定的 12 次减少到 3 次，显著降低了 API 成本。

#### 5. 实验数量与充分性
*   **实验规模**：论文主要通过详细的**案例研究（Case Studies）**展开。针对 BraTS 数据集进行了 16 次独立重复运行以评估诊断一致性。
*   **充分性评价**：
    *   **优点**：实验涵盖了两种截然不同的临床场景（脑部 MRI 和胸部 X 光），验证了框架的通用性；一致性分析证明了模型在显著特征识别上的可靠性。
    *   **不足**：实验样本量较小，缺乏大规模的定量指标（如在整个验证集上的准确率、F1 分数等），目前更偏向于原型验证和定性分析。

#### 6. 论文的主要结论与发现
*   **可审计性**：RVLM 能够生成完整的 Python 执行日志，将像素级观察与临床结论联系起来，满足了高风险 AI 系统的监管要求。
*   **跨模态发现**：RVLM 能够自主检测出单次推理模型无法识别的矛盾。例如，它能发现 FLAIR 信号显示的异常与分割边界不一致的情况。
*   **自适应效率**：RRouter 成功实现了根据任务复杂度动态调整计算量，在保证复杂案例推理深度的同时，大幅提升了简单案例的处理效率。
*   **高一致性**：在重复实验中，模型对显著放射学特征（如肿块存在、增强模式）的识别一致性达到 90% 以上。

#### 7. 优点（亮点）
*   **透明度设计（Trust-by-Design）**：将可解释性融入架构底层，而非事后解释。
*   **无需微调**：通过“代码即思考（Code-as-thought）”的提示工程，使通用大模型在专业医疗任务上表现出极高的专业性。
*   **自适应计算**：引入 RRouter 解决了 LLM 代理中常见的“固定步数”低效问题。
*   **多模态融合**：通过 REPL 环境实现了对图像的程序化操作（如裁剪、对比度增强、差分图计算），超越了简单的视觉问答。

#### 8. 不足与局限
*   **定量评估缺失**：缺乏在大规模数据集上的自动化评估指标（如 CheXBERT 或 RadGraph 分数）。
*   **维度限制**：目前主要处理 2D 轴向切片，尚未完全扩展到 3D 体素级的递归导航。
*   **权重启发式**：RRouter 的复杂度特征权重目前是手动设置的，而非通过数据驱动学习获得。
*   **临床验证**：作为研究原型，其生成的报告尚未经过双盲临床对照实验验证。

（完）
