---
title: "Seeing Like Radiologists: Context- and Gaze-Guided Vision-Language Pretraining for Chest X-rays"
title_zh: 像放射科医生一样观察：针对胸部 X 射线的上下文与注视引导的视觉语言预训练
authors: "Kang Liu, Zhuoqi Ma, Siyu Liang, Yunan Li, Xiyue Gao, Chao Liang, Kun Xie, Qiguang Miao"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26049v1"
tags: ["query:cpath"]
score: 8.0
evidence: 医学放射影像的视觉语言预训练
tldr: 本研究提出CoGaze框架，旨在模拟放射科医生的诊断过程。针对现有模型忽视临床上下文和视线引导的问题，CoGaze引入了上下文注入的视觉编码器，并结合多级监督范式，利用放射科医生的视线作为概率先验来引导注意力。实验证明，该方法在胸部X光报告生成、零样本分类和图像检索等任务上均显著优于现有最先进模型，有效提升了医学视觉语言预训练的诊断推理能力。
selection_source: fresh_fetch
motivation: 现有医学视觉语言预训练模型忽视了临床上下文和放射科医生视线在诊断推理中的关键作用，导致跨模态对齐效果不佳。
method: 提出CoGaze框架，通过上下文注入编码器整合临床信息，并利用视线引导的注意力及多级监督范式实现疾病感知的跨模态对齐。
result: "CoGaze在报告生成、零样本分类（AUROC提升23.2%）和图像检索等任务上均刷新了SOTA记录，表现出极强的诊断准确性。"
conclusion: 结合临床上下文和专家视线先验能显著增强模型对放射影像的理解，是提升医学视觉语言预训练性能的有效途径。
---

## 摘要
尽管医学视觉语言预训练近期取得了进展，但现有模型仍难以捕捉诊断工作流：放射影像通常被视为与上下文无关的图像，而放射科医生的注视（视觉推理的关键线索）在现有方法中仍未得到充分探索。这些局限性阻碍了疾病特定模式的建模，并削弱了跨模态对齐。为了弥补这一差距，我们引入了 CoGaze，这是一个针对胸部 X 射线的上下文与注视引导的视觉语言预训练框架。我们首先提出了一种注入上下文的视觉编码器，该编码器模拟了放射科医生如何整合临床上下文（包括病史、症状和诊断意图）来引导诊断推理。随后，我们提出了一种多级监督范式：(1) 通过混合正样本对比学习强化模态内和模态间的语义对齐；(2) 通过疾病感知的跨模态表示学习注入诊断先验；(3) 利用放射科医生的注视作为概率先验，引导注意力转向诊断显著区域。广泛的实验表明，CoGaze 在多项任务中始终优于现有最先进的方法，在自由文本和结构化报告生成中分别提升了高达 2.0% 的 CheXbertF1 和 1.2% 的 BLEU2，在零样本分类中提升了 23.2% 的 AUROC，在图文检索中提升了 12.2% 的 Precision@1。代码可在 https://github.com/mk-runner/CoGaze 获取。

## Abstract
Despite recent advances in medical vision-language pretraining, existing models still struggle to capture the diagnostic workflow: radiographs are typically treated as context-agnostic images, while radiologists' gaze -- a crucial cue for visual reasoning -- remains largely underexplored by existing methods. These limitations hinder the modeling of disease-specific patterns and weaken cross-modal alignment. To bridge this gap, we introduce CoGaze, a Context- and Gaze-guided vision-language pretraining framework for chest X-rays. We first propose a context-infused vision encoder that models how radiologists integrate clinical context -- including patient history, symptoms, and diagnostic intent -- to guide diagnostic reasoning. We then present a multi-level supervision paradigm that (1) enforces intra- and inter-modal semantic alignment through hybrid-positive contrastive learning, (2) injects diagnostic priors via disease-aware cross-modal representation learning, and (3) leverages radiologists' gaze as probabilistic priors to guide attention toward diagnostically salient regions. Extensive experiments demonstrate that CoGaze consistently outperforms state-of-the-art methods across diverse tasks, achieving up to +2.0% CheXbertF1 and +1.2% BLEU2 for free-text and structured report generation, +23.2% AUROC for zero-shot classification, and +12.2% Precision@1 for image-text retrieval. Code is available at https://github.com/mk-runner/CoGaze.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **CoGaze** 的医学视觉语言预训练（VLP）框架，旨在通过模拟放射科医生的诊断逻辑来提升胸部 X 射线影像的分析能力。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的医学视觉语言预训练模型通常将 X 射线影像视为孤立的、与上下文无关的图像，忽视了放射科医生在实际诊断中依赖的两个关键要素：**临床上下文**（如病史、症状）和**注视轨迹**（反映诊断关注点的视觉推理线索）。
*   **研究背景**：直接套用自然图像的 VLP 策略（如 CLIP）在医学领域效果有限，因为医学影像标注成本极高且数据稀缺。现有模型虽尝试引入辅助任务，但仍缺乏临床合理性，导致生成的表示缺乏临床落地价值。

### 2. 方法论：CoGaze 框架
CoGaze 通过整合临床上下文和专家注视信号，构建了一个模拟人类医生思维的预训练流程：
*   **上下文注入视觉编码器 (Context-Infused Vision Encoder)**：
    *   利用 **Perceiver** 模块将图像补丁（Patches）与临床上下文（Indication 和 History）进行融合。
    *   引入可学习的视图位置嵌入（View-positional embeddings），处理不同拍摄角度（如正位、侧位）。
    *   设计了上下文自适应机制，在临床信息缺失时能优雅降级为纯图像推理。
*   **多级监督范式 (Multi-Level Supervision)**：
    1.  **混合正样本对比学习 ($L_{con}$)**：针对一个检查报告对应多张影像的临床实际，统一了单正样本和多正样本的对比学习，强化全局语义对齐。
    2.  **疾病感知跨模态学习 ($L_{cls}$)**：利用 CheXbert 自动标注的 14 种疾病标签，在共享的标签空间内对齐图像和文本，注入诊断先验。
    3.  **软注视引导 ($L_G$)**：将放射科医生的注视轨迹建模为**概率先验**（而非简单的二值掩码），通过双向 **Jensen-Shannon 散度 (JSD)** 约束模型注意力图与专家注视热力图的一致性。

### 3. 实验设计
*   **数据集**：
    *   **预训练**：MIMIC-CXR（约 24 万张影像），其中 1,711 例包含注视数据（来自 EGD 和 REFLACX）。
    *   **下游任务**：涵盖 7 个公开数据集（NIH, SIIM, RSNA, Shenzhen, TBX11K 等）。
*   **任务与 Benchmark**：
    *   **报告生成**：自由文本（MIMIC-CXR）和结构化报告（SRRG-Findings）。
    *   **分类**：多标签分类、二分类、零样本（Zero-shot）分类。
    *   **分割与检索**：病灶分割（Dice 指标）和图文检索（P@K, R@K）。
*   **对比方法**：对比了 13 种以上 SOTA 模型，包括 MedCLIP, MGCA, MRM, EGMA, 以及大模型 CheXagent 和 LLaVA-Med。

### 4. 资源与算力
*   **硬件**：单张 **NVIDIA RTX 5880 Ada GPU (48GB)**。
*   **训练细节**：
    *   预训练阶段：10 个 Epoch，Batch Size 为 80，学习率 5e-5。
    *   模型参数：预训练模型约 2.25 亿参数（1.39 亿可训练）。
    *   报告生成变体：使用了 DistilGPT2（轻量级）和 Llama-3.2-3B-Instruct（基于 LoRA 微调）。

### 5. 实验数量与充分性
*   **实验规模**：论文在 **6 类下游任务**和 **7 个数据集**上进行了广泛测试，实验组数非常多。
*   **消融实验**：详细探讨了临床上下文、多级监督模块、注视数据比例（从 1% 到 100%）、以及不同损失函数（MSE vs. JSD）的影响。
*   **客观性**：遵循了 BenchX 等标准评估协议，并对注视数据极度稀缺（仅占总数据 0.71%）的情况进行了鲁棒性验证，证明了即使少量专家数据也能带来显著提升，实验设计严谨且公平。

### 6. 主要结论与发现
*   **性能刷新**：CoGaze 在所有任务中均优于 SOTA。特别是在**零样本分类**中，AUROC 提升了 **23.2%**；在**图像检索**中，Precision@1 提升了 **12.2%**。
*   **注视价值**：证明了将专家注视作为“软先验”比硬掩码（Binary Mask）更有效，能帮助模型学习到更精细的解剖学特征。
*   **上下文重要性**：引入病史和症状显著提升了报告生成的临床准确性（CheXbert F1 提升 2.0%）。

### 7. 优点（亮点）
*   **临床一致性**：不同于纯数据驱动的模型，CoGaze 的设计逻辑高度贴合放射科医生的实际工作流。
*   **软注视建模**：利用 JSD 处理注视热力图，捕捉了视觉注意力的连续性和不确定性，比以往的二值化处理更科学。
*   **高效性**：在相对较小的算力（单卡）和模型规模下，达到了超越大型多模态模型的性能。

### 8. 不足与局限
*   **注视数据稀缺**：虽然模型在少量注视数据下表现良好，但目前这类高质量专家标注数据的获取依然是瓶颈。
*   **缺乏时序建模**：目前的模型主要针对单次检查，尚未整合患者的历史影像进行纵向对比（Longitudinal analysis），这在发现微小病变演变时至关重要。
*   **细微病变识别**：在定性分析中发现，模型有时会遗漏极轻微的异常（如轻微肺不张），说明对病变严重程度的区分能力仍有提升空间。

（完）
