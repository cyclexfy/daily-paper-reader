---
title: "Clore: Interactive Pathology Image Segmentation with Click-based Local Refinement"
title_zh: Clore：基于点击式局部细化的交互式病理图像分割
authors: "Tiantong Wang, Minfan Zhao, Jun Shi, Hannan Wang, Yue Dai"
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27625v1"
tags: ["query:cpath"]
score: 9.0
evidence: 具有局部细化的交互式病理图像分割
tldr: 本研究针对病理图像分割中现有交互式方法依赖全局更新、难以捕捉细微结构的问题，提出了Clore（基于点击的局部细化）框架。该框架采用分层交互范式：初始点击用于快速生成全局轮廓，后续点击则专注于局部细节的逐步精细化。Clore不仅减少了冗余预测，还显著提升了对复杂边界的处理能力。在四个数据集上的实验证明，该方法在分割精度与交互成本之间达到了最佳平衡，为高效病理图像分析提供了有力支持。
selection_source: fresh_fetch
motivation: 现有的交互式分割方法主要依赖迭代式全局更新，导致计算冗余且难以修正局部的细微错误。
method: 提出Clore流水线，通过分层交互范式将初始点击用于全局轮廓生成，后续点击用于局部细节的精确细化。
result: 在四个病理图像数据集上的实验结果表明，Clore在保持高精度的同时显著降低了用户的交互成本。
conclusion: Clore通过结合全局与局部细化策略，为病理图像的高效、精准交互式分割提供了一种简单且有效的解决方案。
---

## 摘要
近年来，基于深度学习的交互式分割方法的进展显著提升了病理图像分割的性能。大多数现有方法利用用户提供的正向和负向点击来引导分割过程。然而，这些方法主要依赖迭代式的全局更新进行细化，这导致了冗余的重复预测，且在局部调整过程中往往无法捕捉细粒度结构或修正细微错误。为了解决这一局限性，我们提出了基于点击的局部细化（Clore）流水线，这是一种旨在增强交互式分割的简单且高效的方法。Clore 的核心创新在于其分层交互范式：初始点击驱动全局分割以快速勾勒出大的目标区域，而随后的点击则逐步细化局部细节以实现精确的边界。这种方法不仅提高了处理细粒度分割任务的能力，而且能以更少的交互次数获得高质量的结果。在四个数据集上的实验结果表明，Clore 在分割精度和交互成本之间达到了最佳平衡，使其成为高效且准确的交互式病理图像分割的有效解决方案。

## Abstract
Recent advancements in deep learning-based interactive segmentation methods have significantly improved pathology image segmentation. Most existing approaches utilize user-provided positive and negative clicks to guide the segmentation process. However, these methods primarily rely on iterative global updates for refinement, which lead to redundant re-prediction and often fail to capture fine-grained structures or correct subtle errors during localized adjustments. To address this limitation, we propose the Click-based Local Refinement (Clore) pipeline, a simple yet efficient method designed to enhance interactive segmentation. The key innovation of Clore lies in its hierarchical interaction paradigm: the initial clicks drive global segmentation to rapidly outline large target regions, while subsequent clicks progressively refine local details to achieve precise boundaries. This approach not only improves the ability to handle fine-grained segmentation tasks but also achieves high-quality results with fewer interactions. Experimental results on four datasets demonstrate that Clore achieves the best balance between segmentation accuracy and interaction cost, making it an effective solution for efficient and accurate interactive pathology image segmentation.

---

## 论文详细总结（自动生成）

这是一份关于论文《Clore: Interactive Pathology Image Segmentation with Click-based Local Refinement》的结构化总结：

### 1. 核心问题与研究动机
病理图像分割是肿瘤诊断的金标准，但全监督学习依赖昂贵的像素级标注。**交互式分割**通过人类点击引导模型，是平衡精度与成本的有效手段。
*   **现有问题**：大多数方法（如 RITM）采用迭代式全局更新，每增加一个点击都要重新预测整幅图像，导致计算冗余，且难以捕捉细微的局部结构或修正局部错误。
*   **改进方向**：虽然 FocalClick 引入了局部细化，但其双网络结构增加了计算负担，且在交互早期（用户意图尚不明确时）强制进行局部细化可能会干扰全局分类。

### 2. 方法论
论文提出了 **Clore** 流水线，核心思想是**分层交互范式**：
*   **触发式局部细化策略**：设置超参数 $n$（点击次数阈值）。
    *   **前 $n$ 次点击**：执行全局粗分割，快速勾勒目标轮廓。
    *   **$n$ 次点击后**：触发局部细化。系统根据当前点击位置和预测差异自动选择一个**局部补丁（Local Patch, LP）**。
*   **共享分割网络**：不同于使用独立子网络的方法，Clore 使用同一个 **HRNet+OCR** 网络同时处理全局预测和局部细化。这种参数共享机制能利用高层语义特征，减少模型体积并防止过拟合。
*   **局部补丁选择算法**：通过计算当前预测与前一次预测的差异，识别包含最新点击点的连通区域，并以此为中心提取局部区域进行高分辨率细化。
*   **训练与损失函数**：采用混合点击模拟策略（随机+迭代点击）。损失函数结合了 **归一化 Focal Loss (LNF)** 和辅助的 **二元交叉熵损失 (BCE)**。

### 3. 实验设计
*   **数据集**：在四个具有挑战性的病理数据集上进行了验证：
    1.  **DigestPath**：结直肠癌恶性病变。
    2.  **GlaS**：结直肠腺体（包含正常和癌变）。
    3.  **NuCLS**：多器官（乳腺/肺癌）异质细胞核。
    4.  **BloodCell**：在线收集的各类血细胞图像。
*   **基准模型 (Benchmark)**：对比了当前最先进的交互式分割方法 **RITM** 和 **FocalClick**。
*   **评价指标**：采用达到目标 IoU（85% 和 90%）所需的平均点击次数 (**NoC**) 以及在规定点击次数内未达标的样本数 (**NoF**)。

### 4. 资源与算力
*   **硬件**：使用了一台 **NVIDIA H100 GPU (80GB)** 进行训练和推理。
*   **训练细节**：模型训练了 **230 个 epoch**，使用 Adam 优化器，初始学习率为 $5 \times 10^{-4}$，并在第 20 和 40 个 epoch 时进行衰减。

### 5. 实验数量与充分性
*   **实验规模**：论文在 4 个不同类型的病理数据集上进行了详尽的定量对比，并绘制了 mDice 随点击次数变化的收敛曲线。
*   **消融实验**：针对触发阈值 $n$ 进行了专门的消融研究（测试了 $n=2$ 到 $n=9$ 的效果），验证了 $n=5$ 作为通用策略的合理性。
*   **效率分析**：对比了每秒点击处理时间 (SPC) 和完成单张标注的总耗时。
*   **评价**：实验设计较为全面，涵盖了精度、速度、鲁棒性和超参数敏感性分析，结果具有较强的说服力。

### 6. 主要结论与发现
*   **性能领先**：在 GlaS、NuCLS 和 DigestPath 数据集上，Clore 的 NoC@90% 表现均为最优，相比基准方法平均减少了约 1.8 次点击。
*   **收敛稳定**：在 NuCLS 等复杂数据集上，Clore 在 15 次点击后的表现显著优于 FocalClick（领先约 8.99%），证明了其在处理密集细微结构时的稳定性。
*   **高效率**：Clore 的平均单次点击处理时间为 **77ms**，总标注时间比 RITM 快 35%，比 FocalClick 快 52%。

### 7. 优点
*   **逻辑合理**：分层交互符合人类标注习惯（先定大轮廓，再修小细节）。
*   **架构精简**：通过共享网络实现全局和局部的统一，既提升了特征一致性，又降低了计算开销。
*   **局部修正精准**：通过方差分析和连通域选择，能有效解决腺体粘连、边界模糊等病理图像特有的难点。

### 8. 不足与局限
*   **对噪声敏感**：在 BloodCell 数据集上表现略逊于基准方法。论文指出，这主要是因为该数据集标注质量不一且染色不均，导致全局预测产生的噪声先验在局部细化阶段被放大。
*   **超参数依赖**：虽然 $n=5$ 是推荐值，但针对不同难度的任务（如极度密集的细胞核 vs 巨大的肿瘤区域），最优触发时机可能仍有差异。
*   **极端场景挑战**：对于超高密度目标或背景极度复杂的样本，模型仍存在误差累积的风险。

（完）
