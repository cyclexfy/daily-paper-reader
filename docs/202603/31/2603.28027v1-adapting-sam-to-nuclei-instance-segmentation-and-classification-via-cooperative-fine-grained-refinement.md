---
title: Adapting SAM to Nuclei Instance Segmentation and Classification via Cooperative Fine-Grained Refinement
title_zh: 通过协同细粒度细化将 SAM 适配于细胞核实例分割与分类
authors: "Jingze Su, Tianle Zhu, Jiaxin Cai, Zhiyi Wang, Qi Li, Xiao Zhang, Tong Tong, Shu Wang, Wenxi Liu"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28027v1"
tags: ["query:cpath"]
score: 10.0
evidence: 将SAM基础模型应用于计算病理学中的细胞核分割
tldr: 针对细胞核实例分割中SAM模型缺乏局部结构感知且微调成本高的问题，本文提出了一种名为CFR-SAM的参数高效微调框架。该框架通过多尺度自适应局部感知适配器、分层调制融合模块和边界引导掩码细化三个核心组件，在保留SAM全局建模能力的同时，显著增强了对局部细节和边界的捕捉能力，实现了高效且精准的细胞核分割与分类。
selection_source: fresh_fetch
motivation: SAM模型在处理医学影像时缺乏对细胞核局部结构特征的感知能力，且全量微调面临巨大的计算开销。
method: 提出由多尺度自适应适配器、分层特征融合模块和边界引导细化组件构成的协同微调框架，以增强局部感知并保留空间细节。
result: 该框架以极小的参数代价成功迁移了SAM的先验知识，显著提升了细胞核分割的准确性及边界清晰度。
conclusion: CFR-SAM证明了通过协同增强局部特征与边界信息，可以高效地将通用大模型适配到复杂的医学图像分割任务中。
---

## 摘要
细胞核实例分割在计算病理学中对于癌症诊断和预后至关重要。最近，Segment Anything Model (SAM) 凭借其在大规模自然图像预训练中获得的丰富先验和强大的全局上下文建模能力，在各种分割任务中展现了卓越的性能。然而，将 SAM 直接应用于医学成像领域面临重大局限：它缺乏对细胞核分割至关重要的局部结构特征的充分感知，且针对下游任务进行全量微调需要巨大的计算成本。为了高效地将 SAM 强大的先验知识迁移到细胞核实例分割，同时补充其任务感知的局部感知能力，我们提出了一种名为 SAM 协同细粒度细化（Cooperative Fine-Grained Refinement of SAM）的参数高效微调框架，该框架由三个核心组件组成：1) 多尺度自适应局部感知适配器（Multi-scale Adaptive Local-aware Adapter），通过以极少的参数增强冻结的 SAM 主干网络，并利用动态生成的多尺度卷积核注入强大的局部结构感知能力，实现有效的性能迁移；2) 分层调制融合模块（Hierarchical Modulated Fusion Module），动态聚合多级编码器特征以保留细粒度的空间细节；3) 边界引导掩码细化（Boundary-Guided Mask Refinement），通过显式监督将多上下文边界线索与语义特征整合，产生聚焦边界的信号以细化初始掩码预测，从而获得更清晰的轮廓。这三个组件协同工作，增强了局部感知，保留了空间细节并细化了边界，使 SAM 能够直接执行准确的细胞核实例分割。

## Abstract
Nuclei instance segmentation is critical in computational pathology for cancer diagnosis and prognosis. Recently, the Segment Anything Model has demonstrated exceptional performance in various segmentation tasks, leveraging its rich priors and powerful global context modeling capabilities derived from large-scale pre-training on natural images. However, directly applying SAM to the medical imaging domain faces significant limitations: it lacks sufficient perception of the local structural features that are crucial for nuclei segmentation, and full fine-tuning for downstream tasks requires substantial computational costs. To efficiently transfer SAM's robust prior knowledge to nuclei instance segmentation while supplementing its task-aware local perception, we propose a parameter-efficient fine-tuning framework, named Cooperative Fine-Grained Refinement of SAM, consisting of three core components: 1) a Multi-scale Adaptive Local-aware Adapter, which enables effective capability transfer by augmenting the frozen SAM backbone with minimal parameters and instilling a powerful perception of local structures through dynamically generated, multi-scale convolutional kernels; 2) a Hierarchical Modulated Fusion Module, which dynamically aggregates multi-level encoder features to preserve fine-grained spatial details; and 3) a Boundary-Guided Mask Refinement, which integrates multi-context boundary cues with semantic features through explicit supervision, producing a boundary-focused signal to refine initial mask predictions for sharper delineation. These three components work cooperatively to enhance local perception, preserve spatial details, and refine boundaries, enabling SAM to perform accurate nuclei instance segmentation directly.

---

## 论文详细总结（自动生成）

这是一份关于论文《Adapting SAM to Nuclei Instance Segmentation and Classification via Cooperative Fine-Grained Refinement》的深度结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何将强大的基础模型 SAM（Segment Anything Model）高效且精准地应用于病理图像中的细胞核实例分割与分类。
*   **研究背景**：细胞核分割是癌症诊断的基础。虽然 SAM 具备强大的全局建模能力，但直接应用存在三大瓶颈：
    1.  **局部感知不足**：SAM 的 Transformer 架构对细胞核这种微小、密集的局部结构特征（如边界、纹理）不敏感。
    2.  **微调成本高**：全量微调 SAM 参数量巨大，且在小规模医学数据集上容易过拟合。
    3.  **后处理依赖**：现有方法多依赖复杂的后处理（如分水岭算法）来分离实例，流程繁琐且易出错。
*   **整体含义**：提出 **CFR-SAM** 框架，通过参数高效微调（PEFT）策略，在冻结 SAM 主干的同时，引入协同的细粒度细化模块，实现端到端的细胞核分割。

### 2. 论文提出的方法论
该框架分为两个阶段：**自动提示生成（Prompt Learning）**和**SAM 适配（SAM Adaptation）**。

*   **第一阶段：Prompt Learner**
    *   使用轻量级卷积网络预测细胞核的质心坐标和类别标签。
    *   通过匈牙利算法（Hungarian Algorithm）进行标签分配，将预测点作为第二阶段的“点提示（Point Prompts）”。
*   **第二阶段：CFR-SAM 核心组件**
    1.  **MALA (多尺度自适应局部感知适配器)**：在冻结的 Transformer 块中插入。它不只是简单的线性变换，而是根据输入特征动态生成多尺度卷积核（3x3, 5x5, 7x7），在低秩空间内增强模型对不同大小细胞核的局部感知。
    2.  **HMFM (分层调制融合模块)**：针对 SAM 掩码解码器忽略浅层特征的问题，利用通道注意力机制动态聚合编码器不同层级的特征，确保细粒度的空间细节（如形状、边缘）参与解码。
    3.  **BGMR (边界引导掩码细化)**：
        *   **MCBE (多上下文边界增强)**：从原始图像提取多尺度边缘线索。
        *   **BGFF (边界引导特征融合)**：通过显式的边界分割任务（Boundary Loss）监督，将语义特征与边缘线索对齐，细化掩码边缘，解决 SAM 上采样导致的边界模糊问题。

### 3. 实验设计
*   **数据集**：
    1.  **PanNuke**：包含19种组织类型、5类细胞核的大规模数据集（最主要的 Benchmark）。
    2.  **CPM-17**：用于验证泛化能力的细胞核分割数据集。
    3.  **MoNuSeg**：多器官细胞核分割数据集。
*   **对比方法**：包括经典的 Mask R-CNN、针对细胞核设计的 HoVer-Net、CPP-Net、以及同样基于 ViT 的 SOTA 模型 CellViT。
*   **评价指标**：二值全景质量（bPQ）、多类全景质量（mPQ）、调整兰德指数（AJI）、F1-score 等。

### 4. 资源与算力
*   **硬件平台**：单张 **NVIDIA RTX 3090 GPU**。
*   **训练配置**：
    *   使用 Adam 优化器，初始学习率为 $1 \times 10^{-4}$。
    *   第一阶段训练 200-600 epochs（视数据集大小而定）。
    *   第二阶段统一训练 200 epochs，Batch Size 为 2。
    *   为了节省显存，训练时每张图随机采样最多 25 个细胞核。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在三个主流数据集上进行了完整的性能对比。
    *   针对 PanNuke 进行了 **5次独立运行** 并报告标准差，确保结果的稳定性。
    *   **消融实验非常详尽**：涵盖了组件有效性分析、不同微调策略（FFT vs. Adapter vs. LoRA vs. MALA）、低秩维度选择、采样数量影响、以及 Prompt 类型（点 vs. 框）的对比。
*   **充分性评价**：实验设计客观、公平。通过与全量微调（FFT）的 SAM 对比，证明了 PEFT 策略在医学领域的优越性。

### 6. 论文的主要结论与发现
*   **性能领先**：在 PanNuke 上，CFR-SAM-H 在 mPQ 和 bPQ 指标上均刷新了 SOTA 记录。
*   **参数高效**：仅需训练约 **10.6%** 的参数（74.2M vs 699.7M），性能却优于全量微调的 CellViT-H。
*   **局部增强的必要性**：实验证明，单纯引入 Adapter 或 LoRA 对细胞核分割提升有限，而 MALA 引入的动态卷积局部感知是性能突破的关键。
*   **端到端优势**：无需复杂的后处理即可获得平滑、准确的边界，尤其在处理密集堆叠的细胞核时表现优异。

### 7. 优点（亮点）
*   **协同设计**：MALA、HMFM 和 BGMR 分别从特征提取、特征融合、结果细化三个阶段针对性地解决了 SAM 的短板。
*   **动态性**：MALA 模块能够根据图像内容自适应调整感受野，适应了病理图像中细胞核形态的高度多样性。
*   **参数与性能的平衡**：在保持基础模型强大泛化能力的同时，极大地降低了微调门槛。

### 8. 不足与局限
*   **稀有类别表现受限**：在 PanNuke 的“死亡细胞（Dead cells）”类别上 F1 分数较低（45%），这主要受限于数据集本身的类别不平衡。
*   **提示依赖性**：第二阶段的分割质量高度依赖第一阶段质心预测的准确性。在极度拥挤的区域，若质心预测缺失或偏移，会导致漏检或误检。
*   **推理速度**：虽然参数高效，但由于引入了多尺度卷积和细化模块，其推理速度（FPS）略低于一些轻量级专用网络（如 CellViT-B），在超大规模 WSI 扫描图上的实时性仍有提升空间。

（完）
