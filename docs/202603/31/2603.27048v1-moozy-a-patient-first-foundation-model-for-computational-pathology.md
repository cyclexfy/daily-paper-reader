---
title: "MOOZY: A Patient-First Foundation Model for Computational Pathology"
title_zh: MOOZY：一种以患者为中心的计算病理学基础模型
authors: "Yousef Kotp, Vincent Quoc-Huy Trinh, Christopher Pal, Mahdi S. Hosseini"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.27048v1"
tags: ["query:cpath"]
score: 10.0
evidence: 用于计算病理学和全切片图像的基座模型
tldr: MOOZY是一个以患者为核心的病理学基础模型，旨在解决现有模型侧重单张切片且忽视患者多切片关联的问题。该模型通过两阶段预训练：首先在公开切片数据上进行自监督视觉编码，随后利用病例转换器在333个临床任务上进行多任务监督对齐。MOOZY在多项任务中表现优于TITAN和PRISM，且参数量仅为GigaPath的1/14，证明了开源、可复现的患者级预训练在计算病理学中的高效性。
selection_source: fresh_fetch
motivation: 现有的病理基础模型多以单张切片为中心，且依赖私有数据或昂贵的报告标注，忽视了同一患者多张切片间的内在联系。
method: 采用两阶段预训练，先通过掩码自蒸馏训练视觉编码器，再利用病例转换器在56个公开数据集的333个分类和生存任务上实现患者级语义对齐。
result: 在多个独立测试任务中，MOOZY的性能显著优于TITAN和PRISM等模型，且参数量仅为85.77M，具备极高的参数效率。
conclusion: 研究证明了基于公开数据的患者级预训练能够产生极具迁移性的表征，为构建可扩展、以患者为中心的病理基础模型提供了可行路径。
---

## 摘要
计算病理学需要能够跨多种临床任务迁移的全切片图像（WSI）基础模型，然而目前的方法在很大程度上仍以切片为中心，通常依赖于私有数据和昂贵的配对报告监督，且未能显式建模同一患者多张切片之间的关系。我们提出了 MOOZY，这是一种以患者为中心的病理学基础模型，其中患者病例（而非单个切片）是表示的核心单元。MOOZY 在预训练期间通过病例 Transformer 显式建模同一患者所有切片之间的依赖关系，并将多阶段开放自监督与规模化的低成本任务监督相结合。在第一阶段，我们使用掩码自蒸馏在 77,134 个公开切片特征网格上预训练了一个纯视觉切片编码器。在第二阶段，我们利用病例 Transformer 和针对来自 56 个公开数据集的 333 个任务（包括跨四个终点的 205 个分类任务和 128 个生存任务）的多任务监督，将这些表示与临床语义进行对齐。在八个留出任务的五折冻结特征探针评估中，MOOZY 在大多数指标上达到了最优或并列最优的性能；在加权 F1、加权 ROC-AUC 和平衡准确率方面，其宏平均值分别比 TITAN 提高了 +7.37%、+5.50% 和 +7.83%，比 PRISM 提高了 +8.83%、+10.70% 和 +9.78%。MOOZY 还具有参数效率，拥有 85.77M 参数，比 GigaPath 小 14 倍。这些结果表明，开放、可重复的患者级预训练可以产生具有迁移性的嵌入，为实现可扩展的以患者为中心的组织病理学基础模型提供了一条切实可行的路径。

## Abstract
Computational pathology needs whole-slide image (WSI) foundation models that transfer across diverse clinical tasks, yet current approaches remain largely slide-centric, often depend on private data and expensive paired-report supervision, and do not explicitly model relationships among multiple slides from the same patient. We present MOOZY, a patient-first pathology foundation model in which the patient case, not the individual slide, is the core unit of representation. MOOZY explicitly models dependencies across all slides from the same patient via a case transformer during pretraining, combining multi-stage open self-supervision with scaled low-cost task supervision. In Stage 1, we pretrain a vision-only slide encoder on 77,134 public slide feature grids using masked self-distillation. In Stage 2, we align these representations with clinical semantics using a case transformer and multi-task supervision over 333 tasks from 56 public datasets, including 205 classification and 128 survival tasks across four endpoints. Across eight held-out tasks with five-fold frozen-feature probe evaluation, MOOZY achieves best or tied-best performance on most metrics and improves macro averages over TITAN by +7.37%, +5.50%, and +7.83% and over PRISM by +8.83%, +10.70%, and +9.78% for weighted F1, weighted ROC-AUC, and balanced accuracy, respectively. MOOZY is also parameter efficient with 85.77M parameters, 14x smaller than GigaPath. These results demonstrate that open, reproducible patient-level pretraining yields transferable embeddings, providing a practical path toward scalable patient-first histopathology foundation models.

---

## 论文详细总结（自动生成）

### MOOZY：以患者为中心的计算病理学基础模型论文总结

#### 1. 核心问题与研究动机
*   **核心问题**：当前的计算病理学基础模型大多是“以切片为中心”（Slide-centric）的，忽视了临床诊断中同一患者往往包含多张切片（如不同组织块、不同染色）的实际情况。此外，现有高性能模型往往依赖私有数据、昂贵的配对报告监督，且模型参数量巨大，难以复现和扩展。
*   **研究动机**：构建一个**以患者为中心**（Patient-first）、完全基于公开数据、参数高效且可复现的基础模型。通过显式建模同一患者多张切片间的依赖关系，提升模型在多样化临床任务中的迁移能力。

#### 2. 方法论
MOOZY 采用了**两阶段预训练框架**：
*   **第一阶段：自监督切片编码预训练（Stage 1）**
    *   **核心思想**：在无标注的公开全切片图像（WSI）上学习通用的空间表征。
    *   **技术细节**：使用掩码自蒸馏（Masked Self-distillation）算法。将切片划分为特征网格，采用多尺度裁剪（Global/Local crops）和块状掩码（Block masking）策略。
    *   **空间建模**：引入 2D ALiBi（线性偏置注意力）来编码切片的空间结构，而无需学习位置嵌入。
*   **第二阶段：患者感知语义对齐（Stage 2）**
    *   **核心思想**：通过大规模多任务监督，将视觉表征转向临床语义，并引入患者级聚合。
    *   **关键技术**：设计了一个**病例转换器（Case Transformer）**。它将同一患者的所有切片嵌入作为输入，通过注意力机制显式建模切片间的依赖关系，生成统一的患者级表示。
    *   **多任务学习**：同时在 333 个任务上进行训练，涵盖分类和四种生存终点（OS, DSS, DFI, PFI），通过掩码损失函数处理稀疏标注。

#### 3. 实验设计
*   **数据集**：整合了 56 个公开数据集（如 TCGA, CPTAC, PANDA 等），涵盖 23 个解剖部位。
    *   Stage 1 使用了 77,134 张切片。
    *   Stage 2 使用了 30,024 名患者的 45,179 张切片。
*   **评估场景**：在 8 个完全留出的（Held-out）任务上进行评估，包括基因突变预测（TP53, KRAS 等）、组织学分级和治疗反应预测。
*   **对比方法**：
    *   **切片编码器**：CHIEF, GigaPath, PRISM, Madeleine, TITAN。
    *   **MIL 基准**：对比了基于 UNI, Phikon, CONCH 等特征提取器的多种多实例学习（MIL）架构（如 ABMIL, TransMIL）。
*   **评估协议**：采用五折交叉验证的**冻结特征探针（Frozen-feature Probe）**，确保评估的客观性。

#### 4. 资源与算力
*   **算力配置**：使用了 8 张 GPU 进行训练。
*   **训练时长**：
    *   **Stage 1**：约 436 GPU 小时（200 个 Epoch）。
    *   **Stage 2**：约 307 GPU 小时（12 个 Epoch）。
*   **总计**：约 743 GPU 小时。论文提到资源由 Calcul Québec 和加拿大数字研究联盟提供。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的实验，包括 333 个预训练任务和 8 个独立验证任务。
*   **消融实验**：针对 Stage 1/Stage 2 的必要性、病例转换器（Case Aggregator）对比平均池化（Mean Pooling）的效果进行了深入分析。
*   **充分性与公平性**：实验覆盖了多种器官和临床终点；通过统一的冻结特征探针协议对比了当前最先进的（SOTA）模型，且所有对比均在相同的留出任务上进行，实验设计严谨、客观。

#### 6. 主要结论与发现
*   **性能卓越**：MOOZY 在大多数指标上达到最优或并列最优。相比 TITAN 和 PRISM，其宏平均加权 F1 分别提升了 7.37% 和 8.83%。
*   **参数高效**：MOOZY 总参数量仅为 **85.77M**，比 GigaPath（1.2B）小 14 倍，比 PRISM 小 8 倍，证明了“切片级建模”比单纯堆叠“切片内特征提取”参数更有效。
*   **患者级建模的价值**：消融实验证实，显式建模多切片依赖关系（Case Transformer）显著优于简单的平均融合，尤其在复杂的临床病例中。
*   **公开数据的潜力**：证明了无需私有数据或昂贵的配对报告，仅靠公开数据和合理的两阶段设计即可构建顶级基础模型。

#### 7. 优点（亮点）
*   **开源与可复现**：提供了完整的训练配方、代码和公开数据处理流程，打破了病理基础模型领域的“黑盒”现状。
*   **架构创新**：首次在预训练阶段引入显式的患者级（Multi-slide）依赖建模。
*   **注意力平衡**：病理学家评估显示，MOOZY 的注意力图在肿瘤和周围组织间分布更平衡，语义鸿沟（Semantic Gap）最小。

#### 8. 不足与局限
*   **切片数量限制**：虽然支持多切片，但在 Stage 2 训练中为了显存效率对 Token 进行了采样（Adaptive Capping），可能损失极微小的局部细节。
*   **任务覆盖**：虽然涵盖了分类和生存分析，但尚未扩展到切片级分割或生成式任务（如报告生成）。
*   **外部验证**：虽然在 8 个留出任务上表现良好，但仍需在更多完全独立的外部临床队列（非公开数据集）中进一步验证其鲁棒性。

（完）
