---
title: Towards Interpretable Foundation Models for Retinal Fundus Images
title_zh: 迈向视网膜眼底图像的可解释基础模型
authors: "Samuel Ofosu Mensah, Maria Camila Roa Carvajal, Kerol Djoumessi, Philipp Berens"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18846v1"
tags: ["query:cpath"]
score: 8.0
evidence: 用于医学影像的可解释基础模型
tldr: 针对医疗影像领域基础模型缺乏解释性的问题，本文提出了Dual-IFM，一种专为视网膜眼底图像设计的可解释基础模型。该模型通过自监督学习在80万张眼底图上预训练，具备局部（类别证据图）和全局（2D投影层）双重解释性。实验表明，Dual-IFM在保持与SOTA模型相当性能的同时，参数量减少了16倍，并能为分布外数据提供可靠的解释。
selection_source: fresh_fetch
motivation: 现有的眼底图像基础模型通常缺乏解释性，这在医疗等高风险领域是一个关键缺陷。
method: 提出Dual-IFM模型，通过类别证据图实现局部解释，并通过2D投影层实现数据集层面的全局可视化。
result: 在80万张图像上预训练后，该模型以极小的参数量达到了与SOTA模型相近的性能，并具备良好的泛化性。
conclusion: 大规模自监督预训练结合固有可解释性，可以为视网膜影像构建鲁棒且透明的表征模型。
---

## 摘要
基础模型通常通过自监督学习（SSL）从大量无标签数据中提取可迁移的表征。然而，许多此类模型依赖于解释性有限的架构，这在医疗成像等高风险领域是一个关键问题。我们提出了 Dual-IFM，这是一种在设计上具有双重可解释性的基础模型：首先，它通过忠实于决策过程的类别证据图为单个图像提供局部可解释性；其次，它通过一个 2D 投影层为整个数据集提供全局可解释性，从而实现模型表征空间的直接可视化。我们在来自不同来源的超过 800,000 张彩色眼底摄影图像上训练了模型，以学习适用于不同下游任务的泛化且可解释的表征。结果表明，该模型的性能达到了与参数量高达其 16 倍的先进基础模型相近的水平，同时还能对分布外数据提供可解释的预测。我们的研究结果表明，大规模 SSL 预训练与内在可解释性相结合，可以为视网膜成像生成鲁棒的表征。

## Abstract
Foundation models are used to extract transferable representations from large amounts of unlabeled data, typically via self-supervised learning (SSL). However, many of these models rely on architectures that offer limited interpretability, which is a critical issue in high-stakes domains such as medical imaging. We propose Dual-IFM, a foundation model that is interpretable-by-design in two ways: First, it provides local interpretability for individual images through class evidence maps that are faithful to the decision-making process. Second, it provides global interpretability for entire datasets through a 2D projection layer that allows for direct visualization of the model's representation space. We trained our model on over 800,000 color fundus photography from various sources to learn generalizable, interpretable representations for different downstream tasks. Our results show that our model reaches a performance range similar to that of state-of-the-art foundation models with up to $16\times$ the number of parameters, while providing interpretable predictions on out-of-distribution data. Our results suggest that large-scale SSL pretraining paired with inherent interpretability can lead to robust representations for retinal imaging.

---

## 论文详细总结（自动生成）

这是一份关于论文《Towards Interpretable Foundation Models for Retinal Fundus Images》（迈向视网膜眼底图像的可解释基础模型）的结构化分析报告：

### 1. 核心问题与研究动机
*   **核心问题**：现有的医疗影像基础模型（如 RETFound）虽然性能强大，但通常是“黑盒”模型，缺乏**固有可解释性（Inherent Interpretability）**。
*   **研究动机**：
    *   **局部解释不可靠**：常用的事后解释方法（如热力图）往往不忠实于模型的实际决策过程。
    *   **全局结构难观测**：高维表征空间难以直观理解，且传统的降维可视化方法（如 t-SNE）是非参数化的，每次新增数据都需要重新计算。
    *   **高风险领域需求**：在眼科等医疗决策中，模型不仅要预测准确，还必须让医生理解“为什么”以及“模型学到了什么”。

### 2. 方法论
论文提出了 **Dual-IFM**，一个在设计上兼顾局部和全局可解释性的基础模型：
*   **核心架构**：采用 **BagNet** 作为骨干网络。BagNet 通过限制感受野，强制模型基于局部图像块（patches）的线性组合进行预测，从而实现天然的局部可解释性。
*   **全局解释技术（t-SimCNE）**：
    *   在自监督对比学习中引入一个**参数化的 2D 投影层**。
    *   **三阶段训练**：1) 标准对比学习（128维）；2) 维度退火（降至2维）；3) 全模型微调。
    *   **效果**：模型可以直接将新图像映射到 2D 平面上，无需额外降维即可可视化整个数据集的语义结构。
*   **局部解释技术（类别证据图）**：
    *   在编码器后添加 1×1 卷积层生成类别激活图，通过全局平均池化（GAP）得出预测分。
    *   引入**稀疏性约束**，使模型关注更具代表性的病灶区域。

### 3. 实验设计
*   **预训练数据集**：整合了 EyePACS、AREDS 和 UKBiobank，共计 **802,360 张**彩色眼底摄影（CFP）图像。
*   **下游任务（分布外测试）**：
    *   **糖尿病视网膜病变（DR）分级**：APTOS, IDRiD, DeepDRiD, Messidor-2。
    *   **青光眼分级**：Glaucoma fundus, PAPILA。
    *   **多疾病分类**：FIVES。
*   **基准模型（Benchmarks）**：
    *   ImageNet 预训练的 BagNet-33。
    *   标准 SimCLR 训练的 BagNet-33。
    *   **RETFound**（目前最广泛使用的眼科基础模型，基于 ViT-Large）。

### 4. 资源与算力
*   **预训练算力**：使用了 **8 台 NVIDIA H100 GPU**。
*   **训练时长**：预训练耗时 **5 天 11 小时**（约 131 小时）。
*   **微调算力**：在 1 台 NVIDIA V100 GPU 上完成。
*   **模型规模**：Dual-IFM 约 **18.3M 参数**，而对比的 RETFound 拥有 **303M 参数**（Dual-IFM 参数量仅为后者的 1/16）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 7 个独立的外部数据集，验证了模型在不同疾病和不同来源数据上的泛化能力。
*   **评估指标**：采用了线性探测（Linear Probing）和全模型微调（Fine-tuning）两种评价方式，并使用 AUROC 衡量性能。
*   **客观性**：实验采用了 **5 折交叉验证**，并在患者水平（Participant-level）进行分层划分，避免了数据泄露，实验设计较为严谨、公平。

### 6. 主要结论与发现
*   **性能对等**：Dual-IFM 在参数量极小的情况下，性能达到了与 SOTA 模型（RETFound）相当的水平，甚至在部分任务上有所超越。
*   **推理高效**：由于参数量少，Dual-IFM 的推理速度（157ms）比 RETFound（322ms）快一倍。
*   **解释性优势**：
    *   **全局**：2D 投影层成功展示了疾病从健康到严重的连续演变轨迹。
    *   **局部**：生成的证据图在病灶定位精度上（0.674）显著优于 RETFound 的 LRP 解释方法（0.384）。

### 7. 优点
*   **固有可解释性**：不同于事后解释，该模型的解释是其架构的一部分，更加“忠实”于决策逻辑。
*   **参数效率极高**：证明了在医疗特定领域，精心设计的轻量化 CNN 架构配合大规模 SSL 预训练，可以挑战超大规模的 Transformer 模型。
*   **临床友好**：2D 可视化有助于医生识别“边界病例”，局部证据图有助于辅助诊断。

### 8. 不足与局限
*   **模态局限**：目前仅针对彩色眼底图像（CFP），未涵盖 OCT 等其他重要眼科模态。
*   **超参数优化**：微调阶段未针对每个数据集进行系统的超参数搜索，可能限制了模型在某些特定任务上的上限。
*   **架构限制**：BagNet 虽然可解释性强，但其受限的感受野在处理需要全局上下文的复杂病变时可能存在瓶颈。

（完）
