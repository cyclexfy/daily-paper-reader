---
title: "CFCML: A Coarse-to-Fine Crossmodal Learning Framework For Disease Diagnosis Using Multimodal Images and Tabular Data"
title_zh: CFCML：一种利用多模态图像和表格数据进行疾病诊断的由粗到精跨模态学习框架
authors: "Tianling Liu, Hongying Liu, Fanhua Shang, Lequan Yu, Tong Han, Liang Wan"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.20016v1"
tags: ["query:cpath"]
score: 8.0
evidence: 用于疾病诊断的跨模态学习
tldr: 本研究针对临床诊断中医学影像与表格数据间的模态鸿沟问题，提出了一种由粗到精的跨模态学习框架（CFCML）。该框架在粗粒度阶段结合多粒度图像特征与表格信息，在细粒度阶段利用类别感知原型和分层锚点关系挖掘策略，深入探索模态间联系并提取判别性信息。实验证明，该方法在多个数据集上显著优于现有技术，为多模态疾病诊断提供了更精准的解决方案。
selection_source: fresh_fetch
motivation: 现有跨模态学习方法往往忽略了图像的局部细节和任务相关信息，导致医学影像与表格数据之间存在难以消除的模态差异。
method: 提出CFCML框架，通过多粒度特征融合的粗阶段和基于原型及分层锚点关系挖掘的细阶段，逐步缩小模态差距并增强类别区分度。
result: "在MEN和Derm7pt数据集上，该方法的AUC指标分别比当前最先进方法提升了1.53%和0.91%。"
conclusion: CFCML通过由粗到精的策略有效整合了多模态信息，显著提升了复杂临床场景下的疾病诊断准确性。
---

## 摘要
在临床实践中，包括医学图像和表格数据在内的跨模态信息对于疾病诊断至关重要。这些数据类型之间存在显著的模态鸿沟，阻碍了跨模态诊断准确性的提升。大多数现有的跨模态学习（CML）方法主要关注探索高层编码器输出之间的关系，导致忽略了图像中的局部信息。此外，这些方法往往忽视了任务相关信息的提取。在本文中，我们提出了一种新颖的由粗到精跨模态学习（CFCML）框架，通过深入探索模态间关系，逐步缩小多模态图像与表格数据之间的模态鸿沟。在粗粒度阶段，我们探索了来自不同图像编码器阶段的多粒度特征与表格信息之间的关系，促进了模态鸿沟的初步缩小。在细粒度阶段，我们生成了包含类别感知信息的单模态和跨模态原型，并建立了基于层次锚点的关系挖掘（HRM）策略，以进一步缩小模态鸿沟并提取具有判别性的跨模态信息。该策略利用模态样本、单模态原型和跨模态原型作为锚点来开发对比学习方法，从多个角度有效增强类间差异，同时减少类内差异。实验结果表明，我们的方法优于现有最先进（SOTA）的方法，在 MEN 和 Derm7pt 数据集上的 AUC 指标分别提高了 1.53% 和 0.91%。代码可在 https://github.com/IsDling/CFCML 获取。

## Abstract
In clinical practice, crossmodal information including medical images and tabular data is essential for disease diagnosis. There exists a significant modality gap between these data types, which obstructs advancements in crossmodal diagnostic accuracy. Most existing crossmodal learning (CML) methods primarily focus on exploring relationships among high-level encoder outputs, leading to the neglect of local information in images. Additionally, these methods often overlook the extraction of task-relevant information. In this paper, we propose a novel coarse-to-fine crossmodal learning (CFCML) framework to progressively reduce the modality gap between multimodal images and tabular data, by thoroughly exploring inter-modal relationships. At the coarse stage, we explore the relationships between multi-granularity features from various image encoder stages and tabular information, facilitating a preliminary reduction of the modality gap. At the fine stage, we generate unimodal and crossmodal prototypes that incorporate class-aware information, and establish hierarchical anchor-based relationship mining (HRM) strategy to further diminish the modality gap and extract discriminative crossmodal information. This strategy utilize modality samples, unimodal prototypes, and crossmodal prototypes as anchors to develop contrastive learning approaches, effectively enhancing inter-class disparity while reducing intra-class disparity from multiple perspectives. Experimental results indicate that our method outperforms the state-of-the-art (SOTA) methods, achieving improvements of 1.53% and 0.91% in AUC metrics on the MEN and Derm7pt datasets, respectively. The code is available at https://github.com/IsDling/CFCML.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《CFCML: A Coarse-to-Fine Crossmodal Learning Framework For Disease Diagnosis Using Multimodal Images and Tabular Data》** 的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
在临床诊断中，医生通常需要结合医学影像（如MRI、皮肤镜图像）和临床表格数据（如年龄、病史、病灶描述）进行综合判断。然而，这两类数据之间存在巨大的**模态鸿沟（Modality Gap）**。
*   **现有挑战**：目前的跨模态学习（CML）方法大多只关注编码器输出的高层全局特征，忽略了图像中的局部细节（如病灶微小特征）；同时，这些方法往往缺乏对任务相关（类别感知）信息的针对性提取，导致融合特征中包含大量冗余噪声。
*   **研究目标**：提出一种由粗到精的框架，通过多粒度特征交互和类别感知关系挖掘，逐步缩小模态差距，提升疾病诊断的准确性。

### 2. 论文提出的方法论
论文提出了 **CFCML（由粗到精跨模态学习）** 框架，主要包含两个阶段：
*   **粗粒度阶段：多粒度跨模态信息增强（MG-CIE）模块**
    *   **特征提取**：使用 nnMamba（3D图像）或 Swin Transformer（2D图像）提取四个不同阶段的图像特征；使用预训练的 **CLIP Text Encoder** 将表格属性转换为文本嵌入。
    *   **Token 映射**：针对图像 Token 远多于表格 Token 的不平衡问题，通过适配器将两者映射到预定义的 Token 数量，过滤冗余。
    *   **多粒度交互**：在图像编码器的每个阶段，利用交叉注意力机制（Cross-Attention）让图像特征与表格特征互补增强，初步缩小模态差距。
*   **细粒度阶段：类别感知跨模态关系挖掘（CCRM）策略**
    *   **原型构建**：基于类别标签生成“单模态原型”和“跨模态原型”。
    *   **分层锚点对比学习**：设计了三种对比学习策略：
        1.  **样本锚点**：拉近样本与同类原型的距离，推开异类样本。
        2.  **单模态原型锚点**：在单模态内部增强类别区分度。
        3.  **跨模态原型锚点**：以跨模态原型为核心，打破模态边界，提取判别性强的类别特征。

### 3. 实验设计
*   **数据集**：
    1.  **MEN 数据集**（私有）：包含 796 名脑膜瘤患者的 3D MRI 序列（T1C, FLAIR-C, ADC）及 6 项临床属性，任务是三分类分级。
    2.  **Derm7pt 数据集**（公开）：包含 827 例皮肤病变图像（临床图、皮肤镜图）及 5 项临床属性，任务是黑色素瘤与痣的二分类。
*   **对比方法（Benchmark）**：对比了 6 种 SOTA 方法，包括基于不确定性的（ETMC, CCML）、基于特征解耦的（DMD）和基于注意力机制的（MVCNet, GLoMo, MRIM）。

### 4. 资源与算力
*   **硬件环境**：使用单张 **NVIDIA GeForce RTX 3090 GPU**。
*   **软件环境**：Python 3.10, PyTorch 2.1。
*   **训练细节**：训练 50 个 epoch，采用 Adam 优化器，初始学习率在 1e-4 到 5e-4 之间，应用了线性预热（Warm-up）和学习率衰减策略。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个不同维度（2D 和 3D）的数据集上进行了验证。
    *   **MEN 数据集**采用 3 折交叉验证；**Derm7pt 数据集**进行了 3 次不同随机种子的重复实验。
    *   **消融实验**：分别验证了 MG-CIE 模块和 CCRM 策略的有效性，并对比了单粒度与多粒度的差异。
    *   **可视化分析**：提供了 t-SNE 聚类、Grad-CAM 激活图和 MDA 特征空间分布图。
*   **评价**：实验设计较为充分，涵盖了性能对比、消融分析、超参数讨论（如 Token 数量映射）和可解释性分析，结果具有较强的说服力。

### 6. 论文的主要结论与发现
*   **性能提升**：CFCML 在 MEN 数据集上 AUC 达到 98.57%（提升 1.53%），在 Derm7pt 上 AUC 达到 90.52%（提升 0.91%）。
*   **多粒度的重要性**：实验证明，结合图像编码器中层和底层的特征（局部细节）比仅使用高层特征更能有效对齐表格数据。
*   **类别感知的价值**：通过引入原型对比学习，模型能够显著减少类内差异并扩大类间边界，使融合后的特征更具判别力。
*   **CLIP 的优势**：使用预训练的 CLIP 提取表格特征优于传统的 MLP 嵌入。

### 7. 优点
*   **由粗到精的逻辑清晰**：从多粒度特征初步对齐到类别感知深度挖掘，符合临床诊断的逻辑。
*   **解决 Token 不平衡**：通过 Token Mapping 解决了图像与表格数据在 Transformer 架构中信息量不对等的问题。
*   **可解释性强**：通过 Grad-CAM 证明了模型能够准确聚焦于病灶区域（如肿瘤和水肿区）。

### 8. 不足与局限
*   **计算复杂度**：由于引入了多粒度的跨模态交互，模型的参数量和计算量（GFLOPs）相比部分单粒度方法有所增加。
*   **超参数敏感性**：Token 映射的数量（$n_x, n_t$）需要针对不同数据集进行手动调优，缺乏完全自动化的机制。
*   **数据依赖**：虽然在公开数据集上表现良好，但核心实验之一基于私有数据集（MEN），其他研究者难以完全复现该数据集上的结果。

（完）
