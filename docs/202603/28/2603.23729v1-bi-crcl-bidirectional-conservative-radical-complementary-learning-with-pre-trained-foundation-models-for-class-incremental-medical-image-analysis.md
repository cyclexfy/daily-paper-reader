---
title: "Bi-CRCL: Bidirectional Conservative-Radical Complementary Learning with Pre-trained Foundation Models for Class-incremental Medical Image Analysis"
title_zh: Bi-CRCL：基于预训练基础模型的双向保守-激进互补学习，用于类增量医学图像分析
authors: "Xinyao Wu, Zhe Xu, Cheng Chen, Jiawei Ma, Yefeng Zheng, Raymond Kai-yu Tong"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23729v1"
tags: ["query:cpath"]
score: 8.0
evidence: 利用预训练基础模型进行类增量医学图像分析
tldr: 针对医疗影像类增量学习中数据异构和隐私限制导致的灾难性遗忘问题，本文提出了一种基于预训练大模型的双向保守-激进互补学习框架（Bi-CRCL）。该框架通过保守学习器保持旧知识稳定性，激进学习器捕捉新类别塑性，并利用双向交互机制实现知识的持续整合与前向/后向迁移，在推理阶段通过自适应融合提升预测鲁棒性，为临床部署提供了可扩展的方案。
selection_source: fresh_fetch
motivation: 医疗影像增量学习需在无记忆回放的情况下平衡旧知识保留与新疾病适应，而现有方法难以应对复杂的解剖结构和机构间异构性。
method: 提出Bi-CRCL框架，利用保守和激进双学习器的双向交互与自适应融合，实现知识的稳定保留与快速更新。
result: 在五个医疗影像数据集上的实验证明，Bi-CRCL在多种任务配置和跨数据集偏移场景下均显著优于现有最先进方法。
conclusion: 该研究通过模拟互补学习系统，为医疗影像领域的类增量学习提供了一种高效且具备鲁棒性的新方案。
---

## 摘要
医学影像引导诊断中的类增量学习（CIL）要求在适应新出现的疾病类别的同时保留先前的诊断知识，这对于可扩展的临床部署至关重要。由于异构数据和防止记忆回放（memory replay）的隐私限制，这一问题尤其具有挑战性。尽管预训练基础模型（PFMs）推动了通用领域 CIL 的发展，但它们在医学影像中的潜力仍有待挖掘，由于解剖结构的复杂性和机构间的异构性，领域特定的适配虽然必不可少但却十分困难。为了填补这一空白，我们对近期基于 PFM 的 CIL 方法进行了系统的基准测试，并提出了双向保守-激进互补学习（Bi-CRCL），这是一个受互补学习系统启发的双学习器框架。Bi-CRCL 集成了一个通过面向稳定性的更新来保留先验知识的保守学习器，以及一个通过面向可塑性的学习来快速适应新类别的激进学习器。双向交互机制实现了正向迁移和反向巩固，在减轻灾难性遗忘的同时，允许新知识的持续整合。在推理过程中，来自两个学习器的输出被自适应融合，以实现稳健的预测。在五个医学影像数据集上的实验表明，在包括跨数据集偏移和不同任务配置在内的多种设置下，该方法相较于现有最先进方法具有持续的改进。

## Abstract
Class-incremental learning (CIL) in medical image-guided diagnosis requires retaining prior diagnostic knowledge while adapting to newly emerging disease categories, which is critical for scalable clinical deployment. This problem is particularly challenging due to heterogeneous data and privacy constraints that prevent memory replay. Although pretrained foundation models (PFMs) have advanced general-domain CIL, their potential in medical imaging remains underexplored, where domain-specific adaptation is essential yet difficult due to anatomical complexity and inter-institutional heterogeneity. To address this gap, we conduct a systematic benchmark of recent PFM-based CIL methods and propose Bidirectional Conservative-Radical Complementary Learning (Bi-CRCL), a dual-learner framework inspired by complementary learning systems. Bi-CRCL integrates a conservative learner that preserves prior knowledge through stability-oriented updates and a radical learner that rapidly adapts to new categories via plasticity-oriented learning. A bidirectional interaction mechanism enables forward transfer and backward consolidation, allowing continual integration of new knowledge while mitigating catastrophic forgetting. During inference, outputs from both learners are adaptively fused for robust predictions. Experiments on five medical imaging datasets demonstrate consistent improvements over state-of-the-art methods under diverse settings, including cross-dataset shifts and varying task configurations.

---

## 论文详细总结（自动生成）

以下是对论文《Bi-CRCL: Bidirectional Conservative-Radical Complementary Learning with Pre-trained Foundation Models for Class-incremental Medical Image Analysis》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：医学影像诊断模型在临床部署中需要不断学习新出现的疾病类别（类增量学习，CIL），但传统方法容易出现“灾难性遗忘”，即在学习新知识时覆盖旧知识。
*   **研究动机**：
    *   **临床限制**：医学数据具有高度异构性，且受隐私保护限制，通常无法存储历史病人数据进行“记忆回放”（Replay-free）。
    *   **范式转换**：传统CIL多从零训练（如教婴儿），而利用预训练基础模型（PFM）则如同引导成年专家，具有更强的表示能力。
    *   **现有不足**：通用领域的PFM在医学影像中存在领域偏差，且现有的增量学习方法在处理医学影像的低类间差异和高领域异构性时表现不佳。

### 2. 方法论
论文提出了**Bi-CRCL**框架，其核心思想是模拟人脑的“互补学习系统”（CLS），通过两个协同的PFM学习器平衡**稳定性**与**塑性**：
*   **双学习器架构**：
    1.  **保守学习器（Conservative Learner）**：类似大脑皮层，通过稳定性导向的更新保留长期诊断知识。
    2.  **激进学习器（Radical Learner）**：类似海马体，通过塑性导向的适配快速获取新疾病概念。
*   **关键技术细节**：
    *   **初始化领域对齐**：在首个任务中通过轻量级适配器（Adapter）微调，将通用PFM对齐到医学领域。
    *   **双向知识交互**：
        *   **正向迁移**：新任务开始前，用保守学习器的权重初始化激进学习器。
        *   **反向巩固**：任务完成后，通过指数移动平均（EMA）将激进学习器的更新整合回保守学习器。
    *   **双学习器交叉分类对齐**：引入正则化损失，确保两个学习器的特征表示在决策边界上保持一致。
    *   **解析分类器（Analytical Classifier）**：采用岭回归（Ridge Regression）的闭式解代替梯度下降更新分类头，理论上等同于在所有历史数据上重新训练，有效缓解遗忘。
    *   **自适应推理融合**：基于KL散度衡量两个学习器的分歧，动态融合预测结果。

### 3. 实验设计
*   **数据集**：涵盖5个多样化的医学影像数据集：
    *   **Colon**（组织病理学）、**Blood**（显微镜血细胞）、**Skin8**（皮肤镜）、**MedMNIST-Sub**（多模态子集）、**COVID (CT&X-ray)**（放射学）。
*   **Benchmark与对比方法**：
    *   **传统CIL**：iCaRL, DER, FOSTER, ACL等。
    *   **基于PFM的CIL**：L2P, DualPrompt, SLCA, ADAM, EASE, MOS, SSIAT等。
    *   **基准线**：联合训练（Joint Training，性能上限）和全微调（Finetune，性能下限）。

### 4. 资源与算力
*   **硬件**：使用了 **2 台 NVIDIA A100 80G GPU**。
*   **训练配置**：初始任务训练20个epoch，后续增量任务每个训练15个epoch；采用带动量的SGD优化器，初始学习率为0.01。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的评估，包括：
    *   在5个数据集上的标准类增量对比。
    *   **消融实验**：验证了双向交互、领域对齐、双学习器推理等模块的必要性。
    *   **鲁棒性分析**：测试了不同任务顺序（包括反向顺序）、不同任务粒度（4步vs 8步增量）的影响。
    *   **跨领域/数据集实验**：模拟了更严苛的机构间分布偏移。
    *   **PFM敏感性测试**：对比了通用PFM（ViT-B/16）与医学专用PFM（BiomedCLIP, RAD-DINO）。
*   **评价**：实验设计非常充分且客观，通过多维度变量控制验证了框架在隐私敏感（无回放）临床场景下的实用性。

### 6. 主要结论与发现
*   **性能领先**：Bi-CRCL在所有数据集上均显著优于现有的SOTA方法，且在无记忆回放的情况下接近联合训练的性能。
*   **PFM选择**：发现通用领域PFM（ImageNet预训练）在医学增量学习中往往比现有的医学专用PFM表现更稳健、迁移性更好。
*   **回放必要性**：实验证明，在Bi-CRCL框架下，加入记忆回放对性能提升微乎其微，说明该双学习器机制已能有效保留关键知识。

### 7. 优点与亮点
*   **脑启发设计**：将海马体-皮层交互机制转化为双向EMA更新和初始化策略，逻辑自洽且生物学解释性强。
*   **解析解优势**：引入解析分类器避免了增量学习中常见的梯度不稳定问题。
*   **隐私友好**：完全无需存储历史图像，符合医疗数据合规性要求。
*   **泛化性强**：在跨数据集（Inter-dataset）实验中表现优异，证明了其处理极端领域偏移的能力。

### 8. 不足与局限
*   **计算开销**：由于需要同时维护和运行两个学习器（虽然是轻量级Adapter），在推理和训练时的内存占用及计算量比单模型略高。
*   **小样本敏感性**：在极少样本（Few-shot）的增量场景下，解析分类器计算类原型（Prototypes）的准确性可能会受到影响。
*   **任务扩展**：目前主要集中在分类任务，尚未扩展到更复杂的分割或检测任务的增量学习中。

（完）
