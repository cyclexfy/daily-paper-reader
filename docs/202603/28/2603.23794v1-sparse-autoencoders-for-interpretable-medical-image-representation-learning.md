---
title: Sparse Autoencoders for Interpretable Medical Image Representation Learning
title_zh: 用于可解释医学图像表示学习的稀疏自编码器
authors: "Philipp Wesp, Robbie Holland, Vasiliki Sideri-Lampretsa, Sergios Gatidis"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23794v1"
tags: ["query:cpath"]
score: 8.0
evidence: 利用视觉基础模型进行可解释的医学图像表示学习
tldr: 视觉基础模型在医学影像中表现优异但缺乏可解释性。本研究利用稀疏自编码器（SAEs）将BiomedParse和DINOv3的抽象嵌入转化为人类可理解的稀疏特征。通过对90万张CT/MRI切片进行训练，SAEs在保持高保真度的同时实现了极高的维度压缩，并支持通过大语言模型进行特征解释，为构建可解释、概念驱动的医学视觉系统提供了新路径。
selection_source: fresh_fetch
motivation: 针对医学视觉基础模型表征过于抽象、临床医生难以验证和理解的黑盒问题，探索可解释的特征提取方法。
method: 在大规模医学影像数据集上训练稀疏自编码器，将基础模型的潜在表征分解为稀疏且具有语义意义的特征。
result: SAEs能以极高精度重建原始嵌入，仅用10个特征即可恢复大部分下游性能，并支持零样本语言驱动的图像检索。
conclusion: 稀疏自编码器是实现医学影像模型可解释性和概念驱动分析的一种极具前景的技术方案。
---

## 摘要
视觉基础模型（FMs）在医学成像领域取得了最先进的性能。然而，它们将信息编码在抽象的潜表示中，临床医生无法对其进行询问或验证。本研究旨在探讨利用稀疏自编码器（SAEs）将不透明的视觉基础模型图像表示替换为人类可解释的稀疏特征。我们利用来自 TotalSegmentator 数据集的 909,873 张 CT 和 MRI 二维图像切片，在 BiomedParse（生物医学）和 DINOv3（通用）的嵌入向量上训练了 SAEs。我们发现学习到的稀疏特征：(a) 能以高保真度重构原始嵌入（R2 高达 0.941），且仅使用 10 个特征（降维率达 99.4%）即可恢复高达 87.8% 的下游性能；(b) 在图像检索任务中保持了语义保真度；(c) 对应于特定的概念，这些概念可以通过基于大语言模型（LLM）的自动解释用语言表达；(d) 在零样本语言驱动的图像检索中桥接了临床语言与抽象潜表示。我们的工作表明，SAEs 是通往可解释、概念驱动的医学视觉系统的一条极具前景的路径。代码仓库：https://github.com/pwesp/sail。

## Abstract
Vision foundation models (FMs) achieve state-of-the-art performance in medical imaging. However, they encode information in abstract latent representations that clinicians cannot interrogate or verify. The goal of this study is to investigate Sparse Autoencoders (SAEs) for replacing opaque FM image representations with human-interpretable, sparse features. We train SAEs on embeddings from BiomedParse (biomedical) and DINOv3 (general-purpose) using 909,873 CT and MRI 2D image slices from the TotalSegmentator dataset. We find that learned sparse features: (a) reconstruct original embeddings with high fidelity (R2 up to 0.941) and recover up to 87.8% of downstream performance using only 10 features (99.4% dimensionality reduction), (b) preserve semantic fidelity in image retrieval tasks, (c) correspond to specific concepts that can be expressed in language using large language model (LLM)-based auto-interpretation. (d) bridge clinical language and abstract latent representations in zero-shot language-driven image retrieval. Our work indicates SAEs are a promising pathway towards interpretable, concept-driven medical vision systems. Code repository: https://github.com/pwesp/sail.

---

## 论文详细总结（自动生成）

以下是对论文《Sparse Autoencoders for Interpretable Medical Image Representation Learning》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：视觉基础模型（FMs）在医学影像任务中表现卓越，但其内部表示是抽象且不透明的“黑盒”，临床医生无法理解或验证其决策依据。
*   **研究动机**：医学领域对可解释性有极高要求。研究者试图探索“机械可解释性”（Mechanistic Interpretability）路径，利用**稀疏自编码器（SAEs）**将基础模型中复杂的、多语义的特征分解为稀疏的、单语义的（即一个特征对应一个具体临床概念）特征，从而弥合抽象潜空间与临床语言之间的鸿沟。

### 2. 论文提出的方法论
*   **核心思想**：在冻结的基础模型嵌入（Embeddings）之上训练 SAEs，将高维稠密向量映射到更高维但极度稀疏的特征空间。
*   **关键技术细节**：
    *   **Matryoshka SAE 架构**：采用嵌套字典（Nested Dictionary）设计，包含 4 个不同规模的层级。早期层级捕捉粗略结构，后期层级进行精细化。
    *   **BatchTopK 稀疏化**：在训练期间，通过 BatchTopK 算法强制每层平均只有 $k$ 个特征处于激活状态，相比固定 TopK 更具灵活性。
    *   **JumpReLU 激活**：在推理阶段使用 JumpReLU 阈值替代 BatchTopK，以提高重建保真度。
    *   **自动解释流程**：利用视觉语言模型（VLM，如 MedGemma）根据激活该特征的顶级图像样本自动生成自然语言描述，并由 LLM 充当裁判进行验证。

### 3. 实验设计
*   **数据集**：使用 **TotalSegmentator** 数据集，包含来自 10 个机构的 1,844 个扫描序列（1,228 个 CT，616 个 MRI），共计 **909,873 张 2D 图像切片**。
*   **基础模型（Benchmark）**：
    *   **BiomedParse**：专门的生物医学视觉基础模型。
    *   **DINOv3**：通用自监督视觉模型。
    *   **随机权重基准**：使用随机初始化的 BiomedParse 权重，以证明学习到的特征源于训练而非架构本身。
*   **对比与评估任务**：
    *   **重建质量**：计算 $R^2$ 分数。
    *   **下游性能恢复**：评估在解剖结构分类任务中的 ROC-AUC。
    *   **图像检索**：对比稀疏“指纹”检索与原始稠密向量检索的语义一致性。
    *   **零样本语言驱动检索**：通过临床文本查询直接检索图像。

### 4. 资源与算力
*   **训练细节**：使用 Adam 优化器，学习率 $10^{-4}$（余弦退火至 $10^{-6}$），训练 100 个 epoch，Batch Size 为 2048。
*   **硬件说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。但考虑到处理近 100 万张图像及运行 27B 参数的 MedGemma 模型，该研究需要相当规模的计算资源（推测为 A100 或 H100 集群）。

### 5. 实验数量与充分性
*   **实验规模**：每个基础模型测试了 **96 种配置**（涵盖 4 种字典大小家族和 8 种稀疏模式），总计进行了数百组实验。
*   **充分性与公平性**：
    *   实验涵盖了 CT 和 MRI 两种模态，以及全身多个解剖区域。
    *   使用了独立的测试集（完全预留 3 个机构的数据），确保了评估的客观性。
    *   引入随机权重基准作为对照组，有力证明了 SAEs 提取的是模型学到的语义结构。

### 6. 论文的主要结论与发现
*   **高保真重建**：SAEs 能以极高精度重建原始嵌入（$R^2$ 最高 0.941）。
*   **极高压缩比**：仅使用 **10 个稀疏特征**（维度缩减 99.4%）即可恢复基础模型约 **88%** 的下游分类性能。
*   **语义涌现**：即使是自监督模型（如 DINOv3），在没有显式标签的情况下也学到了与解剖结构、模态和人口统计学相关的单语义特征。
*   **通用模型优势**：令人意外的是，通用模型 DINOv3 产生的单语义特征比专门的医学模型 BiomedParse 更多且更清晰。
*   **语言桥接**：证明了可以通过 LLM 将临床查询映射到稀疏特征，实现无需参考图像的零样本检索。

### 7. 优点
*   **架构无关性**：SAEs 作为独立层运行，无需修改或重新训练庞大的基础模型。
*   **高效性**：Matryoshka 架构允许在单个模型中探索多种稀疏度/容量权衡。
*   **自动化评估**：引入 VLM 自动生成和验证特征含义，解决了传统可解释性研究依赖人工标注的扩展性难题。

### 8. 不足与局限
*   **维度限制**：目前分析基于 **2D 图像切片**，未充分利用医学影像的 3D 空间上下文。
*   **病理缺失**：TotalSegmentator 数据集主要涵盖正常解剖结构，SAEs 在**病理/异常检测**中的表现尚未验证。
*   **评估偏差**：单语义性的评分依赖于 VLM 生成的描述，可能存在模型幻觉或评估代理的偏差。
*   **检索深度**：语言驱动检索目前仅作为概念验证（单条查询演示），缺乏大规模、多场景的定量评估。

（完）
