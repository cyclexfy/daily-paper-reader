---
title: Distribution-based deep multiple instance learning for tumor proportion scoring in NSCLC
title_zh: 基于分布的深度多实例学习用于非小细胞肺癌肿瘤比例评分
authors: "Krzysztof Pysz, Artur Bartczak, Jarosław Kwiecień, Piotr Krajewski, Witold Dyrka"
date: 2026-06-25
pdf: "https://arxiv.org/pdf/2606.27579v1"
tags: ["query:cpath"]
score: 9.0
evidence: 基于深度多实例学习的非小细胞肺癌肿瘤比例评分
tldr: 非小细胞肺癌的肿瘤比例评分（TPS）评估对治疗规划至关重要，但手动标注繁琐且专家稀缺。现有多实例学习（MIL）方法难以处理非表达性图像。本文提出基于零膨胀贝塔（ZIBeta）分布的MIL框架，先提取病理切片中每个图块的嵌入特征，再通过MIL聚合预测整个切片的TPS概率分布参数。仅使用切片级标签进行端到端训练，该方法在预测精度上显著优于线性回归和岭回归基线，并通过分布集中度反映预期准确性。该分布驱动架构提升了预测的准确性和可解释性，为自动化TPS评分提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.27579v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1126, \"height\": 1447, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27579v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1037, \"height\": 599, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27579v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1072, \"height\": 624, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27579v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1077, \"height\": 942, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.27579v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1263, \"height\": 267, \"label\": \"Table\"}]"
motivation: 现有MIL方法无法有效处理零类图像，且TPS评估依赖大量人工标注，亟需一种仅利用切片级标签的高精度可解释方法。
method: 提出包含嵌入提取网络和ZIBeta分布MIL聚合的两阶段框架，以零膨胀贝塔参数形式建模整张切片的TPS概率分布。
result: ZIBeta建模在TPS预测精度上显著超越线性回归与岭回归基线，并通过分布集中度表征预测可靠性。
conclusion: 基于分布的多实例学习有效提升了TPS评分准确性与可解释性，降低了对密集标注的依赖。
---

## 摘要
在非小细胞肺癌（NSCLC）中，准确评估肿瘤比例评分（TPS）对于治疗计划和预后至关重要。主要挑战包括手动标注每张切片所需的繁琐工作，以及该任务认证专家数量有限。多实例学习（MIL）已被证明是预测切片级别TPS评分的有效方法；然而，现有方法在处理无表达（零类）图像时存在困难。我们的方法涉及两个模型：（1）嵌入提取和多类分类网络，捕获单个图块的组织病理学特征，以及（2）MIL模型，聚合这些嵌入以预测零膨胀贝塔（ZIBeta）参数，代表整个切片的总体TPS概率分布。仅使用切片级别的TPS评分作为标签，我们展示了这种端到端框架如何利用新颖的基于分布的架构来提高预测准确性和可解释性。ZIBeta建模显著优于基线线性回归和岭回归，同时通过分布集中度捕捉预期准确性。

## Abstract
Accurate assessment of tumor proportion score (TPS) in non-small cell lung cancer (NSCLC) is critical for treatment planning and prognosis. Key challenges include the tedious manual work required to annotate each slide, combined with the limited number of experts certified for this task. Multiple instance learning (MIL) has proven to be an effective approach for predicting TPS scores at the slide level; however, existing methods struggle with non-expressive (zero class) images. Our approach involves two models: (1) an embedding-extraction and multiclass-classification network that captures the histopathological features of individual patches, and (2) a MIL model that aggregates these embeddings to predict zero-inflated beta (ZIBeta) parameters representing the overall TPS probability distribution for the entire slide. Using only slide-level TPS scores as labels, we demonstrate how this end-to-end framework can leverage a novel distribution-based architecture to improve prediction accuracy and explainability. ZIBeta modeling significantly outperforms baseline linear and ridge regression while capturing expected accuracy through distribution concentration.

---

## 论文详细总结（自动生成）

### 0. 论文的源代码链接
无（论文未提及开源代码链接）。

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题：** 非小细胞肺癌（NSCLC）的肿瘤比例评分（TPS，即PD-L1阳性肿瘤细胞占比）对免疫治疗方案决策至关重要。但当前TPS评估依赖病理专家手动标注每张全切片图像（WSI），耗时且专家稀缺；现有自动化方法（尤其是多实例学习）在预测TPS=0的“零类”样本时表现不佳。
- **整体含义：** 本文旨在构建一个仅需切片级TPS标签（弱监督）的端到端框架，通过概率分布建模（零膨胀贝塔分布）提升TPS回归精度，特别是对零类的识别能力，并提供内置的不确定性估计与可解释性。

### 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想：** 采用两阶段框架：第一阶段用公开数据集训练一个嵌入提取网络（MobileNetV3Large），将每个图块映射为特征向量；第二阶段用多实例学习（MIL）聚合所有图块的嵌入，预测整个切片的TPS概率分布参数（零膨胀贝塔分布的三个参数 \( \phi, \mu, \nu \)），从而同时输出预测值（均值）和置信度（浓度）。
- **关键技术细节：**
  - **嵌入提取：** 在MIHIC数据集上训练MobileNetV3Large，进行6类组织分类（+背景），取倒数第二层输出作为图块嵌入（128维）。
  - **MIL架构（ZIBeta模型）：** 输入为包（bag）中所有肿瘤图块嵌入，经过线性变换→ReLU→Dropout层，然后并行两个分支：
    1. **门控注意力聚合**：计算每个图块的注意力权重，加权求和得到包特征，再通过线性层输出预测均值 \( \mu \)。
    2. **实例级预测**：每个图块独立通过分类头（Linear + Hardsigmoid）得到实例级TPS预测，再经注意力权重加权平均作为辅助。
  - **分布参数预测：** 从包特征再分出两个头：
    - 头 \( \phi \)：Sigmoid激活，表示TPS=0的概率。
    - 头 \( \nu \)：Softplus激活，表示贝塔分布的浓度参数（\( \nu = \alpha + \beta \)）。
    - 为防止数值不稳定，将 \( \nu \) 校正为 \( \nu_{\text{corrected}} = \nu + \max(1/\mu, 1/(1-\mu)) + \epsilon \)。
  - **损失函数：** 结合负对数似然（NLL）与均方误差（MSE），并加入熵最小化项（鼓励每个图块的预测趋向0或1）：
    \[
    L_{\text{ZIBeta}} = -\log L(t) + \text{MSE}((1-\phi)\mu, t) + \Omega
    \]
    其中 \( L(t) \) 对零样本取 \( \phi \)，对正样本取 \( (1-\phi) \cdot \text{BetaPDF}(\tilde{t}; \alpha, \beta) \)。
    - 熵项 \( \Omega = \frac{1}{n}\sum_i \mu_i(1-\mu_i) \)。
- **训练细节：** 每个bag固定采样4096个图块（用于推理），训练时随机抽512个图块/epoch；优化器AdamW，学习率 \( 2\times10^{-5} \)（注意力模型）或 \( 5\times10^{-5} \)（均值池化），Dropout=0.3，训练500 epochs，选验证损失最低的检查点。

### 3. 实验设计：数据集、基准与对比方法
- **数据集：**
  - **嵌入模型训练集：** MIHIC（公共，>30万张图像，6类组织+背景，来自NSCLC患者的IHC染色）。
  - **TPS预测数据集：** 私有WSI，来自162例NSCLC患者（159例SP263染色，3例22C3染色），由一名病理专家按10%间隔标注TPS（<20%时更细粒度）。剔除非肿瘤组织区域。
- **基准（Benchmark）：** 无公开基准，与自身设计的多个基线对比。
- **对比方法：**
  - **Linear：** 基于肿瘤图块嵌入平均池化后进行线性回归（sklearn）。
  - **Ridge：** 同Linear但使用岭回归。
  - **MIL base：** 基本MIL，采用均值聚合实例预测（无注意力）。
  - **MIL att：** 门控注意力聚合实例预测（无ZIBeta头）。
  - **MIL ZIB：** 本文提出的ZIBeta模型（包含 \( \phi, \mu, \nu \) 头）。
- **评估指标：** RMSE、MAE（回归）；Macro F1、三类F1（<1%, 1-49%, 50-100%）、Cohen's Kappa（分类）；还分析了浓度 \( \nu \) 与绝对误差的关系、注意力可视化。

### 4. 资源与算力
**论文未明确说明使用的GPU型号、数量及训练时长。** 仅提及训练采用AdamW优化器，batch size=32，500 epochs。无法评估算力投入。

### 5. 实验数量与充分性
- **实验轮次：** 3次随机数据划分（train:val:test=70:15:15，分层），每次划分训练所有5种模型，共15次独立实验。
- **消融实验：** 通过对比不同聚合方式（均值 vs 注意力）和是否使用ZIBeta分布，间接进行了消融。但缺少对嵌入模型选择、patch尺寸、损失函数各分量作用的细致消融。
- **充分性评价：** 实验设计相对规范（多轮划分、验证集选最优、置信区间报告），但样本量仅162张WSI，且只有一位病理学家标注（存在标注偏差）。此外仅与简单的回归基线对比，未与SOTA MIL方法（如ABMIL、CLAM、TransMIL等）比较，实验覆盖不够广泛。总体而言，实验较充分但存在局限性。

### 6. 论文的主要结论与发现
- ZIBeta模型在RMSE（0.201）、MAE（0.140）、零类F1（0.612）上均优于或媲美其他深度MIL模型，且显著优于线性/岭回归，特别是零类准确率提升明显。
- ZIBeta的浓度参数 \( \nu \) 与绝对预测误差呈负相关（误差随 \( \nu \) 增大而降低），但噪声较大，尚不能作为可靠的不确定性代理。
- 注意力机制有效聚焦于信息密集区域（肿瘤细胞阳性区域），提升可解释性，并能帮助识别伪影。
- 仅使用切片级标签的弱监督方法在较小数据集上仍能取得有竞争力的结果。

### 7. 优点
1. **分布建模创新：** 首次在TPS评分中引入零膨胀贝塔分布，同时预测均值和置信度，提供内在的不确定性估计。
2. **弱监督训练：** 仅需切片级标签，降低对密集标注的依赖。
3. **开源嵌入复用：** 利用公共MIHIC数据集预训练嵌入模型，便于迁移到其他私有数据集。
4. **可解释性：** 注意力权重图与图块级TPS预测结合，可定位染色阳性区域，辅助病理医生审核。
5. **零类处理：** 零膨胀组件专门建模TPS=0的样本，有效解决现有方法在此类上的困难。

### 8. 不足与局限
1. **数据集规模小且单一：** 仅162张WSI（几乎全为SP263染色），且来自同一机构，泛化性存疑。真值仅来自一位病理学家，存在评估偏差。
2. **标签粗糙：** 高TPS（≥20%）标签为10%间隔，限制了模型细粒度回归能力。
3. **基线对比不足：** 未与当前主流MIL方法（如ABMIL、CLAM、DSMIL、TransMIL等）对比，说服力不够。
4. **缺乏算力报告：** 未披露训练资源，可复现性打折扣。
5. **不确定性估计实用性有限：** 浓度参数 \( \nu \) 与误差相关性虽统计显著，但噪声较大，未提供具体阈值用于拒绝低置信预测。
6. **嵌入模型选择未调优：** 直接采用MobileNetV3Large在MIHIC上训练，未探讨更优的特征提取器（如ResNet、ViT）。
7. **无公开代码或模型权重：** 限制后续研究复现与比较。

（完）
