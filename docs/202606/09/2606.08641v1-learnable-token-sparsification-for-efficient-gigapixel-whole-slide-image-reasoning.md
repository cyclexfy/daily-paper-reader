---
title: Learnable Token Sparsification for Efficient Gigapixel Whole Slide Image Reasoning
title_zh: 可学习令牌稀疏化用于高效十亿像素全切片图像推理
authors: "Jingzhi Chen, Landi He, Zhuo Chen, Shawn Young, Lijian Xu"
date: 2026-06-07
pdf: "https://arxiv.org/pdf/2606.08641v1"
tags: ["query:cpath"]
score: 9.0
evidence: 基于可学习令牌稀疏化的千兆像素全切片图像推理
tldr: "全切片图像视觉语言模型面临过量token问题，现有下采样或启发式剪枝会丢失稀疏诊断证据。本文提出可学习稀疏化方法，利用SparseLearn模块的可微Soft Top-K进行训练，推理时转为Hard Top-K仅保留32个token（原长0.78%）。在SlideBench TCGA上达73.32%准确率，超越基线并展现零样本泛化能力。该方法解决了视觉上下文瓶颈，实现高效端到端推理。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.08641v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1687, \"height\": 886, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08641v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1697, \"height\": 794, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08641v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1637, \"height\": 1120, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08641v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1674, \"height\": 923, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08641v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1684, \"height\": 612, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.08641v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1688, \"height\": 860, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.08641v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1715, \"height\": 323, \"label\": \"Table\"}]"
motivation: 现有全切片图像token剪枝依赖固定启发式，丢弃了稀疏但关键的诊断模式。
method: 提出解耦路由架构，用SparseLearn的方差保持噪声门和可微Soft Top-K实现可训练稀疏化，推理时转为Hard Top-K保留32个token。
result: "在SlideBench TCGA上达73.32%准确率，超越采样基线和通用视觉语言模型，在BCNB和WSI VQA*上展现零样本泛化。"
conclusion: "可学习稀疏化将视觉token降至0.78%，有效防止诊断证据稀释，为端到端全切片图像推理提供高效范式。"
---

## 摘要
在视觉语言模型中处理十亿像素级全切片图像面临一个主要困难，即视觉令牌数量过多。现有解决方案通常依赖于空间下采样或无需训练的启发式剪枝策略，这些方法常常丢弃细微但具有临床意义的模式，因为病理证据在组织中不规则地散布。为克服这一限制，我们将全切片图像中的令牌缩减问题重新定义为可训练的稀疏化问题，使模型能够学习最优选择策略，而非遵循固定启发式方法。我们提出一种解耦路由架构。为了在训练期间通过不可微的剪枝操作实现梯度传播，我们引入一个称为SparseLearn的组件。该组件使用一个保持方差的噪声门控，通过可微的Soft Top-K算子调节每个补丁的信息流，并结合一个对角注意力去噪器，在不泄露空间信息的情况下恢复受扰动的表示。在推理时，SparseLearn模块被完全丢弃，训练后的评分器应用确定性Hard Top-K算子仅保留得分最高的32个令牌，不产生额外计算。通过将视觉序列压缩至仅32个令牌的稀疏集（仅为原始长度的0.78%），我们的框架在SlideBench（TCGA）上实现了73.32%的总体准确率，持续超越基于采样的基线和通用视觉语言模型。它在SlideBench（BCNB）和WSI VQA*上也展现出强大的零样本泛化能力。通过解决视觉上下文瓶颈并防止稀疏诊断证据的稀释，这项工作为端到端十亿像素全切片图像推理提供了一种高效范式。

## Abstract
The processing of gigapixel whole slide images within vision language models faces a major difficulty due to an excessive number of visual tokens. Existing solutions typically rely on spatial downsampling or heuristic pruning strategies that operate without training, and these methods often discard subtle but clinically meaningful patterns because pathological evidence is scattered irregularly across the tissue. To overcome this limitation, we reformulate token reduction in whole slide images as a trainable sparsification problem, allowing the model to learn an optimal selection strategy instead of following fixed heuristics. We propose a decoupled routing architecture. To enable gradient propagation through the nondifferentiable pruning operation during training, we introduce a component called SparseLearn. This component uses a variance-preserving noise gate that regulates the information flow of each patch via a differentiable Soft Top-K operator, together with a diagonal attention denoiser that recovers perturbed representations without leaking spatial information. At inference time, the SparseLearn module is entirely discarded, and the trained scorer applies a deterministic Hard Top-K operator to keep only the highest scoring 32 tokens, incurring no extra computation. By compressing the visual sequence down to a sparse set of just 32 tokens, which represents as little as 0.78% of the original length, our framework achieves 73.32% overall accuracy on SlideBench (TCGA), consistently surpassing sampling-based baselines and general-purpose vision language models. It also demonstrates strong zero shot generalization on SlideBench (BCNB) and WSI VQA*. By resolving the visual context bottleneck and preventing the dilution of sparse diagnostic evidence, this work provides a highly efficient paradigm for end to end gigapixel whole slide image reasoning.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 0. 源代码链接
无（论文中未提供公开代码链接）。

## 1. 核心问题与整体含义（研究动机和背景）
- **背景**：十亿像素级别的全切片图像（Whole Slide Image, WSI）在视觉语言模型（VLM）中处理时，会产生大量视觉token（通常超过10⁵个），导致LLM预填充阶段的自注意力复杂度呈二次增长，计算和内存开销难以承受。
- **现有方法局限**：现有token缩减方法多采用无训练的空间下采样或启发式剪枝（如基于注意力幅度、视觉相似性），这些方法本质上是静态、固定的，忽略了病理证据的极端稀疏性（如微转移灶仅占切片面积的<1%），容易丢弃关键的诊断信息。
- **核心挑战**：如何在不牺牲诊断准确性的前提下，将WSI的视觉序列压缩到极小的稀疏集合，且保证梯度可微以支持端到端优化。
- **本文意义**：将WSI token剪枝重新定义为**可学习的稀疏化问题**，让模型通过连续优化自主学习最佳剪枝策略，而非依赖固定启发式。

## 2. 方法论：核心思想、关键技术细节
### 2.1 整体框架：解耦路由架构
- 训练路径：视觉编码器 → SparseLearn模块（含Scorer、Soft Top-K、VP噪声门控、对角注意力去噪器）→ 投影层 → LLM（LoRA微调）。
- 推理路径：抛弃SparseLearn模块，仅用训练好的Scorer + Hard Top-K直接保留Top-32 tokens，零额外计算。
- 核心目标：在固定稀疏预算K=32下学习最优token选择。

### 2.2 SparseLearn模块关键组件
#### (1) 可学习评分器（Scorer） + Soft Top-K
- Scorer由L层轻量Transformer编码器+线性投影组成，输出每个token的标量重要性分数s∈ℝᴺ。
- Soft Top-K算子Φₖ对归一化分数进行温度缩放，输出连续权重α∈[0,1]ᴺ，满足Σαᵢ≈K（而非softmax的Σ=1）。
- 训练时温度τ从τ_start余弦退火至τ_end，使权重从扩散逼近二值掩码。

#### (2) 方差保持（VP）噪声门控
- 代替直接丢弃token（不可微），对每个token注入高斯噪声：**ẋᵢ = √αᵢ xᵢ + √(1-αᵢ) εᵢ, εᵢ∼𝒩(0, I)**。
- 当αᵢ→1（重要token），噪声分量趋近0，保留原始特征；当αᵢ→0（不重要token），信号被高斯噪声淹没。
- 系数√αᵢ和√(1-αᵢ)确保方差不变，避免分布偏移。

#### (3) 对角注意力去噪器（Denoiser）
- 轻量单层Transformer编码器，但**使用对角注意力**（每个token仅关注自身），防止高分数token的信息通过注意力泄露到低分数token，从而绕开稀疏瓶颈。
- 只通过值投影和前馈网络恢复被噪声扰动的表示，不引入空间信息泄漏。
- 推理时完全丢弃。

### 2.3 训练与推理流程
- 训练：最小化负对数似然损失，联合优化Scorer参数θ、Denoiser参数ω、LLM的LoRA参数ϕ。LLM主干冻结。
- 推理：**Ẋᵥ = Top-K(Xᵥ, Sθ(Xᵥ), K)**，直接保留得分最高的K个token，同时保留其位置索引以保证RoPE能捕捉空间拓扑。

## 3. 实验设计
### 3.1 数据集与基准
- **SlideBench (TCGA)**：主实验，报告总体准确率（Overall）及两个子类别：Microscopy（低阶形态与染色特征）、Diagnosis（组织学推理与亚型）。
- **零样本泛化**：SlideBench (BCNB) 和 WSI-VQA* 数据集。

### 3.2 对比方法
- **上界**：SlideChat（无token缩减，保留全部证据，4,096 tokens）。
- **计算匹配的基线**：随机采样、GPT-4o、LLaVA-Med、Quilt-LLaVA、MedDr（均为通用或医学VLM，使用空间采样或固定token数）。
- **消融实验**：比较完整SparseLearn vs. 替换为全局注意力 vs. 替换为尺度门控（直接乘以α）。

### 3.3 评估指标
- 总体准确率、子类别准确率、FLOPs、推理时token压缩倍数、动态压缩比。

## 4. 资源与算力
- **GPU**：2块 NVIDIA A6000。
- **训练策略**：两阶段训练，Stage 1冻结LLM，仅优化Slide编码器、SparseLearn、跨模态投影器；Stage 2冻结Slide编码器，继续更新SparseLearn和投影器，同时引入LoRA对LLM进行参数高效微调。
- **训练时长**：论文未明确说明总训练时长。

## 5. 实验数量与充分性
### 5.1 主要实验
- **表1**（主结果）：在SlideBench TCGA上对比7种方法（含上界、随机、4种VLM、Ours），报告Micros./Diagnos./Overall准确率及FLOPs；零样本测试两个外部数据集。
- **图3**：token效率对比（token数 vs 准确率）。
- **图4**：输入序列长度变化时的准确率稳定性（64~10000+ tokens）。
- **图5**：UMAP可视化，展示选中的32个token位于拓扑边缘（稀疏但判别性强的区域）。
- **表2**（消融实验）：K=32时，替换对角注意力为全局注意力（-1.70%）、替换VP噪声门控为尺度门控（-2.92%）。

### 5.2 充分性与公平性
- 对比方法覆盖了计算匹配的通用VLM和领域专用VLM，且报告了上界SlideChat，表明方法在极低token数下的有效性。
- 消融实验针对两个关键设计点进行了控制变量验证，结论清晰。
- 零样本测试展示了泛化能力，避免过拟合单一数据集。
- 不足之处：仅做了K=32一种固定预算的消融，未探索不同K的影响；未与近期其他可学习剪枝方法（如LearnPruner、Token Merging等）直接对比。

## 6. 主要结论与发现
- **性能**：在固定32 token（平均压缩58倍，最大128倍）下，SlideBench TCGA总体准确率73.32%，在Diagnosis子类上达到SlideChat上界的96%性能，计算开销仅1.3%。
- **泛化**：零样本性能优于所有计算匹配基线，甚至超过GPT-4o。
- **尺度不变性**：准确率在输入长度64~10000+ tokens下高度稳定（≈73.32%），说明模型学到的选择机制不依赖物理尺寸。
- **可视化**：选中的token总是位于UMAP空间的拓扑极端，对应稀疏但判别性强的病理特征。
- **消融证实**：对角注意力和VP噪声门控均为必要设计，替换后性能显著下降。

## 7. 优点
1. **创新性**：将WSI token剪枝重新定义为可学习稀疏化问题，利用连续梯度优化离散剪枝决策，避免了STE近似或Gumbel-Softmax的偏差。
2. **高效性**：推理时零额外开销（SparseLearn完全丢弃），仅保留32 token，极大降低LLM计算瓶颈。
3. **可微性设计巧妙**：VP噪声门控+对角注意力去噪器组成了可微的剪枝替代，且不泄漏空间信息。
4. **实验完整**：包含主实验、零样本泛化、效率分析、可视化、消融，论证充分。
5. **泛化能力强**：在外部数据集上零样本优于GPT-4o等大模型。

## 8. 不足与局限
1. **评分器局部性**：假设每个patch的诊断重要性可独立判断，未考虑宏观形态模式或弥散边界，可能遗漏跨patch的上下文关系。
2. **静态稀疏预算**：固定K=32，不同临床任务可能需要不同预算，缺乏动态适应能力。
3. **缺乏与其他可学习剪枝方法（如LearnPruner、Token Merging）的直接对比**，削弱了比较的完整度。
4. **仅报告了准确率**，未提供召回率/精确率/F1等指标，诊断任务中这些指标同样重要。
5. **计算资源与训练时长未充分公开**，可复现性受限。
6. **消融仅覆盖K=32**，未探索不同K值下各组件的影响。

（完）
