---
title: Rethinking the Adaptation of Vision Foundation Models for Efficient Cell Segmentation
title_zh: 重新思考视觉基础模型在高效细胞分割中的适应策略
authors: "Qing Xu, Xiangjian He, Wenting Duan, Jiebo Luo, Zhen Chen"
date: 2026-06-20
pdf: "https://arxiv.org/pdf/2606.21913v1"
tags: ["query:cpath"]
score: 8.0
evidence: 将视觉基础模型适配用于计算病理学中的高效细胞分割
tldr: 细胞分割在计算病理学中至关重要，但现有视觉基础模型(VFM)适配方法需重训练编码器，计算开销大且依赖大规模标注。本文提出EffiCell-Seg，无需重训练编码器，利用冻结VFM中的全局显著性和局部形态先验，通过CSP-Encoder合成显式结构图，并由SM-Decoder联合预测几何距离场和语义图以增强上下文一致性。在多种细胞成像数据集上，仅5M可训练参数即达到最优性能，参数量仅为全微调VFM的1/130以上。该方法显著降低了资源开销，为高效细胞分割提供了新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.21913v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1142, \"height\": 329, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21913v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1190, \"height\": 503, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21913v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1142, \"height\": 320, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.21913v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1270, \"height\": 384, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21913v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1267, \"height\": 381, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21913v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1271, \"height\": 319, \"label\": \"Table\"}]"
motivation: 现有VFM适配方法需重训练编码器，计算开销大且依赖大规模细胞标注。
method: 冻结编码器，CSP-Encoder将VFM的全局显著性和局部形态先验合成显式结构图，SM-Decoder联合预测距离场与语义图。
result: 仅约5M可训练参数即在多种细胞成像上超越SOTA，参数量比全微调VFM减少130倍以上。
conclusion: 提出无需重训练编码器的范式，大幅降低资源需求，适用于多样本场景。
---

## 摘要
细胞分割对于计算病理学和生物医学发现至关重要。尽管最近的视觉基础模型展示了卓越的通用特征表示，但充分发挥其在细胞成像中的潜力目前受到资源密集型适应范式的瓶颈限制。现有方法通常依赖于微调重型视觉编码器，导致大量计算开销并依赖大规模标注。为解决这一问题，我们提出了EffiCell-Seg框架，可在不重新训练视觉编码器的情况下实现高效细胞分割。我们的核心洞察是：预训练的视觉基础模型内在地编码了互补的结构先验——用于定位潜在细胞的全局显著性，以及用于描绘细胞结构的局部形态模式。为了利用这些先验，我们设计了细胞结构提示编码器，将冻结的视觉基础模型表示中的语义感知显著性和主要形态特征合成为显式结构先验图。此外，我们提出了协同掩码解码器，通过联合预测几何距离场和语义图并利用相互交叉引导来强制上下文一致性。大量实验表明，EffiCell-Seg在多种细胞成像模态下优于最先进方法，同时仅需约5M可训练参数，比完全微调的视觉基础模型对应方法少130倍以上。代码可在https://github.com/xq141839/EffiCell-Seg获取。

## Abstract
Cell segmentation is critical for computational pathology and biomedical discovery. While recent Vision Foundation Models (VFMs) have demonstrated remarkable universal feature representations, unlocking their full potential for cellular imaging is currently bottlenecked by resource-intensive adaptation paradigms. Existing methods typically rely on fine-tuning heavy visual encoders, leading to extensive computational overhead and a dependency on large-scale annotations. To address this, we propose the EffiCell-Seg framework for highly efficient cell segmentation without re-training the visual encoder. Our core insight is that pretrained VFMs intrinsically encode complementary structural priors: global saliency for localizing potential cells, and local morphological patterns for delineating cellular structures. To harness these priors, we devise a Cell Structure Prompt Encoder (CSP-Encoder) that synthesizes semantic-aware saliency and principal morphological features from frozen VFM representations into explicit structural prior maps. Moreover, we propose a Synergistic Mask Decoder (SM-Decoder) that enforces contextual consistency by jointly predicting geometric distance fields and semantic maps via mutual cross-guidance. Extensive experiments demonstrate that EffiCell-Seg outperforms state-of-the-art methods across diverse cell imaging modalities while requiring only ~5M trainable parameters, over 130x fewer than fully fine-tuned VFM counterparts. The code is available at https://github.com/xq141839/EffiCell-Seg.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 0. 源代码链接
https://github.com/xq141839/EffiCell-Seg

## 1. 论文的核心问题与整体含义
- **研究动机**：细胞分割是计算病理学和生物医学发现的关键任务。现有视觉基础模型（VFM）如DINOv3虽然具备强大的通用特征表示，但将其应用于细胞成像时，主流方法是全微调或使用参数高效微调（如Adapter、LoRA），这仍然需要大量可训练参数和像素级标注数据，计算开销大且数据获取困难。
- **核心问题**：能否在不重新训练预训练VFM编码器的情况下，仅利用其冻结的特征实现高性能细胞分割？
- **整体含义**：作者发现VFM（如DINOv3）在预训练中已编码了通用结构先验（如闭合边界、局部连续性），这些先验与细胞结构高度共享。因此，无需重训练编码器，只需重新对齐这些先验即可高效完成细胞分割。

## 2. 论文提出的方法论
- **核心思想**：利用冻结VFM编码器输出的CLS token和空间token分别提取全局显著性和局部形态先验，通过轻量模块合成显式结构先验图，再联合解码几何距离场和语义图，实现精准分割。
- **关键技术细节**：
  - **CSP-Encoder（细胞结构提示编码器）**：
    1. 计算CLS token与每个空间token的余弦相似度，得到**语义感知显著性图** `ai`，用于定位细胞区域。
    2. 对空间token进行PCA，保留前K个主成分作为**形态特征** `mi`，捕捉细胞边界和内部纹理。
    3. 通过元素级乘法融合两者得到**结构先验图（SPM）** `zi = ai · mi`，再用轻量卷积块转换为提示嵌入。
  - **SM-Decoder（协同掩码解码器）**：
    1. 维护两组可学习查询：几何查询 `qg`（用于距离场）和语义查询 `qs`（用于语义图）。
    2. 通过双向Transformer初始化查询表示，并利用交叉注意力使几何与语义相互促进。
    3. 最终输出水平/垂直距离场和语义图，推理时用分水岭算法分离实例。
  - **冻结策略**：VFM编码器冻结，仅训练CSP-Encoder和SM-Decoder（约5M参数）。
- **损失函数**：联合损失 `L_Cell = L_CE + L_Dice + λ＊L_MSE`，平衡语义与距离回归。

## 3. 实验设计
- **数据集**：
  - **CellSeg**：1101张多模态显微镜图像，含多种细胞类型（如骨髓瘤细胞、白细胞）。
  - **DSB**：670张图像，来自2018 Data Science Bowl竞赛。
  - 两者按7:1:2划分训练/验证/测试。
- **Benchmark**：对比了经典方法（CPPNet、Swin-UMamba）和VFM-based方法（CellSAM、PromptNucSeg、CellViT、SAC）。所有方法在相同输入分辨率（512×512）下评估。
- **评估指标**：语义分割用Dice和mIoU；实例分割用PQ和F1。

## 4. 资源与算力
- 论文明确：所有实验在**NVIDIA H20 GPU**上使用PyTorch完成。
- 训练配置：batch size=16，epochs=100，优化器AdamW（lr=1e-4），余弦退火调度。
- **未明确说明**：使用的GPU数量、总训练时长、内存消耗等细节。但参数量仅约5M，根据经验推断训练资源需求远低于全微调方法。

## 5. 实验数量与充分性
- **核心实验**：
  - 表1：细胞**语义分割**对比（6个方法+本方法）。
  - 表2：细胞**实例分割**对比（6个方法+本方法）。
  - 表3：**消融研究**，依次添加相似度图、PCA图、CSP-Encoder、SM-Decoder，验证各组件贡献。
  - 图3：定性结果可视化，展示多种模态下的分割效果。
- **充分性评估**：
  - 对比了主流SOTA方法，且包含经典和VFM两类基线，较为全面。
  - 消融实验覆盖了所有核心组件，有力证明了设计有效性。
  - 但缺少跨VFM泛化性实验（如换成SAM、ViT等）和不同编码器大小的影响分析，公平性方面与基线在同一设置下对比，结果客观。

## 6. 论文的主要结论与发现
- EffiCell-Seg在CellSeg和DSB两个数据集上均取得**最优的语义和实例分割性能**，比第二好的SAC分别高出0.78~1.03% Dice/mIoU，1.27~1.35% PQ/F1。
- **仅需约5M可训练参数**，比全微调CellSAM少130倍以上，比SAC少约10倍。
- 冻结VFM编码器并通过轻量模块对齐结构先验，比重新训练编码器更高效且更鲁棒，避免了过拟合领域特定外观变化。
- 交叉引导的几何-语义解码有效解决了边缘模糊和粘连细胞分离问题。

## 7. 优点
- **高效性**：无需重训练大型VFM编码器，参数量极少，计算和存储需求大幅降低。
- **创新性**：首次提出仅利用冻结VFM的固有结构先验（显著性+形态）进行细胞分割，无需额外辅助模型或提示。
- **泛化能力**：在多种成像模态（H&E、荧光等）和细胞类型上均表现优异，且结构先验图可视化（图1）验证了跨域一致性。
- **设计简洁**：CSP-Encoder+SM-Decoder模块逻辑清晰，易于复现和扩展。

## 8. 不足与局限
- **实验覆盖**：仅测试了DINOv3一种VFM，未评估其他基础模型（如SAM、CLIP）的适配效果，泛化性证据不足。
- **资源报告不完整**：未提供精确的GPU数量、训练耗时、内存占用等，不利于复现和比较。
- **缺失复杂场景分析**：未讨论极端重叠、低对比度或稀疏细胞场景下的性能边界。
- **应用限制**：依赖预训练VFM（如DINOv3）在自然图像上的学习，若细胞图像形态与自然图像差异极大（如特殊染色或高噪声），先验可能失效。此外，分水岭算法后处理可能对距离场精度敏感。
- **偏差风险**：数据集规模（CellSeg 1101张、DSB 670张）相对有限，结果是否在更大规模或更低质量数据上保持优势有待验证。

（完）
