---
title: Reducing Redundancy in Whole-Slide Image Patching for Scalable Indexing and Retrieval
title_zh: 减少全切片图像分块中的冗余以实现可扩展的索引与检索
authors: "Jialiang Geng, Ghazal Alabtah, Saghir Alfasly, Wataru Uegami, H. R. Tizhoosh"
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.26157v1"
tags: ["query:cpath"]
score: 9.0
evidence: 减少全切片图像补丁冗余以用于索引和检索
tldr: "数字病理学中全切片图像索引的存储成本高、扩展性差。本文提出ARReST策略，通过识别并剪除跨类别区分贡献小的对立补丁来压缩索引。在TCGA 21器官数据集上实现3%-60%存储节省，平均14%，检索性能不变。该方法为大规模临床AI检索系统提供了低成本、可扩展的解决方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.26157v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1355, \"height\": 812, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.26157v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1675, \"height\": 966, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.26157v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1674, \"height\": 966, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.26157v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1702, \"height\": 600, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.26157v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1661, \"height\": 1120, \"label\": \"Table\"}]"
motivation: 全切片图像索引存储成本高，限制了医疗机构的可扩展性，急需在保持检索精度的同时压缩存储。
method: 提出ARReST框架，利用跨组织类别的冗余，识别对立补丁（跨类区分贡献小）并剪除，压缩索引而不损失形态多样性。
result: "在TCGA 21器官数据集上，存储节省3%-60%（平均14%±13%），检索性能与未压缩时相当。"
conclusion: ARReST实现了低成本、可扩展的WSI索引，适用于下一代基于检索的临床AI系统。
---

## 摘要
数字病理学的快速发展催生了对全切片图像高效索引与检索的迫切需求。新兴的生成式AI工作流，特别是检索增强生成（RAG），对可靠的相似性搜索提出了更高要求，以支持高风险临床决策。然而，高性能存储的高昂成本限制了许多医疗机构在WSI索引方面的可扩展性和可及性。因此，在保持检索精度的同时降低存储需求的方法已成为关键研究重点。我们提出ARReST（对立冗余缩减策略），这是一个原则性的对立框架，利用不同组织类别间的冗余来显著减少需从每个WSI索引的图像块数量。ARReST不仅消除同类重复，还识别出那些对跨类别判别贡献最小的对立图像块，并将其从可搜索存档中剔除。这种针对性缩减在无损形态多样性或检索保真度的前提下大幅压缩索引。通过最小化冗余图像块表示，ARReST减少了存储占用、降低了计算开销，并加速了大型病理数据库中的相似性搜索。在TCGA数据库（涵盖21个器官的癌症基因组图谱）上的广泛实验表明，ARReST在实现显著索引压缩的同时保持了具有竞争力的检索性能。对于许多器官，观察到的存储节省3%至60%（14%±13%）可在不牺牲检索性能的情况下可靠实现。所提出的策略实现了可扩展、成本效益高的WSI索引，非常适合下一代检索驱动的临床AI系统。

## Abstract
The rapid growth of digital pathology has created an urgent need for efficient indexing and retrieval of whole slide images (WSIs). This need is intensified by emerging generative AI workflows, particularly retrieval-augmented generation (RAG), which require dependable similarity search to support high-stakes clinical decision-making. Yet the substantial cost of high-performance storage limits the scalability and accessibility of WSI indexing for many healthcare institutions. Consequently, methods that can reduce storage demands while preserving retrieval accuracy have become a critical research priority. We propose ARReST (Antithetical Redundancy Reduction Strategy), a principled oppositional framework that leverages redundancy across dissimilar tissue classes to markedly decrease the number of patches that must be indexed from each WSI. Instead of eliminating only within-class duplicates, ARReST identifies antithetical patches-those whose representations contribute minimally to cross-class discrimination-and prunes them from the searchable archive. This targeted reduction substantially compresses the index without sacrificing morphological diversity or retrieval fidelity. By minimizing superfluous patch representations, ARReST reduces storage footprint, lowers computational overhead, and accelerates similarity search across large pathology repositories. Extensive experiments on TCGA repository (The Cancer Genome Atlas with 21 organs) demonstrate that ARReST achieves significant index compression while maintaining competitive retrieval performance. The observed storage savings of 3% to 60% (14%$\pm$13%) can be reliably achieved without compromising retrieval performance for many organs. The proposed strategy enables scalable, cost-efficient WSI indexing and is well-suited for next-generation retrieval-driven clinical AI systems.

---

## 论文详细总结（自动生成）

## 论文结构化总结

### 0. 论文的源代码链接
无（论文未提供源代码链接）

### 1. 论文的核心问题与整体含义
- **研究动机**：数字病理学中全切片图像（WSI）的索引存储成本极高（估计每年需50–150 PB高性能存储），限制了医疗机构的可扩展性和AI临床部署。新兴的检索增强生成（RAG）等生成式AI工作流需要可靠的相似性搜索，进一步加剧了对高效索引的需求。
- **核心问题**：如何在保持检索精度的前提下，大幅压缩WSI索引的存储占用，同时降低计算开销和检索时间。
- **整体含义**：提出一种基于跨类别冗余剪除的策略，使WSI索引更加经济高效，推动数字病理学及检索驱动型临床AI系统的规模化落地。

### 2. 论文提出的方法论
- **核心思想**：不同癌症类别的WSI中，存在大量高度相似的正常组织补丁，这些补丁对跨类别区分贡献极小，属于“对立冗余”（antithetical redundancy）。通过识别并剪除这些补丁，可在无损判别信息的前提下压缩索引。
- **关键技术细节**：
  1. **构建冗余库**：对已标注的WSI，先采用无监督补丁选择算法（如Yottixel）提取代表性补丁，再用基础模型（UNI）提取嵌入。对不同类别的WSI，两两比较补丁嵌入的相似度，将相似度低于全局q分位数阈值的补丁对移除，并存入冗余库。
  2. **冗余剪除**：对于待索引或检索的WSI，其补丁嵌入与冗余库中所有嵌入计算相似度，若低于阈值则移除该补丁，得到精简后的表示集。
  3. **下游处理**：精简后的补丁嵌入转为BOB（二进制条码），通过汉明距离进行Top-k多数投票预测标签。
- **公式/算法流程**（文字说明）：
  - 算法1：构建冗余库——对所有WSI两两跨类计算补丁嵌入相似度，得到全局相似度分布；取q分位数作为阈值；低于阈值的补丁对存入冗余库并从原WSI中删除。
  - 算法2：新WSI冗余剪除与测试——对新WSI的每个补丁嵌入，若与冗余库中任一嵌入的相似度低于阈值则删除；剩余嵌入转为BOB条码；与库中条码计算汉明距离，取Top-k最相似WSI进行多数投票预测标签。

### 3. 实验设计
- **数据集**：TCGA（The Cancer Genome Atlas），包含11,679张WSI，覆盖21个器官（如宫颈、肺、结肠、肾等），每个器官有不同癌症亚型标签。
- **基准方法**：并未与其他冗余减少方法对比，而是以Yottixel作为基线补丁选择算法，对比“使用ARReST前”与“使用ARReST后”的检索性能。
- **评估指标**：准确率（Accuracy）和宏平均F1分数，检索场景包括Top-1、MV@3（前3多数投票）、MV@5。
- **实验设置**：
  - 20×放大率，补丁大小224×224。
  - 特征提取使用UNI模型（公开的病理视觉基础模型，训练于100M图像、100K WSI）。
  - 5折分层组交叉验证（Stratified Group K-Fold），确保同一患者的所有WSI在同一折中，并保持标签分布。
  - 冗余库构建时使用训练折的WSI；测试折的WSI经冗余剪除后再检索。
- **实验充分性**：覆盖21个器官，每个器官5折交叉验证，报告了多个检索场景下的准确率和F1。但未与其他冗余减少方法（如直接减少补丁数、聚类剪枝等）对比，仅做了自身前后对照。

### 4. 资源与算力
- 论文未明确说明使用的GPU型号、数量、训练时长等硬件资源。仅提到使用UNI模型（预训练）进行嵌入提取，但未提及推理或冗余库构建的计算开销细节。

### 5. 实验数量与充分性
- **实验数量**：共21个器官，每个器官5折，报告了6种检索场景×2指标共12组数值。此外还按器官特征分组进行了定性分析（表2）。
- **充分性评价**：
  - 优点：多器官、多指标、交叉验证、留患者策略，实验设计较为严谨。
  - 不足：缺少与其他冗余缩减方法的定量对比；仅使用单一补丁选择算法（Yottixel）和单一特征提取器（UNI）；未测试不同阈值q的影响；未在不平衡数据集上进行专门分析（仅事后根据F1下降指出局限）。
  - 总体客观但对比不足，结论的可推广性有待更多验证。

### 6. 论文的主要结论与发现
- ARReST平均可实现14%±13%的存储节省（范围3%–60%），在准确率指标上基本不损失（26个实验提升，20个不变，17个下降，p值高表明无显著差异）。
- 在F1指标上（适用于非平衡数据集），16个实验提升，9个不变，38个下降，统计显著（p<0.05），表明在非平衡场景下可能损害少数类检索性能。
- 效果高度依赖器官特性：同质性高、类间分离度好的器官（如宫颈、直肠、脑）效果最佳；异质性高、稀有亚型多的器官（如肺、头颈、软组织）可能降低F1。
- 整体结论：ARReST能在保持可接受检索精度的前提下实现有意义的存储压缩，适合作为大规模WSI索引的轻量化方案。

### 7. 优点
- **创新性**：首次提出利用跨类别冗余（对立冗余）来剪除补丁，区别于传统的同类冗余消除。
- **实用性**：可无缝集成到现有WSI索引流水线（如Yottixel+UNI+BOB），只要补丁嵌入可用。
- **效率提升**：除存储节省外，还因嵌板数量减少而加速检索（距离计算量按因子n减少）。
- **实验规模**：涵盖21个器官、11k+张WSI，多指标验证，具有一定说服力。

### 8. 不足与局限
- **对比缺乏**：未与任何其他冗余缩减方法（如随机下采样、基于聚类的剪枝、基于自监督的补丁选择）进行对比，无法说明ARReST的相对优势。
- **阈值依赖**：相似度阈值q为全局固定值，未探索自适应阈值或基于深度学习的相似性估计，可能在不同器官上非最优。
- **不平衡场景表现**：在F1分数上显著下降，对少数类敏感，未提供缓解策略。
- **单一模型与算法**：仅用Yottixel+UNI组合，未测试其他补丁选择算法或基础模型，结果泛化性有限。
- **计算资源未报告**：缺乏算力数据，无法评估方法本身的计算开销是否可接受。
- **应用限制**：仅验证于癌症亚型检索，未扩展到更广泛的组织学任务（如预后、突变预测）或其他医学影像领域。

（完）
