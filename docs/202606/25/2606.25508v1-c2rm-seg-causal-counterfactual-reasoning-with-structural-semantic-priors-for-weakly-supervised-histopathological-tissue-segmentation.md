---
title: "C2RM-Seg: Causal Counterfactual Reasoning with Structural-Semantic Priors for Weakly Supervised Histopathological Tissue Segmentation"
title_zh: "C2RM-Seg: 基于结构语义先验的因果反事实推理用于弱监督组织病理学组织分割"
authors: "Hualong Zhang, Siyang Feng, Zihan Huan, Yi Qian, Zhenbing Liu, Rushi Lan, Xipeng Pan"
date: 2026-06-24
pdf: "https://arxiv.org/pdf/2606.25508v1"
tags: ["query:cpath"]
score: 8.0
evidence: 弱监督组织病理组织分割
tldr: 弱监督组织病理分割中CAM伪标签常受染色外观误导而忽略因果形态。C2RM-Seg提出两阶段框架：分类阶段通过因果反事实推理模块分解特征并干预潜在因素，生成形态对齐CAM；分割阶段采用双路径结构语义架构融合ResNeSt细粒度特征与DINOV3全局先验，经交叉门控机制保持边界，并设计不确定性门控损失抑制噪声。在两个公共数据集上实现了最优分割性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.25508v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1265, \"height\": 584, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25508v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1263, \"height\": 534, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25508v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1121, \"height\": 507, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.25508v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1257, \"height\": 540, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25508v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1238, \"height\": 494, \"label\": \"Table\"}]"
motivation: 现有弱监督组织分割方法受限于CAM伪标签的染色偏差和结构不一致问题，需引入因果推理与结构语义先验。
method: 提出C2RM-Seg，包括因果反事实推理模块、双路径结构语义架构和不确定性门控边际损失。
result: 在两个公共组织病理学数据集上达到最先进性能。
conclusion: 因果反事实推理与结构语义先验有效提升弱监督分割的准确性和一致性。
---

## 摘要
组织病理学组织分割对于计算机辅助诊断至关重要，但弱监督方法常受限于类激活映射（CAM）生成的噪声伪标签。现有CAM方法倾向于关注染色驱动的外观线索而非真正的因果组织形态，导致定位虚假且结构一致性差。为解决此问题，我们提出C$^2$RM-Seg，一个两阶段框架，将因果伪标签精炼与结构感知语义增强相结合。在分类阶段，我们引入因果反事实推理模块（C$^2$RM），将特征分解为潜在因子，并通过学习到的因果结构矩阵执行反事实干预，抑制混淆上下文并生成形态对齐的CAM。在分割阶段，我们设计双路径结构语义架构，结合来自ResNeSt的细粒度结构特征与来自冻结DINOV3基础模型的全局语义先验。交叉路径门控机制利用局部结构线索自适应调节语义注入以保持边界保真度。为进一步减轻残留伪标签噪声，我们提出不确定性门控边界（UGM）损失，基于预测不确定性动态平衡边界强制和置信度学习。在两个公开组织病理学组织数据集上的大量实验表明，C$^2$RM-Seg达到了最先进性能。

## Abstract
Histopathological tissue segmentation is essential for computer-aided diagnosis, yet weakly supervised methods often suffer from noisy pseudo-labels generated by Class Activation Mapping (CAM). Existing CAM approaches tend to focus on staining-driven appearance cues rather than true causal tissue morphology, resulting in spurious localization and poor structural consistency. To address this issue, we propose C$^2$RM-Seg, a two-stage framework that integrates causal pseudo-label refinement with structure-aware semantic enhancement. For classification, we introduce a Causal Counterfactual Reasoning Module (C$^2$RM) that decomposes features into latent factors and performs counterfactual intervention via a learned causal structure matrix, suppressing confounding context and producing morphology-aligned CAMs. For segmentation, we design a Dual-Path Structural-Semantic Architecture that combines fine-grained structural features from ResNeSt with global semantic priors from a frozen DINOV3 foundation model. A cross-path gating mechanism adaptively regulates semantic injection using local structural cues to preserve boundary fidelity. To further mitigate residual pseudo-label noise, we propose an Uncertainty-Gated Margin (UGM) loss, which dynamically balances margin enforcement and confidence learning based on prediction uncertainty. Extensive experiments on two public histopathological tissue datasets show that C$^2$RM-Seg achieves state-of-the-art performance.

---

## 论文详细总结（自动生成）

# C2RM-Seg: 基于结构语义先验的因果反事实推理用于弱监督组织病理学组织分割

## 0. 源代码链接
- 代码开源地址：https://github.com/OceanPetal/C2AM-Seg（论文中标注）

## 1. 核心问题与整体含义（研究动机和背景）
- **问题**：弱监督组织病理学组织分割中，传统的类激活映射（CAM）生成的伪标签常受染色驱动的外观线索（如染色变异、背景纹理等）误导，而非真正的因果组织形态，导致定位虚假、结构一致性差。
- **背景**：完全监督分割需要密集像素级标注，成本高且依赖专家知识。弱监督语义分割（WSSS）仅用图像级标签生成伪标签，但现有CAM方法缺乏对潜在混淆因素的显式建模和因果干预，使得模型偏向于外观而不是因果形态。
- **整体含义**：本文提出C²RM-Seg框架，通过因果反事实推理去除染色混淆，结合基础模型的结构语义先验，生成高保真伪标签并训练鲁棒分割模型，在组织病理学图像上取得最先进性能。

## 2. 方法论：核心思想与关键技术细节
### 核心思想
- 将CAM生成过程建模为结构因果模型（SCM），通过反事实干预抑制混淆上下文（染色外观），提取与形态对齐的因果特征，并利用冻结的DINOV3基础模型提供全局语义先验，同时设计噪声容忍损失。

### 关键技术细节
1. **因果反事实推理模块（C²RM）**
   - 将编码器特征 \(X\) 投影到 \(K\) 个潜在因子子空间（每个因子 \(z_k \in \mathbb{R}^D\)），通过平均池化获得全局因子统计。
   - 学习有向因子邻接矩阵 \(A \in \mathbb{R}^{K \times K}\)（对角掩码、行softmax），用于估计因子间的混淆贡献：\(E_i = \sum_{j\neq i} A_{i,j} z_j\)。
   - 执行线性反事实干预：\(z_i^{cf} = z_i - E_i\)，即 \(Z^{cf} = Z - AZ\)，去除父节点影响，保留形态主导信号。
   - 将反事实因子通过门控残差校正注入原始空间特征：\(\hat{X} = X + \gamma \cdot (\sigma(R(\text{Broadcast}(Z^{cf}))) \odot X)\)。

2. **双路径结构语义分割架构**
   - **结构分支**：使用ResNeSt-200e骨干网络 + 金字塔池化模块提取多尺度结构特征 \(F_S\)。
   - **语义分支**：使用冻结的DINOV3（ViT-B/16）编码器 + 可学习适配器，将全局语义嵌入投影到结构流形，通过上采样对齐空间尺寸：\(F_V = \text{Up}(\sigma(B(W_{ad} \cdot E_{\text{ViT}}(X))))\)。
   - **交叉路径门控机制**：从结构特征导出空间门控图 \(G = \sigma(\Psi(F_S))\)，用于调节语义注入：\(\hat{F} = F_S + G \odot F_V\)，确保语义集中在结构显著区域。

3. **不确定性门控边际（UGM）损失**
   - 对每个像素 \(i\)，定义边际违反 \(M_i = [m - (p_{i,y} - p_{i,s})]_+\) 和置信度惩罚 \(C_i = -\log(p_{i,y}+\epsilon)\)。
   - 根据预测不确定性 \(u_i = 1 - p_{i,y}\)，用S型函数动态分配权重，组合损失：
     \[
     \mathcal{L} = \frac{1}{|\Omega|}\sum_{i\in\Omega} \left[ \frac{1}{1+e^{-\kappa(u_i-0.5)}} C_i + \left(1 - \frac{1}{1+e^{-\kappa(u_i-0.5)}} \right) M_i \right]
     \]
   - 低不确定度时优先扩大决策边界，高不确定度时（可能误标区域）采用保守的置信度惩罚。

## 3. 实验设计
- **数据集**
  - **LUAD-HistoSeg**（肺腺癌）：54张全切片图像，4种表型（TE, TAS, NEC, LYM），16,678训练补丁（图像级标签），607验证/测试补丁（高密度像素级标注）。
  - **BCSS**（乳腺癌）：151张全切片图像，4种组织（TUM, STR, LYM, NEC），23,422训练补丁，8,404验证/测试补丁。
- **Benchmark**：标准弱监督语义分割指标——mIoU、bIoU（边界IoU）、HD95（Hausdorff距离第95百分位数）。
- **对比方法**：包括完全弱监督方法（TPRO、HAMIL、MLPS、SIPE、ARML、OEEM、ESFAN）以及使用基础模型作为基准的方法（SAM、SAM2、MedSAM，均用第一阶段的伪标签作为提示）。所有方法在相同设置下比较。

## 4. 资源与算力
- 文中未明确说明GPU型号、数量及训练总时长。仅提到：“Experiments are conducted in PyTorch on a NVIDIA RTX 3090.” 未提及多卡训练或具体训练时间。分类阶段训练20个epochs（batch size=20），分割阶段超参数已给出（LR等），但未报告整体训练耗时。

## 5. 实验数量与充分性
- **主要实验**：两个数据集上的全面定量比较（表1），与9种前沿方法对比，重复5次报告均值和标准差（±符号）。
- **消融实验**：表2展示了LUAD-HistoSeg上C²RM、Dual-Path架构、UGM损失三个模块的逐步消融（3个独立模块组合为7种情况），验证每个组件的独立贡献和协同效果。
- **可视化分析**：图2定性分割对比，图3 t-SNE可视化特征空间（显示C²RM去除染色混淆后的聚类效果）。
- 实验设计较为充分：覆盖主流数据集、多个对比方法、重复运行、消融研究、可视化。但缺少更多泛化性测试（如其他器官或染色方法）及与其他WSSS变体在计算效率上的详细对比（虽有GFLOPs/FPS，但未讨论显存占用）。

## 6. 主要结论与发现
- C²RM-Seg在BCSS-WSSS和LUAD-HistoSeg上均达到最优mIoU（72.17%和79.62%）、bIoU（49.52%和49.47%）和HD95（27.31和18.07），显著优于ESFAN等先进方法。
- 因果反事实推理有效消除了染色驱动的混淆，生成形态对齐的伪标签；双路径架构融合结构先验与基础模型语义，改善边界保真度；UGM损失通过不确定性感知动态加权有效抑制伪标签噪声。
- 各模块独立均可提升性能，联合使用实现最大增益，表明因果纠正、结构语义融合、噪声容忍优化三者协同作用。

## 7. 优点
- **方法创新**：首次将因果反事实推理引入弱监督组织分割，通过显式建模潜在混淆因子（染色外观）并执行反事实干预，从根本上解决CAM偏置问题，思路新颖且具有理论支撑（SCM）。
- **框架完整性**：两阶段端到端设计，分类阶段生成高质量伪标签，分割阶段利用基础模型先验和自适应门控机制，并配套噪声容忍损失，形成闭环。
- **实验严谨性**：在多个数据集上，与多种方法（包括基础模型SAM系列）公平对比，采用多次重复（5次）并报告标准差，消融实验完整，可视化直观。
- **实际可用性**：虽集成大模型（DINOV3），但通过冻结和轻量适配器保持计算效率（191.47 GFLOPs, 86.08 FPS），具有临床部署潜力。

## 8. 不足与局限
- **实验覆盖不全**：仅使用两个组织病理学数据集（肺癌和乳腺癌），未在其他器官（如结肠、前列腺）或染色方法（如IHC）上验证，泛化性有待证明。
- **计算资源未详细报告**：未提供训练时间、显存占用、多卡训练策略等，不利于复现和效率评估。
- **方法局限性**：
  - 线性SCM假设（\(Z^{cf}=Z-AZ\)）可能不足以建模复杂的非线性因果交互；非线性因果模型可能提升效果。
  - 冷冻DINOV3虽防止过拟合噪声，但可能限制对特定病理形态的适应能力；未来可探索任务适配微调策略。
  - UGM损失中的超参数 \(m\) 和 \(\kappa\) 需要手动调节，缺乏自适应优化机制。
- **潜在偏差风险**：伪标签质量仍依赖第一阶段CAM的因果校正效果，若染色混淆极其严重或存在未见过的染色模式，C²RM可能失效。
- **与相关工作的讨论**：仅对比方法性能，未深入分析为何其他方法（如SAM系列）在提示下效果不如本方法的原因（可能因为伪标签本身噪声大而基础模型对噪声敏感），需更多推理。

（完）
