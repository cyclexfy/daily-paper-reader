name: Debug Secrets
on: workflow_dispatch

jobs:
  debug:
    runs-on: ubuntu-latest
    env:
      LLM_PRIMARY_BASE_URL: ${{ secrets.LLM_PRIMARY_BASE_URL }}  # 将Secrets注入为环境变量
    steps:
      - name: Print Base URL
        run: |
          echo "Base URL: $LLM_PRIMARY_BASE_URL"  # 打印环境变量（显示实际值）
