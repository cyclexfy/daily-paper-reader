name: Debug Secrets
on: workflow_dispatch

jobs:
  debug:
    runs-on: ubuntu-latest
    env:
      LLM_PRIMARY_BASE_URL: ${{ secrets.LLM_PRIMARY_BASE_URL }}
    steps:
      - name: Print Base URL
        run: |
          echo "Base URL: $LLM_PRIMARY_BASE_URL"
